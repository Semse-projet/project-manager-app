---
type: spec
id: "tools.lien-rights-management"
title: "Lien Rights Management"
feature: "Lien Rights Management"
domain: "legal"
version: "1.0"
status: "APPROVED"
owner: "semse-core"
risk: "high"
module: "2.1"
blocks: "2.1.A → 2.1.F"
date: "2026-06-21"
author: "Agentic Harness — SDD driven"
spec_index: "docs/SPEC_INDEX.md"
depends_on:
  - "docs/specs/tools/fase-2/README.md"
  - "docs/specs/api/payments.spec.md"
related_files:
  - apps/api/src/modules/liens
  - apps/api/src/integrations/liengrid.ts
  - apps/worker/src/main.mjs
  - apps/api/src/modules/payments/escrow-release.service.ts
  - apps/api/src/modules/payments/payment-governance.service.ts
  - apps/api/src/modules/payment-governance/payment-governance.service.ts
related_tests:
  - apps/api/test/bloque-u-liens-automation.test.ts
  - apps/api/test/liens-integration.test.ts
  - apps/api/test/notice-generator-contract-amount.test.ts
  - apps/api/test/notice-send-address-parsing.test.ts
  - apps/api/test/lien-alerts-scheduler-integration.test.ts
  - apps/api/test/liens-service-create-calendar.test.ts
  - apps/api/test/waiver-controller-get-sign-url.test.ts
related_endpoints:
  - v1/projects/:projectId/liens
  - v1/admin/liens/check-deadlines
related_events: []
related_agents: []
last_verified: "2026-08-27"
---

# Spec: Lien Rights Management (LienGrid API)

> **Propósito:** Automatizar deadlines de preliminary notice, waivers, y calendarios de liens en 50 estados US.
> 
> **Para quién:** Contratista que necesita protección legal automática · OPS_ADMIN que supervisa compliance · Cliente que necesita certainty en escrow.
> 
> **Privacidad:** `true` — Project address es location-critical, encriptar en reposo.

---

## 1. Qué Resuelve

El contratista debe presentar "preliminary notices" en plazos muy breves (25-35 días en muchos estados) o pierde derechos de lien.

**Problema:**
- 50 estados = 50 plazo diferentes
- Plazo perdido = pérdida de derechos legales
- Abogados cobran $300-500 por notice
- Contratista no sabe ni que existe

**Solución:**
1. Al crear proyecto: capturar dirección y estado
2. Consultar LienGrid API para deadlines automáticos
3. Crear calendario con alertas (30 días, 7 días, 3 días, 1 día)
4. Generar preliminary notices pre-poblados con datos del proyecto
5. Enviar vía correo certificado digital (Lob.com)
6. Capturar firma de waiver en milestones

**Resultado:** Contratista protegido automáticamente. Cero trabajo manual.

---

## 2. Actores y Permisos

| Actor | Rol | Puede hacer | No puede hacer |
|-------|-----|-------------|----------------|
| Contratista | `PRO` | Ver calendario de liens · Firmar waivers | Modificar deadlines · Ignorar alertas sin riesgo |
| Cliente | `CLIENT` | Ver que liens están protegidos | Acceder a detalles legales |
| OPS Admin | `OPS_ADMIN` | Ver calendarios de todos · Resend notices · Audit trail | Modificar detalles de ley |
| Plataforma | `PLATFORM` | Crear calendarios · Enviar alertas · Integrar LienGrid + Lob | — |

**Permisos:**
- `projects:liens:read` — ver calendario
- `projects:liens:write` — firmar waivers (solo PRO del proyecto)
- `admin:liens:write` — resend notices (OPS_ADMIN)

---

## 3. Máquina de Estados — Lien Calendar

**Entidad:** `LienCalendar` (uno por proyecto + estado)

```
                   Proyecto creado
                    + dirección
                         │
                         ▼
   PENDING ◄─────────────────────────────┐
      │                                   │
  LienGrid consulta                       │
  deadlines ley                           │
      │                                   │
      ▼                                   │
   CREATED                              RETRY
      │                                   ▲
  30 días antes                           │
  (alerta)                                │
      │                                   │
      ▼                                   │
  ALERTED_30D ───────── NO NOTICE SENT ──┘
      │
  7 días antes (2ª alerta)
      │
      ▼
  ALERTED_7D
      │
  3 días antes (3ª alerta)
      │
      ▼
  ALERTED_3D
      │
  Notice enviado vía Lob.com
      │
      ▼
  NOTICE_SENT
      │
  ¿Confirmación de entrega?
      │
      ├──→ SÍ ──→ NOTICE_DELIVERED
      │
      └──→ NO ──→ DELIVERY_FAILED (vuelve a RETRY)

[TERMINAL: NOTICE_DELIVERED, NOTICE_FAILED]
[INVARIANTE: un único calendar por proyecto+estado]
```

### Transiciones

| Desde | Hacia | Trigger | Actor | Efecto |
|-------|-------|---------|-------|--------|
| `PENDING` | `CREATED` | LienGrid API responde con deadlines | PLATFORM (scheduler) | Guardar deadlines en BD |
| `CREATED` | `ALERTED_30D` | T-30 días antes deadline | PLATFORM (scheduler) | Push notification + email al PRO |
| `ALERTED_30D` | `ALERTED_7D` | T-7 días antes deadline | PLATFORM (scheduler) | 2ª notificación (urgencia) |
| `ALERTED_7D` | `ALERTED_3D` | T-3 días antes deadline | PLATFORM (scheduler) | 3ª notificación (final) |
| `ALERTED_3D` | `NOTICE_SENT` | PRO acepta envío O sistema auto-envía | PLATFORM | Crear PDF notice + enviar Lob |
| `NOTICE_SENT` | `NOTICE_DELIVERED` | Webhook de Lob: entrega confirmada | PLATFORM (webhook) | Audit log + mark as complete |
| `NOTICE_SENT` | `DELIVERY_FAILED` | Webhook de Lob: bounce/error | PLATFORM (webhook) | Volver a RETRY |
| cualquiera | `RETRY` | Error en integración | PLATFORM | Reintentar (max 3 veces) |

---

## 4. Contratos de API

### 4.1 Integración con LienGrid

**Endpoint LienGrid:** `POST https://api.liengrid.com/v1/deadlines`

**Request:**
```json
{
  "address": "123 Main St, San Francisco, CA 94102",
  "state": "CA",
  "projectStartDate": "2026-07-01",
  "projectDescription": "Roof replacement",
  "contractAmount": 50000,
  "apiKey": "${LIENGRID_API_KEY}"
}
```

**Response:**
```json
{
  "state": "CA",
  "preliminaryNoticeDeadline": "2026-08-10",
  "waiverDeadline": "2026-09-15",
  "finalNoticeDeadline": "2026-10-20",
  "statusLienDeadline": "2026-12-20",
  "requiresNotary": false,
  "requiresCertifiedMail": true,
  "recipientTypes": ["owner", "general_contractor", "lender"]
}
```

### 4.2 Endpoints SEMSE

**POST /v1/projects/:projectId/liens/calendar**

Crear calendario de liens para un proyecto.

```bash
curl -X POST http://localhost:4000/v1/projects/proj_abc/liens/calendar \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{
    "stateName": "California",
    "projectStartDate": "2026-07-01"
  }'
```

Response:
```json
{
  "id": "lien_cal_123",
  "projectId": "proj_abc",
  "stateName": "California",
  "preliminaryNoticeDeadline": "2026-08-10",
  "waiverDeadline": "2026-09-15",
  "status": "CREATED",
  "createdAt": "2026-06-21T10:00:00Z"
}
```

**GET /v1/projects/:projectId/liens/calendar**

Obtener calendario de liens.

```bash
curl -H "Authorization: Bearer ${TOKEN}" \
  http://localhost:4000/v1/projects/proj_abc/liens/calendar
```

Response:
```json
[
  {
    "id": "lien_cal_123",
    "stateName": "California",
    "preliminaryNoticeDeadline": "2026-08-10",
    "waiverDeadline": "2026-09-15",
    "status": "ALERTED_7D",
    "nextAlert": "2026-08-03T00:00:00Z"
  }
]
```

**POST /v1/projects/:projectId/liens/send-notice**

Enviar preliminary notice vía Lob.com (correo certificado).

```bash
curl -X POST http://localhost:4000/v1/projects/proj_abc/liens/send-notice \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{
    "lienCalendarId": "lien_cal_123",
    "recipientType": "owner",
    "sendMethod": "certified_mail"
  }'
```

Response:
```json
{
  "id": "notice_send_123",
  "lienCalendarId": "lien_cal_123",
  "noticeGeneratedAt": "2026-06-21T10:00:00Z",
  "sentVia": "certified_mail",
  "lobLetterTrackingId": "usps_123abc",
  "status": "NOTICE_SENT",
  "expectedDelivery": "2026-06-28T00:00:00Z"
}
```

**GET /v1/projects/:projectId/liens/waivers**

Obtener lista de waivers pendientes (para FSM de milestone).

```bash
curl -H "Authorization: Bearer ${TOKEN}" \
  http://localhost:4000/v1/projects/proj_abc/liens/waivers
```

Response:
```json
[
  {
    "id": "waiver_123",
    "lienCalendarId": "lien_cal_123",
    "type": "conditional",
    "releaseAmount": 25000,
    "requiredBefore": "2026-09-15T00:00:00Z",
    "status": "PENDING",
    "signingUrl": "https://semse.app/sign/waiver/waiver_123?token=xyz"
  }
]
```

---

## 5. Integración con Payments FSM

**Gate antes de `RELEASED`:**

```ts
// En PaymentGovernanceService
async authorizeRelease(escrowId: string): Promise<boolean> {
  const escrow = await prisma.paymentEscrow.findUnique({ 
    where: { id: escrowId },
    include: { milestone: { include: { project: true } } }
  });

  // Check: ¿hay liens pendientes para esta release?
  const pendingWaivers = await prisma.lienWaiver.findMany({
    where: {
      lienCalendar: { projectId: escrow.milestone.project.id },
      releaseAmount: escrow.amount,
      status: 'PENDING'
    }
  });

  if (pendingWaivers.length > 0) {
    return false; // BLOCK RELEASE
  }

  // Approve release
  return true;
}
```

**Efecto:** Cliente no puede liberar escrow hasta que PRO firme waiver.

---

## 6. Integración con Change Orders

**Ante un change order aprobado:**

1. Recalcular lien deadlines (si aumentó contract amount)
2. Crear nuevas alertas si es necesario
3. Notificar PRO de deadline extendido

```ts
async onChangeOrderApproved(changeOrder: ChangeOrder) {
  const { projectId, newContractAmount } = changeOrder;
  const lienCals = await prisma.lienCalendar.findMany({
    where: { projectId }
  });

  for (const cal of lienCals) {
    // Reconsultar LienGrid con nuevo monto
    const updated = await liengrid.getDeadlines({
      contractAmount: newContractAmount,
      state: cal.stateName
    });
    
    // Actualizar si cambió
    await prisma.lienCalendar.update({
      where: { id: cal.id },
      data: updated
    });
  }
}
```

---

## 7. Casos de Uso

### UC-2.1.1: Crear Proyecto en California

**Actor:** PRO (contratista)

1. Crear proyecto con dirección en California
2. Sistema consulta LienGrid → deadline 2026-08-10
3. Crear LienCalendar con estado CREATED
4. Schedulear alertas para 30d, 7d, 3d, 1d antes

**Resultado:** PRO recibe notificaciones automáticas. Sin hacer nada.

### UC-2.1.2: Alerta de Preliminary Notice (7 días antes)

**Actor:** PLATFORM (scheduler)

1. Cada hora, buscar LienCalendars donde T-7 días = hoy
2. Cambiar a ALERTED_7D
3. Enviar push + email: "Preliminary notice due in 7 days"
4. Incluir link "Send Now" que llama a send-notice endpoint

**Resultado:** PRO puede enviar con 1 click. O ignorar y recibir alerta final en 3 días.

### UC-2.1.3: Enviar Preliminary Notice

**Actor:** PRO o PLATFORM (auto)

1. PRO hace click en "Send Now" o system auto-sends en deadline-1d
2. Sistema genera PDF pre-poblado con:
   - Project address, description, start date
   - Contractor info (desde Profile)
   - Required recipients (from LienGrid response)
3. Llamar Lob.com API para envío certificado
4. Guardar tracking ID de Lob
5. Cambiar estado a NOTICE_SENT

**Resultado:** Notice enviado vía USPS certified mail. Tracking automático.

### UC-2.1.4: Recibir Confirmación de Entrega

**Actor:** PLATFORM (webhook from Lob.com)

1. Lob.com webhook: `letter_delivered` event
2. Actualizar LienCalendar a NOTICE_DELIVERED
3. Emit event: `lien.notice_delivered`
4. Agents pueden reaccionar (ej: Auto-unlock escrow waiver)

**Resultado:** Audit trail completo. System knows notice was delivered.

### UC-2.1.5: Firmar Conditional Waiver (antes de release)

**Actor:** PRO (cuando hace click en Payment Release)

1. System detecta pending waivers
2. Redirigir a página de firma digital
3. PRO firma waiver condicional
4. Guardar firma + timestamp
5. Actualizar LienWaiver a SIGNED
6. Unlock escrow release

**Resultado:** Client puede liberar escrow. PRO protegido legalmente.

---

## 8. Notas Técnicas

### LienGrid API Cost Model

- **Setup:** $0 (no hay setup fee)
- **Per-notice:** $0.50 - $2.00 (variación por estado)
- **Estimated:** ~$5-20 por proyecto (1-10 notices)

**Implementation:** Stripe-like API key, consumo por API call.

### Lob.com Integration

- **Setup:** $0 (no setup fee)
- **Per-letter:** ~$0.50 (USPS first class + handling)
- **API:** REST, webhooks para delivery tracking
- **Rate limit:** 100 reqs/sec (suficiente)

### Retry Strategy

- **LienGrid API fails:** Reintentar hasta 3 veces con backoff exponencial
- **Lob.com send fails:** Reintentar hasta 5 veces (más crítico)
- **Webhook not received:** Manual resend vía OPS endpoint

### Privacy & Security

- **Address:** Encriptado en reposo (AES-256)
- **API keys:** Nunca enviar al frontend; solo backend
- **Waivers:** Firmados digitalmente (no plain text)
- **Audit log:** Toda transacción de notice/waiver queda registrada

### Testing

- **Mock LienGrid:** Fixture de respuestas por estado
- **Mock Lob.com:** Fixture de tracking IDs
- **Unit tests:** Máquina de estados, transitions
- **Integration tests:** Full flow creation → delivery

No necesita datos reales; todo simulable.

---

## 9. Bloques de Implementación

| Bloque | Qué | Dependencias |
|--------|-----|---|
| 2.1.A | Integrar LienGrid API | Nada |
| 2.1.B | Capturar datos → calendario | 2.1.A |
| 2.1.C | Alertas automáticas + notices | 2.1.B |
| 2.1.D | Generación de notices pre-poblados | 2.1.C |
| 2.1.E | Envío vía Lob.com + tracking | 2.1.D |
| 2.1.F | Waivers + integración payments FSM | 2.1.E |

---

## 10. Artefactos de Código

- `apps/api/src/modules/liens/liens.service.ts` — servicios principales
- `apps/api/src/modules/liens/liens.controller.ts` — endpoints REST
- `apps/api/src/integrations/liengrid.ts` — cliente LienGrid
- `apps/api/src/integrations/lob.ts` — cliente Lob.com
- `packages/db/prisma/schema.prisma` — modelos `LienCalendar`, `LienWaiver`, `LienNotice`
- `apps/api/test/liens-integration.test.ts` — tests end-to-end

---

## 11. Siguiente

**Bloque 2.1.B:** Capturar datos de proyecto → generar calendario de deadlines automáticamente.

## 12. Estado real (reconciliado 2026-08-27)

**⚠️ HALLAZGO CRÍTICO — NO CORREGIDO, requiere decisión humana explícita
antes de tocar código de payment-governance/fund-release (fuera de mi
autoridad en esta sesión):** la sección 5 de este mismo spec
("Integración con Payments FSM") documenta un gate obligatorio —
`PaymentGovernanceService.authorizeRelease()` debe bloquear la liberación
de un escrow si existen `LienWaiver` en estado `PENDING` para ese
proyecto/monto. Ese gate **existe como código** —
`apps/api/src/modules/liens/waiver-payment-gate.service.ts`
(`WaiverPaymentGateService.authorizeRelease()`/`canReleaseEscrow()`/
`getBlockingReason()`, lógica correcta, `@Injectable()`) — pero
**no está registrado en ningún `@Module`** (confirmado con un barrido
mecánico de las 168 clases `@Injectable` de `apps/api/src/modules` contra
todos los `providers` de todos los `.module.ts` — sólo 3 huérfanas, ver
abajo) y **cero referencias a "lien"/"waiver" existen en
`apps/api/src/modules/payments/escrow-release.service.ts` ni en
cualquiera de los dos `payment-governance.service.ts` del repo**
(`modules/payments/` y `modules/payment-governance/`). Es decir: **el
flujo real de liberación de fondos hoy no verifica waivers de lien en
absoluto** — el gate que este spec exige nunca fue conectado.

No lo conecté yo mismo en este pase: tocar el camino real de liberación
de escrow es exactamente el tipo de cambio que esta sesión tiene
instrucción explícita de no hacer sin sign-off humano previo, y el propio
snippet de la sección 5 de este spec está escrito contra un shape de
`PaymentEscrow` que tampoco es el real (referencia `escrow.milestone.
project.id`, pero `PaymentEscrow` no tiene relación `milestone` — tiene
`projectId` directo) — adaptarlo bien requiere una decisión de quién
revisa el cambio antes de que toque dinero real, no solo una corrección
mecánica de nombres de campo como las de abajo. Queda documentado aquí
como el gap más importante de todo este spec, a resolver con sign-off
explícito de producto/seguridad antes de cualquier código.

**Hallazgo más severo entre los que sí corregí en este pase:**
auditar el resto del módulo contra el schema real (y confirmar cada caso
con `tsc` contra los tipos generados de Prisma, sin necesitar una base de
datos real) encontró que dos consultas Prisma más usaban `select` con
campos que no existen en `Project` — **`Prisma.ProjectSelect` no acepta
`name`/`address` en absoluto**, así que estas dos consultas nunca fueron
solo "datos incorrectos": Prisma las habría rechazado directamente en
tiempo de ejecución, antes de llegar a cualquier lógica.
- `LienAlertsScheduler.checkAndAlertDeadlines()` — el `select` del `include`
  de `project` pedía `name`/`address`. Esta es la función que la
  automatización de este mismo documento (arriba) dispara cada hora — con
  el bug, habría fallado en su primera consulta cada vez que corriera,
  nunca llegando a transicionar ningún calendario.
- `LiensService.createLienCalendar()` — el `select` de `project` pedía
  `address`. **Este es el endpoint público y ya alcanzable
  `POST /v1/projects/:projectId/liens/calendar`** — a diferencia de
  `NoticeSendService`, esta ruta sí está registrada y expuesta; cualquier
  llamada real a este endpoint, desde que se escribió, habría fallado.

Ambos corregidos 2026-08-27: `select: { ..., job: { select: { title/
location: true } } }` en vez de los campos inexistentes. 4 tests nuevos
(`lien-alerts-scheduler-integration.test.ts`,
`liens-service-create-calendar.test.ts`) instancian las clases reales
contra un Prisma falso — el patrón que faltaba en todo este módulo, cuya
suite existente reimplementa la lógica en línea en vez de invocar las
clases reales, razón por la que ninguno de estos bugs (éste, el de
`${{contractAmount}}`, ni el de `project.name`/`address`/`startDate` de
`NoticeGeneratorService`) había sido detectado antes.

**Verificación sistemática, no solo grep manual (2026-08-27):** dos
técnicas mecánicas cerraron el resto de la auditoría de este módulo:
1. Un script cruzó las 168 clases `@Injectable` de `apps/api/src/modules`
   contra los `providers` de todos los `.module.ts` — encontró exactamente
   3 huérfanas: `NoticeSendService`/`PortfolioService` (ya documentadas
   arriba) y `WaiverPaymentGateService` (ver el hallazgo crítico al inicio
   de esta sección).
2. Se quitó temporalmente `@ts-nocheck` de los 3 archivos de este módulo
   que aún no se habían auditado a fondo (`waiver.controller.ts`,
   `governance.service.ts`, `milestones.repository.ts` — este último fuera
   de `liens/` pero listado igual por tener el mismo pragma) y se corrió
   `tsc --noEmit` contra los tipos reales. `governance.service.ts` y
   `milestones.repository.ts` compilaron limpio — sin cambios, se les dejó
   sin `@ts-nocheck` como mejora de tipado permanente ya que no dependían
   de la supresión para nada. `waiver.controller.ts` sí tenía un bug real:
   `WaiverController.getSignUrl()` llamaba a
   `this.liensService.getLienWaiver(waiverId)` (singular) — un método que
   nunca existió en `LiensService` (solo `getLienWaivers`, plural, con otro
   propósito: todos los waivers pendientes de un proyecto, no uno por id).
   Cualquier llamada real a
   `GET /v1/projects/:projectId/liens/waivers/:waiverId/sign-url` habría
   lanzado `TypeError`. Corregido agregando `LiensService.getLienWaiver()`
   (mismo patrón de `findUniqueOrThrow` que ya usa `signWaiver()`); también
   se le quitó `@ts-nocheck`, ahora que compila limpio. 2 tests nuevos en
   `waiver-controller-get-sign-url.test.ts`.

**Última pasada — quitar `@ts-nocheck` de los 5 archivos restantes del
módulo (2026-08-27):** encontró 3 bugs reales más, todos en escritura de
datos (Prisma habría rechazado o corrompido el `create`, no solo un
`select` inválido como los de arriba):
- `NoticeGeneratorService.generateNoticeFromCalendar()` y el método
  gemelo sin caller `LiensService.createNotice()` — ambos hacían
  `lienNotice.create()` **sin `sentVia`**, un campo `String` requerido en
  el schema (`"certified_mail" | "email" | "manual"`, sin `@default`).
  Corregido con `sentVia: 'certified_mail'` (el único canal que este
  módulo integra de verdad, vía Lob.com). Sin este fix, cada notice
  automático que el scheduler ahora dispara cada hora habría fallado en
  este `create()`, aun después de las tres correcciones anteriores.
- `LiensService.createWaiver()` (sin caller real) — guardaba
  `releaseAmount` como `BigInt(monto_en_dólares * 100)` (centavos), pero
  el campo es `Decimal(15,2)` en el schema (dólares, no centavos enteros)
  — y `checkWaiverRequirements()`, la función que sí importa (la que
  alimenta el gate de pagos del hallazgo crítico de arriba), compara
  `releaseAmount` como dólares planos. Un desfase de ~100x entre cómo se
  escribía y cómo se leía el mismo campo. Corregido a
  `releaseAmount: data.releaseAmount ?? null`.
- `LiensService.createLienCalendar()` — pasaba el objeto tipado
  `LienGridDeadlines` directo a un campo `Json` de Prisma; solo un
  problema de tipos (el objeto es JSON-serializable sin pérdida), resuelto
  con el cast `as unknown as Prisma.InputJsonValue` que ya se usa en otros
  módulos de este repo.

`governance.service.ts`/`milestones.repository.ts` y ahora
`liens.service.ts`/`notice-generator.service.ts`/
`notice-send.service.ts`/`lien-alerts.scheduler.ts` quedan sin
`@ts-nocheck` de forma permanente — todos compilan limpio.
`portfolio.service.ts` conserva el pragma a propósito (sigue sin spec
aprobado ni reescritura, solo el comentario de aviso de antes).

Este spec quedó congelado en 2026-07-12 mientras la implementación siguió
avanzando sin actualizarlo — mismo patrón de documentación desincronizada
que apareció varias veces en este proyecto.

**Corrección posterior en la misma sesión:** el hallazgo de `${{contractAmount}}`
de abajo llevó a auditar el resto de `generateNoticeFromCalendar()` contra el
schema real, y aparecieron dos bugs más de la misma familia — campos que el
código asume que existen en `Project` pero que en realidad no existen ahí:
- `Project` no tiene `name`/`address`/`startDate` — el nombre y la ubicación
  viven en `Project.job.title`/`Project.job.location` (relación con `Job`);
  la fecha de inicio real es `Project.startAt`. Corregido en
  `notice-generator.service.ts` — **este es el camino que sí es alcanzable**
  hoy (vía el scheduler automatizado más arriba).
- El mismo patrón existía en `NoticeSendService.sendNotice()`
  (`notice-send.service.ts`): leía `project.address` (inexistente) y además
  tenía `city: 'City'` y `zip: '12345'` **hardcodeados** para el envío de
  correo certificado real vía Lob.com — cada carta real habría ido a una
  dirección falsa e infranqueable. Corregido con un parser real de
  `"calle, ciudad, ESTADO zip"` (mismo formato ya asumido por
  `ProjectLiensService.extractStateFromAddress()` en este mismo módulo) que
  **falla explícitamente** (lanza, no manda) si la ubicación falta o no
  tiene ese formato — nunca manda a una dirección adivinada.
  **Importante sobre alcance real:** al investigar esto se confirmó que
  `NoticeSendService` **no está registrado como provider en `LiensModule`**
  y `LobClient` **no está provisto en ningún módulo de la API** — este
  servicio no es alcanzable desde ningún request real hoy, igual que
  `PortfolioService` (`apps/api/src/modules/portfolio/portfolio.service.ts`).
  El fix queda igual de correcto y vale la pena tenerlo, pero no era una
  fuga activa en producción — es una corrección preventiva para cuando (si)
  se cablee.

- `LienAlertsScheduler.checkAndAlertDeadlines()` (transiciones de estado)
  estaba completamente implementado y probado, pero solo se disparaba
  manualmente vía `POST /v1/admin/liens/check-deadlines` — el comentario del
  propio archivo decía "debe ser ejecutado cada hora por BullMQ o similar",
  pero nada lo ejecutaba. Corregido 2026-08-27: `apps/worker/src/main.mjs`
  ahora dispara este endpoint cada hora, detrás del kill switch
  `LIEN_ALERTS_ENABLED` (default off, documentado en `apps/api/.env.example`).
- **Hallazgo crítico, no solo un gap de checklist:** la generación
  automática de notices en `ALERTED_3D` — que este mismo documento describía
  como "completamente implementada" en una redacción anterior de esta
  sección — en realidad **fallaba en el 100% de los casos**, silenciosamente.
  `NoticeGeneratorService.getNoticeTemplate()` tiene el placeholder
  `${{contractAmount}}` dentro de un template literal de JS (backticks): el
  `$` inmediatamente antes de `{{` hace que el motor de JS lo interprete
  como interpolación real (`${ {contractAmount} }`, azúcar sintáctico de
  objeto), no como el placeholder `{{variable}}` que `populateTemplate()`
  reemplaza por regex — y lanzaba `ReferenceError: contractAmount is not
  defined` en cada invocación. `generateAllNoticesForCalendar()` no atrapa
  ese error, así que se propagaba hasta el `try/catch` del scheduler
  (`checkAndAlertDeadlines`), que solo lo loggeaba como advertencia y
  seguía con el resto — nunca se generó un notice real desde este código,
  en ningún ambiente, desde que se escribió. Corregido 2026-08-27
  (`\${{contractAmount}}`, escapando el `$` para que sea texto literal).
  De paso también se corrigió `contractAmount: 0` hardcodeado (line
  original) por `PaymentEscrow.totalAmount` — el valor real más cercano a
  "monto del contrato" en el modelo de datos hoy (`Contract` no tiene campo
  de monto). 2 tests nuevos en
  `apps/api/test/notice-generator-contract-amount.test.ts` que ejercitan
  `generateNoticeFromCalendar()` de punta a punta contra el HTML real
  generado — los tests existentes de este módulo (`bloque-v-notice-
  generation.test.ts`) nunca habían detectado el bug porque reimplementan
  la sustitución de template inline en vez de invocar el servicio real.
- **Gap real que sigue abierto:** el push + email al PRO en cada transición
  (`ALERTED_30D`/`ALERTED_7D`/`ALERTED_3D`) nunca se implementó — es un
  `TODO` comentado en `lien-alerts.scheduler.ts`, no solo un checkbox sin
  marcar. No se implementó a ciegas en este pase porque requiere resolver
  un problema real: `Project.assignedProOrgId` es un `Org`, no un `userId`,
  y este módulo no tiene ninguna noción establecida de "usuario principal
  de un Org" para dirigir un aviso legal — asumir un criterio (¿primer
  miembro? ¿owner del Org?) sin confirmarlo con producto podría mandar (o
  fallar en mandar) un aviso de deadline legal a la persona equivocada.
  Necesita una decisión de producto antes de codificarse.
- **Gap real que sigue abierto:** `NoticeGeneratorService.generatePdfFromHtml()`
  no genera un PDF — devuelve el HTML crudo como `Buffer`, con un comentario
  explícito de que es un placeholder ("por ahora: retornar HTML como
  Buffer"). No tocado en este pase; requiere agregar `pdfkit` como
  dependencia real, un cambio más grande que los de arriba.
