# Plan de remediación — Auditoría SEMSE 2026-07

> Documento vivo. Se actualiza a medida que se audita cada módulo (Cliente → Worker → Admin) y a medida que se corrige cada hallazgo. No es un reporte de una sola sesión — es el backlog de trabajo.

## Cómo usar este documento

- Cada hallazgo tiene: severidad, estado (`[ ]` pendiente / `[x]` corregido), archivo:línea, qué está mal, y hacia dónde apunta el fix.
- El narrativo completo (por qué importa, cómo se verificó, capturas de pantalla en vivo) está en el reporte original: artefacto **"SEMSE — Auditoría de UI/UX y backend"**, publicado en esta cuenta de Claude — pídele a Claude que lo busque con `action: "list"` si necesitas el contexto largo de un hallazgo específico. Este documento es el resumen accionable, no el reemplazo.
- Orden de trabajo sugerido: **Sección 0 (seguridad transversal) primero**, sin importar en qué módulo estés — son los hallazgos más graves y no son específicos de una sola pantalla. Después, módulo por módulo: Cliente (ya auditado) → Worker (pendiente) → Admin (pendiente).
- Al corregir un hallazgo: márcalo `[x]`, y si vale la pena, anota el commit/PR al final de la línea.

**Estado de auditoría por módulo:**

| Módulo | Código estático | En vivo (producción) | Fecha |
| --- | --- | --- | --- |
| Backend transversal (auth, pagos, Forge, SSE, algoritmos) | ✅ Completo | Parcial (solo lo alcanzable desde el rol Cliente) | 2026-07-20 |
| Cliente | ✅ Completo | ✅ Completo | 2026-07-20 |
| Worker / Profesional (rol real: `PRO`) | ✅ Completo (ronda de 5 agentes en paralelo sobre `apps/web/app/(app)/worker/**`, 2026-07-21) | 🟡 Parcial (todas las pantallas del sidebar recorridas; falta OPS_ADMIN para comparar contraparte admin) | 2026-07-21 |
| Admin | ✅ Completo (ronda de 5 agentes Crew G, 2026-07-22) | ✅ Completo (2026-07-27, cuenta demo; segunda pasada dirigida 2026-08-02) | 2026-07-27 / 2026-08-02 |

---

## Sección 0 — Seguridad transversal (hacer esto primero, sin importar el módulo)

### 0.0 — CRÍTICO — CAUSA RAÍZ CONFIRMADA del bug de "estado en cero/incorrecto" que se repite en Cliente, Worker y Admin

- **Qué exactamente:** el enum real `JobStatus` en `packages/db/prisma/schema.prisma:11-23` está en **MAYÚSCULAS** (`DRAFT`, `POSTED`, `PUBLISHED`, `RESERVED`, `ACCEPTED`, `IN_PROGRESS`, `REVIEW`, `DISPUTE`, `COMPLETED`, `AWARDED`, `CANCELLED`) — así lo escribe el backend (confirmado en `bids.repository.ts:362-367`, `db.job.update({ data: { status: "ACCEPTED" } } )`). Pero el frontend, en **8 archivos**, compara ese valor contra arrays/objetos escritos en **minúsculas**, sin ninguna normalización real:
  - `apps/web/app/(app)/client/dashboard/page.tsx:119` — `jobs.filter(j => ["in_progress","reserved","accepted","review"].includes(j.status))` → nunca hace match → **"Trabajos activos" siempre 0**, sin importar cuántos jobs reales estén aceptados.
  - `apps/web/app/(app)/client/jobs/page.tsx:22-24,113` — mismo filtro para la pestaña "Activos" (siempre vacía) + un mapa de colores/labels de badge (`reserved`, `accepted`, `in_progress`) que tampoco hace match → los jobs reales caen al badge por defecto.
  - `apps/web/app/(app)/client/jobs/[jobId]/page.tsx` — mismo patrón.
  - `apps/web/app/(app)/worker/jobs/[jobId]/page.tsx:150-151` — la variable se llama `normalizedStatus` pero **nunca se normaliza de verdad**: `const normalizedStatus = asString(job?.status) ?? "posted";` toma el valor crudo (`"ACCEPTED"`) tal cual, sin `.toLowerCase()`. La búsqueda `JOB_STATUS_META["ACCEPTED"]` falla (el mapa solo tiene claves minúsculas) y cae al fallback `JOB_STATUS_META.posted` → **por eso el profesional ve "Publicado" en un trabajo que está realmente `ACCEPTED`**, y además recibe el "próximo paso" equivocado (`WORKER_NEXT_ACTION[normalizedStatus]` también falla).
  - `apps/web/app/(app)/worker/dashboard/page.tsx`, `apps/web/app/(app)/worker/agenda/page.tsx:149,154,160`, `apps/web/app/(app)/admin/dashboard/page.tsx`, `apps/web/app/dashboard/dashboard-client.tsx` — mismo patrón de código copiado (`ACTIVE_STATUSES` en minúsculas comparado con `.has(j.status)`, sin normalizar). **Matiz importante, confirmado en vivo el 2026-07-20:** en `/worker/agenda`, con el mismo job real (`ACCEPTED`), la pestaña "Activos" **sí mostró correctamente** "1 trabajo" con badge "Aceptado" — a pesar de tener el código idéntico al roto. Esto indica que el bug **no es uniforme**: probablemente la ruta/BFF que alimenta `/worker/agenda` ya normaliza el status a minúsculas antes de llegar al frontend, mientras que las rutas que sí fallan (`client/dashboard`, `client/jobs`, `worker/jobs/[jobId]`) reciben el enum crudo de Prisma. **Antes de tocar código:** identificar exactamente qué endpoint/BFF alimenta cada pantalla y si ya normaliza, en vez de asumir que las 8 están rotas igual — el fix correcto podría ser normalizar en un solo punto (el BFF o el mapper compartido) en vez de tocar 8 componentes por separado.
  - **CAUSA EXACTA DEL MATIZ, RESUELTA 2026-07-21 (ronda de agentes de código):** el backend tiene **dos convenciones de casing coexistiendo para el mismo campo `Job.status`, sin que nada las reconcilie**. `apps/api/src/common/visible-response.ts` (`toVisibleJob`) **mayúsculiza intencionalmente** el status para las respuestas de `GET /v1/jobs` y `GET /v1/jobs/:id` (`posted→"POSTED"`, etc.) — esta es la ruta que alimenta `worker/jobs/[jobId]/page.tsx` (vía `fetchJob`) y `worker/dashboard/page.tsx` (vía `fetch("/api/semse/jobs")` directo), por eso ahí llega en mayúsculas y el filtro lowercase nunca matchea. En cambio, `apps/api/src/modules/bids/bids.repository.ts:120` (`listByWorker`, usado por `fetchMyBids()` → `/v1/my-bids`) hace `jobStatus: bid.job.status.toLowerCase()` manualmente y **nunca pasa por `toVisibleJob`** — por eso `/worker/agenda` y `/worker/bids` (que consumen `fetchMyBids`) reciben el valor ya en minúsculas y el mismo patrón de filtro sí funciona ahí. No es que una ruta "normalice mejor" que otra por diseño — son dos code paths independientes con convenciones opuestas para el mismo dato.
- **Por qué se ve como "varios bugs distintos":** no lo es. Es una sola línea de patrón (comparar un enum real en mayúsculas contra literales en minúsculas) copiada y pegada en 8 lugares durante el desarrollo — probablemente escrita contra una versión anterior del schema que sí usaba minúsculas, y nunca actualizada cuando el enum canónico pasó a mayúsculas. Explica de una sola vez: **1.4** (cliente, "cero en 5 pantallas"), **2.1b** (worker, badge "Publicado" incorrecto), y muy probablemente el equivalente en Admin en cuanto se pueda verificar en vivo.
- **Fix:** no inventar una capa de normalización nueva — alinear los 8 archivos para comparar/mapear contra los valores reales del enum `JobStatus` (mayúsculas), que es la fuente de verdad según el propio schema de Prisma. Buscar además si `packages/schemas` ya expone un tipo/const compartido para `JobStatus` que se pueda importar en vez de siete copias locales del mismo mapa.
- **Estado:** [x] Corregido (Crew A, 2026-07-21) — normalizado en el punto real de entrada, no en 8 archivos: `packages/schemas/src/job.schema.ts` ya tipaba `JobRecordView.status` en minúsculas (`jobRecordStatusSchema`) — ese es el contrato real que ya usan las 6 pantallas afectadas via `import type { JobRecordView } from "@semse/schemas"`. El bug era que las rutas BFF `apps/web/app/api/semse/jobs/route.ts` y `.../jobs/[jobId]/route.ts` reenviaban el JSON del backend (mayúsculas, via `toVisibleJob`) sin normalizar, violando ese contrato. Se agregó `normalizeJobRecordStatus()` (exportada desde `@semse/schemas`) y se aplicó en esas 2 rutas BFF + en `apps/web/app/dashboard/page.tsx` (la ruta huérfana de 1.6, que pega directo a `/v1/jobs` sin pasar por el BFF). Cero cambios en los componentes de página — su lógica de comparación en minúsculas ya era correcta. `/worker/agenda`/`/worker/bids` (que ya normalizaban bien via `bids.repository.ts`) no se tocaron. Typecheck + lint + tests unitarios pasan. **Pendiente: verificación en vivo** (no hay servidor/credenciales en esta sesión) — confirmar que `/client/dashboard`, `/client/jobs`, `/worker/dashboard`, `/worker/jobs/[jobId]` muestran los conteos/badges correctos contra un job real.

### 0.1 — CRÍTICO — Bypass de login vía spoofing de headers en el BFF
- **Qué:** `apps/web/middleware.ts` no descarta los headers `x-semse-user-id/tenant-id/org-id/roles` cuando no hay sesión, en rutas públicas como `/api/semse/auth/login`. El BFF usa esos headers atacante-controlados para pedir un token real al backend con el secreto `SEMSE_BOOTSTRAP_TOKEN` propio del servidor.
- **Dónde:** `apps/web/middleware.ts:43-55,84-86` · `apps/web/lib/semse-api-auth.ts:6` · `apps/web/app/api/semse/_server.ts:141-159,201-224` · `_access-token-bootstrap.ts:74-80`
- **Fix:** el middleware debe descartar/sobrescribir los headers `x-semse-*` entrantes SIEMPRE que no haya sesión válida, antes de evaluar si la ruta es pública — no solo cuando sí hay sesión.
- **Estado:** [x] Corregido (Crew D, 2026-07-21) — `withSessionHeaders()` ahora borra los 4 headers `x-semse-*` del request entrante SIEMPRE primero, y solo los vuelve a fijar si hay sesión válida — antes solo se sobrescribían dentro de la rama `if (session)`. Cubre las 3 llamadas (ruta pública, ruta API protegida, ruta app protegida) con un solo fix. Pendiente verificación en vivo (probar con un `x-semse-tenant-id` falsificado y sin cookie de sesión).

### 0.2 — CRÍTICO — `SEMSE_BOOTSTRAP_TOKEN` no se exige en código
- **Qué:** a diferencia de `AUTH_SECRET` (obligatorio en producción), esta variable es opcional en el código — si faltara, `POST /v1/auth/token` firma tokens para cualquiera. Hoy SÍ está configurada en Railway (verificado 2026-07-20), pero el código no lo garantiza.
- **Dónde:** `apps/api/src/modules/auth/auth.controller.ts:31-50,69-88`
- **Fix:** hacer que el arranque del servicio truene si la variable no está seteada en producción, igual que `AUTH_SECRET`.
- **Estado:** [x] Corregido (Crew D, 2026-07-21) — no se agregó una verificación al arranque del proceso (`AUTH_SECRET` tampoco lo hace realmente — su chequeo es lazy, por request, vía `requireSecret()`); en su lugar `requireBootstrapToken()` ahora lanza `ForbiddenException` si `SEMSE_BOOTSTRAP_TOKEN` no está seteado **y** `NODE_ENV === "production"` (mismo patrón que el `resetTokenPreview` gateado por NODE_ENV que ya existía en este mismo archivo). Fuera de producción conserva el comportamiento permisivo actual para no romper desarrollo local. Pendiente verificación en vivo.

### 0.3 — CRÍTICO — Ni logout ni cambio de rol invalidan el token
- **Qué:** el JWT es stateless, solo se verifica firma/expiración. `logout()` y el reset de contraseña solo marcan la sesión en BD como revocada, nunca invalidan el token en sí. TTL real: 8 horas.
- **Dónde:** `apps/api/src/modules/auth/auth.service.ts:127-137,233-255` · TTL: `358,383,299`
- **Fix:** verificar el estado de la sesión en BD en cada request (no solo la firma), o reducir el TTL agresivamente y aceptar el trade-off, o implementar una lista de revocación real.
- **Estado:** [ ] Pendiente — **decisión explícita del usuario (2026-07-21): no tocar.** El propio código ya documenta (`auth.service.ts:127-129`) que la verificación de sesión en BD por request se implementó y se **revirtió** por un incidente real: causaba timeouts de 15s cuando Postgres estaba lento en Railway. Reimplementarla reintroduce ese riesgo conocido. Queda registrado como decisión arquitectónica consciente (seguridad vs. disponibilidad), no como bug pendiente de "arreglar" sin más.

### 0.4 — CRÍTICO (seguridad) — IDOR cross-tenant en aprobación de evidencia de milestones
- **Qué:** `updateEvidenceItemStatus` filtra por `id`/`milestoneId`, nunca por `tenantId` — a diferencia de sus funciones vecinas en el mismo archivo. Afecta directo la auto-liberación de escrow. `itemId` es predecible (timestamp + 4 chars).
- **Dónde:** `apps/api/src/modules/milestones/milestones.repository.ts:714-738` · controller: `milestones.controller.ts:220-266`
- **Fix:** agregar el filtro `tenantId` (o `milestone: { project: { tenantId } }`, el patrón que ya usan `archiveEvidenceItem`/`replaceEvidenceItem` dos funciones al lado).
- **Estado:** [x] Corregido (Crew C, 2026-07-21) — `update()` solo acepta campos únicos en `where`, así que el fix es `updateMany()` con el mismo filtro `milestone: { project: { tenantId } }` que ya usan las funciones vecinas, más un re-fetch; controller ahora pasa `tenantId` y lanza 404 si el conteo da 0. No se agregó un test unitario nuevo que explote el escenario cross-tenant específicamente — validado por lectura de código y porque la suite completa (1935/1936) sigue en verde. Pendiente verificación en vivo.

### 0.5 — CRÍTICO (seguridad) — IDOR cross-org en change-orders
- **Qué:** `findOwned()` filtra solo por `id` + `tenantId`, nunca verifica que el actor pertenezca a la org cliente o profesional del job. Aprobar/rechazar/aplicar confían en ese lookup sin capa adicional.
- **Dónde:** `apps/api/src/modules/change-orders/change-orders.service.ts:378-386`
- **Fix:** agregar verificación de org-ownership antes de cualquier mutación.
- **Estado:** [x] Corregido (Crew C, 2026-07-21) — `findOwned()` ahora verifica `actor.orgId === job.clientOrgId || actor.orgId === project.assignedProOrgId` (mismo patrón ya usado en `evidence.policy.ts`), con bypass para `OPS_ADMIN`. **Limitación documentada:** el check solo se aplica cuando `candidate.jobId` está presente — los change orders vinculados solo por `buildOpsProjectId` (una relación legacy pre-migración, semánticamente distinta de `Project.id`) mantienen únicamente scoping por tenant, para no adivinar un join incorrecto. Pendiente verificación en vivo.

### 0.6 — CRÍTICO (seguridad) — Cross-tenant en canal SSE del copiloto (explotable en vivo)
- **Qué:** `planStream`/`delegationsStream` filtran por tenant solo en el snapshot inicial — el canal de push en vivo se suscribe directo a `plan:${planId}`/`delegations:${projectId}` sin revalidar tenant. Los 8 endpoints SSE además son `@Public()` sin verificación de sesión propia.
- **Dónde:** `apps/api/src/infrastructure/sse/sse.controller.ts:31-84`
- **Fix:** revalidar ownership del `planId`/`projectId` contra el tenant conectado antes de abrir la suscripción push; considerar quitar `@Public()` y verificar sesión también a nivel API, no solo confiar en el header que pone el BFF.
- **Estado:** [x] Corregido — parcial (Crew C, 2026-07-21):
  - **La parte "explotable en vivo" está corregida:** `planStream`/`delegationsStream` ya no mezclan el canal `push$` incondicionalmente. Ahora se resuelve primero la propiedad (tenant del plan vía `plans.findById`; para `delegationsStream` con `projectId`, existencia del `Project` bajo ese `tenantId`) y solo si eso confirma pertenencia se abre el canal de push — si no, el stream queda solo con `keepalive$()`, sin datos.
  - **No corregido a propósito:** quitar `@Public()` de los 8 endpoints SSE. Es un cambio de modelo de autenticación más grande (el `EventSource` nativo del navegador no manda headers custom, por eso probablemente se diseñó así) — el propio fix sugerido lo marca como "a mediano plazo", no como bloqueante. Queda como ítem de arquitectura separado.
  - Typecheck + lint + suite completa (1935/1936) pasan. Pendiente verificación en vivo (requiere un cliente SSE real con `x-tenant-id` falsificado).

### 0.7 — CRÍTICO (seguridad) — Un header del cliente decide en qué tenant se escribe un archivo
- **Qué:** el emisor de planes de subida lee `tenantId` de `x-tenant-id` (header del cliente) en vez de `resolveRequestContext(req)`. La ruta de descarga es pública sin firma/expiración.
- **Dónde:** `apps/api/src/infrastructure/storage/uploads.controller.ts:174-175,237-238`
- **Fix:** usar la sesión verificada, no el header.
- **Estado:** [x] Corregido (Crew C, 2026-07-21) — `presignUploadPlan` en `uploads.controller.ts` ahora usa `resolveRequestContext(req).tenantId` en vez de leer `x-tenant-id` del header. Se eliminó `headerValue()`, que quedó sin uso. **Nota de alcance:** esta ruta (`GET /v1/uploads/plan`) parece no ser la que usa el frontend actual (que llama `POST /v1/uploads/plan` en `evidence.controller.ts`, ya correcto — ver Crew B); se corrigió de todas formas por ser una vulnerabilidad real y alcanzable directamente vía API. La segunda parte del hallazgo ("ruta de descarga pública sin firma/expiración") es intencional por diseño — ver comentario en `getFile` (`@Public()`, keys tenant-scoped difíciles de enumerar) — no se tocó. Pendiente verificación en vivo.

### 0.8 — CRÍTICO (seguridad) — Descargar un archivo por el BFF no manda identidad al backend
- **Qué:** el handler `GET` de `uploads/files/[...key]` no adjunta headers autorizados a la petición saliente (el `PUT` en el mismo archivo sí). El backend recibe la descarga como anónima.
- **Dónde:** `apps/web/app/api/semse/uploads/files/[...key]/route.ts` — PUT:15-39 vs GET:60-78
- **Fix:** aplicar el mismo `buildAuthorizedHeaders` que ya usa el PUT.
- **Estado:** [x] Corregido (2026-07-22) — el `GET` ahora arma `authorizedHeaders` con `buildAuthorizedHeaders(config)` y los adjunta a la petición saliente, igual que el `PUT`. Pendiente verificación en vivo.

### 0.9 — CRÍTICO (confianza) — Verificación de identidad del worker (firma DID) es un stub
- **Qué:** el código literalmente comenta "for now, return synthetic verification" y solo valida que las strings no estén vacías. Cero criptografía real.
- **Dónde:** `apps/api/src/modules/worker-verification/worker-verification.repository.ts:115-135`
- **Fix:** implementar verificación criptográfica real (crypto.subtle o tweetnacl, como el propio comentario del código sugiere), o dejar de exponer "verificado" en el producto hasta que exista.
- **Estado:** [x] Corregido (2026-07-22) — se tomó la segunda opción del fix: no existe ningún cliente (web ni de otro tipo) en todo el producto que genere un keypair y firme un challenge — se verificó explícitamente que no hay ninguna ruta ni componente en `apps/web` que llame `POST /v1/workers/:workerId/sign` ni maneje `didSignature`/`didPublicKey`. Implementar "verificación real" contra un encoding inventado habría sido teatro de seguridad (nada produce esas firmas). `verifyDidSignature` ahora falla cerrado siempre (`return false` explícito con log de advertencia) en vez de aprobar con un chequeo de truthy — cierra el hueco donde cualquiera podía marcar a un worker "verificado" mandando dos strings no vacíos por API directa. Queda documentado en el propio método que se requiere criptografía real + un cliente de firma antes de poder reactivar este flujo.

### 0.10 — CRÍTICO (seguridad) — vision-service sin autenticación, expuesto públicamente
- **Qué:** ningún endpoint verifica API-key/firma. CORS con `allow_origins=["*"]` + `allow_credentials=True`. Cualquiera con la URL pública puede llamar endpoints costosos (análisis en lote) gratis.
- **Dónde:** `apps/vision-service/app/main.py:13-19` · `app/routes/evidence.py`
- **Fix:** agregar autenticación (API-key compartida entre `apps/api` y `apps/vision-service` como mínimo), corregir CORS.
- **Estado:** [x] Corregido (2026-07-22) — `evidence_router` ahora exige el header `X-Vision-Api-Key` (dependency `require_api_key` en `main.py`) comparado contra `VISION_SERVICE_API_KEY`; `/health` queda público a propósito para monitoring/Railway. Falla cerrado solo cuando `RAILWAY_ENVIRONMENT`/`RAILWAY_ENVIRONMENT_NAME` está presente y la key no está seteada (mismo patrón que `SEMSE_BOOTSTRAP_TOKEN`), permisivo en dev local. `VisionServiceClient` (`apps/api`) ahora manda ese header en todas sus llamadas si `VISION_SERVICE_API_KEY` está seteada en el lado del API. CORS: se quitó `allow_credentials=True` combinado con `allow_origins=["*"]` (ese combo hacía que FastAPI reflejara el Origin real del llamador); ahora `allow_credentials=False` y los orígenes vienen de `VISION_CORS_ALLOWED_ORIGINS` (vacío por defecto — este servicio solo debería recibir tráfico server-to-server). **Requiere acción del usuario fuera de este código:** configurar `VISION_SERVICE_API_KEY` como variable de servicio en Railway tanto para `apps/api` como para `apps/vision-service` (mismo valor en ambos). Pendiente verificación en vivo.

### 0.11 — CRÍTICO (seguridad) — Bypass del SSRF-guard de vision-service por nombre de archivo
- **Qué:** el gate real es `if "localhost" in url or "127.0.0.1" in url` — substring sobre la URL completa, no el hostname. Una URL de S3 legítima que contenga esas palabras en el nombre del archivo activa el bypass y la "verificación" devuelve una imagen mock fija, sin analizar la foto real.
- **Dónde:** `apps/vision-service/app/services/image_loader.py:51-79` · duplicado en `app/routes/evidence.py:86`
- **Fix:** parsear la URL y comparar el hostname exacto, no un substring de la URL completa.
- **Estado:** [x] Corregido (2026-07-22) — nueva función `is_mock_or_local_url()` en `image_loader.py` que parsea la URL con `urlparse` y compara el hostname exacto contra `localhost`/`127.0.0.1` (o el esquema `mock://`), reemplazando el `"localhost" in url` original. Reutilizada en `evidence.py:86` (el duplicado exacto del mismo bug) importándola en vez de repetir la lógica. El `_assert_safe_url` real (hostname + resolución de IP contra rangos bloqueados) no se tocó — ya estaba bien implementado; el bug era solo en el atajo de mock/local que lo precede. Pendiente verificación en vivo.

### 0.12 — CRÍTICO (dinero) — Estado de transacción marcado SUCCEEDED sin verificar al proveedor real
- **Qué:** `depositFunds`/`releaseFunds`/`refundFunds` escriben `status: "SUCCEEDED"` siempre, sin leer el estado real (pending/processing/authorized) que devuelven Stripe/PayPal/transferencia bancaria.
- **Dónde:** `apps/api/src/modules/payments/payments.repository.ts` — depositFunds:307-314 · releaseFunds:377-385 · refundFunds:444-451
- **Fix:** leer y persistir el estado real del provider intent; solo marcar SUCCEEDED cuando el provider confirme.
- **Estado:** [x] Corregido (Crew D, 2026-07-21) — ver 0.14/0.15 para el detalle: las 3 funciones ahora reservan en `PENDING` y un paso de `finalize*` separado escribe el status real devuelto por el provider (`SUCCEEDED`/`FAILED`), o lo deja en `PENDING` si el provider todavía no confirma de forma síncrona (el caso normal para depósitos vía PaymentIntent). Pendiente verificación en vivo.

### 0.13 — CRÍTICO (dinero) — El webhook de pagos es un no-op total
- **Qué:** verifica la firma correctamente y descarta el payload — no busca la transacción, no actualiza estado, no hay idempotencia.
- **Dónde:** `apps/api/src/modules/payments/payments.service.ts:770-777`
- **Fix:** implementar reconciliación real por `providerRef`.
- **Estado:** [x] Corregido (Crew D, 2026-07-21) — `PaymentsService.webhook()` ahora resuelve `providerRef`+tipo de evento desde el shape real de Stripe (`data.object.id`/`type`, además de mantener compatibilidad con el shape simple `{event, providerRef}` que ya usaban los tests), mapea el `event.type` a un status objetivo, y llama a `PaymentsRepository.reconcileTransactionStatus()` — que es idempotente por construcción (`WHERE status = 'PENDING'`, así que una entrega duplicada o fuera de orden es un no-op seguro). También amplié `paymentsWebhookSchema` para aceptar el envelope real de Stripe. **Nota importante:** el mapeo de `event.type` → status (`mapProviderEventToStatus` en `payments.service.ts`) se basa en los tipos de evento que esta integración de Stripe genera según el código (`PaymentIntent`, `Refund`, `Transfer` reversals) — **no se verificó contra una suscripción real de webhooks de Stripe en esta sesión**; confirmar que los tipos de evento configurados en el dashboard de Stripe coinciden antes de confiar en esto en producción.

### 0.14 — CRÍTICO (dinero) — El dinero se mueve en el proveedor antes de la verificación atómica de saldo
- **Qué:** `release()`/`refund()` llaman al proveedor real (el payout/transfer se dispara) y solo después intentan escribir la transacción dentro del chequeo atómico que puede rechazar por saldo insuficiente. Dos releases concurrentes pueden ambas disparar pago real y una perder la carrera sin dejar registro.
- **Dónde:** `apps/api/src/modules/payments/payments.service.ts` — release:563-611 · refund:682-712
- **Fix:** invertir el orden — verificar/reservar el saldo atómicamente primero, llamar al proveedor después.
- **Estado:** [x] Corregido (Crew D, 2026-07-21) — **decisión explícita del usuario de implementar sin sandbox real de Stripe para probar; ver nota abajo.** Reordenado a reservar→llamar al proveedor→finalizar en `deposit()`, `release()` y `refund()`: 1) se crea el `PaymentTxn` con `status: PENDING` dentro de una transacción `Serializable` que ahora cuenta `PENDING`+`SUCCEEDED` al calcular el saldo disponible (antes solo contaba `SUCCEEDED`, por eso dos releases concurrentes podían pasar ambos el chequeo); 2) se llama al provider recién ahí; 3) `finalizeRelease`/`finalizeRefund`/`finalizeDeposit` escriben el status real — `SUCCEEDED` solo si el provider confirma síncronamente (ej. Stripe Transfer con `status: "paid"`), si no queda `PENDING` hasta que el webhook (0.13) lo confirme. Si el provider lanza excepción, la reserva se marca `FAILED` explícitamente (nunca queda huérfana en `PENDING`). El campo `providerRef` se reserva con un placeholder único y se actualiza al valor real del provider al finalizar. Marcar el milestone como `PAID` se movió de `releaseFunds` a `finalizeRelease` — un provider fallido ya no puede dejar un milestone marcado pagado sin transferencia real. Typecheck + lint + 119 tests dirigidos + suite completa (1935/1936) pasan. **No probado contra Stripe real** — no hay sandbox disponible en esta sesión; toda la validación fue típica/lint/tests unitarios contra el provider mock.

### 0.15 — CRÍTICO (dinero) — Mismo patrón en escrow-release.service.ts (auto-release)
- **Qué:** la transferencia real a Stripe se dispara antes de la transacción de BD. Si la BD falla después de que Stripe ya aceptó, un reintento puede pagar dos veces. Además es fire-and-forget desde `milestones.service.ts` (`.catch(() => undefined)`).
- **Dónde:** `apps/api/src/modules/payments/escrow-release.service.ts:69-122` · disparo: `milestones.service.ts:292`
- **Fix:** mismo fix que 0.14, aplicado a este servicio; no descartar el error en el caller.
- **Estado:** [x] Corregido (Crew D, 2026-07-21) — `EscrowReleaseService.tryAutoRelease` ahora inyecta `PaymentsRepository` y reutiliza exactamente `releaseFunds`/`finalizeRelease` (mismo mecanismo que 0.14, no una segunda implementación paralela). Si la transferencia falla, la reserva se marca `FAILED`. Si la transferencia **tiene éxito** pero el `finalize` (escribir en BD) falla, ya **no se reporta como `released: false`** (que ocultaba que el dinero sí se movió) — se devuelve `released: true` con un `blockers` explícito de "reconciliación manual requerida" y un `logger.error` con el `transferId` real, para que quede rastreable. El caller (`milestones.service.ts:292`) dejó de ser un `.catch(() => undefined)` ciego: ahora loguea `warn` para bloqueos normales de gobernanza y `error` para el caso de reconciliación manual o cualquier excepción. Pendiente verificación en vivo.

### 0.16 — CRÍTICO (dinero) — Stripe Connect: payout a cuenta compartida si el worker no está "activo"
- **Qué:** `createPayoutIntent` arranca con `STRIPE_CONNECT_ACCOUNT_ID` (cuenta legacy fija) y solo la reemplaza si la cuenta del worker está `active`. Sin error/bloqueo si no lo está.
- **Dónde:** `apps/api/.../payments/providers/stripe.provider.ts:73-83`
- **Fix:** bloquear el payout (no caer a la cuenta compartida) si la cuenta Connect del worker no está activa.
- **Estado:** [x] Corregido (Crew D, 2026-07-21) — cuando `input.recipientUserId` está presente (hay un destinatario específico conocido), ya no cae nunca a `STRIPE_CONNECT_ACCOUNT_ID` — si no hay cuenta Connect activa para ese usuario, lanza una excepción explícita en vez de pagar a la cuenta compartida. El fallback a la cuenta legacy compartida ahora solo aplica cuando genuinamente no hay `recipientUserId` (sin destinatario específico conocido). Pendiente verificación en vivo.

### 0.17 — ALTO (dinero) — Webhook de Stripe Connect también es no-op
- **Qué:** no hay ruta dedicada; el estado de la cuenta Connect solo se actualiza con un sync manual.
- **Dónde:** `payments.service.ts:770-777` · `stripe-connect.service.ts:119-142`
- **Estado:** [x] Corregido (Crew D, 2026-07-21) — se agregó `StripeConnectService.syncAccountStatusByStripeAccountId()` (resuelve `userId` a partir del `stripeAccountId` del evento, luego reutiliza el `syncAccountStatus` existente que ya vuelve a consultar a Stripe directamente) y se cableó al mismo endpoint de webhook (`payments.service.ts webhook()`) para el evento `account.updated`. Misma nota de 0.13: no se verificó contra una suscripción real de Stripe en esta sesión.

### 0.18 — CRÍTICO (integridad, Forge) — `completeTask` acepta un resultado fabricado
- **Qué:** el payload de `.../tasks/:taskId/complete` no está validado, incluye `policy.decision`, y el harness le cree para avanzar el run — nunca vuelve a llamar la verificación real. `manifest.fileScopes` es código muerto (nunca se pasa `changedFiles`).
- **Dónde:** `apps/api/.../forge.controller.ts:151-171` · `forge.service.ts:293-309` · `packages/forge/src/policy.ts:86-122`
- **Fix:** derivar la decisión de política server-side a partir de datos reales, no del payload del caller; pasar `changedFiles` real a `evaluateForgePolicy`.
- **Estado:** [x] Corregido (2026-07-22) — `applyTaskResult` ya no lee `policy` del payload del caller; ahora usa directamente el `ForgePolicyResult` real que devuelve `harness.authorizeTaskAction()` (que internamente llama `evaluateForgePolicy()`) — antes ese valor de retorno se calculaba y se descartaba. `changedFiles` ahora se pasa desde `prPackage.changedFiles` del resultado, activando el scoping de `manifest.fileScopes`/`task.allowedFiles` que antes era código muerto. **Alcance:** los campos `.decision` de `prPackage`/`deployment`/`rollback`/`securityReport` (hallazgos de sub-dominios distintos: PR, deployment, rollback, security review) siguen viniendo del payload del caller sin verificación independiente — eso queda fuera del alcance de este hallazgo específico, no se tocó. Pendiente verificación en vivo.

### 0.19 — CRÍTICO (cumplimiento laboral) — No existe multiplicador de horas extra
- **Qué:** `(minutos/60) * tarifa` flat, sin ningún umbral semanal. Solo existe una alerta de QualityGuard para que un admin lo vea — nunca toca el pago real.
- **Dónde:** `apps/api/.../labor-engine.repository.ts:253`
- **Fix:** implementar el multiplicador real (1.5x u otra regla, según la política laboral de SEMSE) en el cálculo de pago, no solo en la alerta.
- **Estado:** [x] Corregido (2026-07-22) — **política confirmada explícitamente por el usuario: 1.5x sobre 40h/semana (estándar FLSA US).** `getTeamSummary()` en `labor-engine.repository.ts` ahora procesa las entradas de cada worker en orden cronológico, acumula minutos por semana calendario (lunes-domingo, UTC) y aplica 1.5x a los minutos que exceden el umbral de 40h — una entrada que cruza el umbral se divide proporcionalmente entre horas regulares y extra dentro de sí misma. Se usó un umbral nuevo y separado (`OVERTIME_WEEKLY_THRESHOLD_MINUTES = 40*60`) del ya existente `QUALITY_GUARD.overtimeWeekMinutes` (48h) en `labor-engine.service.ts` — ese sigue siendo solo una alerta de sobre-trabajo, no la regla de pago. `knownCost` (que ya alimenta directamente `estimatedCost` en `admin/labor-engine/page.tsx`) ahora refleja el pago real con el recargo — no se necesitó ningún cambio de UI. Pendiente verificación en vivo.

### 0.20 — ALTO — Labor Engine sin idempotencia real (duplica horas pagables)
- **Qué:** el `event.id` del cliente nunca se manda al backend; `createTimeEntry` siempre inserta con UUID nuevo. Al fallar la sync a medio lote, el arreglo de reintento conserva TODOS los eventos, incluidos los ya exitosos.
- **Dónde:** `apps/web/.../worker/tracker/page.tsx` — syncPendingEvents:367-434 · `apps/api/.../labor-engine.repository.ts:79-104`
- **Fix:** mandar `event.id` como clave de idempotencia al backend; podar del arreglo de reintento los eventos ya confirmados.
- **Estado:** [x] Corregido (2026-07-22) — `TimeEntry` gana una columna `clientEventId` con `@@unique([tenantId, createdBy, clientEventId])` (migración escrita a mano `packages/db/prisma/migrations/20260722010000_time_entry_client_idempotency`, formato Prisma; no se corrió contra una base de datos real en esta sesión, no hay Postgres disponible). `LaborEngineRepository.createTimeEntry` (y `startRealtimeEntry`) hacen ahora insert-o-recupera: intentan el `create` con el `clientEventId` real y, si choca contra el índice único (`P2002`), hacen `findFirst` por la misma clave y devuelven la fila existente en vez de fallar — mismo patrón `isUniqueConstraintError` ya usado en `evidence.repository.ts`/`reservations.repository.ts`. `LaborEngineService.startTimer` además resuelve por `clientEventId` **antes** de chequear "ya hay un timer activo", para que un replay de un `start` ya confirmado no choque con esa regla. El BFF (`api/semse/labor/timer/start`, `.../entries/manual`) ya reenviaba el body completo tal cual — no necesitó cambios. Del lado cliente, `startLaborTimer`/`createManualEntry` (`labor-api.ts`) ahora aceptan `clientEventId`, y sus 3 sitios de llamada reales lo mandan: `handleStart` y `handleManualSave` (intento directo online, usando el mismo `event.id` que quedaría en la cola si falla) y el reintento de `syncPendingEvents` (`worker/tracker/page.tsx`) — así que incluso "la petición sí llegó pero la respuesta se perdió" (no solo el batch parcial) queda cubierto. Bug del arreglo de reintento corregido aparte: `syncPendingEvents` ahora poda cada evento de `pendingEvents` inmediatamente al confirmarse (no solo si el batch entero termina bien), y en cuanto el evento `start` se confirma persiste el `backendSessionId` real sobre la sesión local — necesario para que un reintento posterior en un batch nuevo (sin el `start` original, ya podado) pueda seguir resolviendo `pause`/`resume`/`stop`/`update_note` contra el id real del backend en vez del id local. **Cambio adicional no pedido explícitamente pero necesario para que 0.24 sea alcanzable desde la UI real:** los dos gates del lado cliente que ya bloqueaban el guardado de una entrada manual con `startTime > endTime` (`manualDurationSeconds` en este mismo `page.tsx`, y el `durationPreview` de `RegistrosTab.tsx`, el segundo formulario de 2.5) se corrigieron igual que 0.24 — sin esto, el fix del backend habría sido inalcanzable porque ambos formularios deshabilitan "Guardar" en cuanto la duración calculada da `null`. Typecheck de `@semse/api` limpio salvo errores preexistentes no relacionados a este módulo (paquetes de workspace como `@semse/schemas`/`@semse/shared` sin build previo en este checkout, no tocados por este fix). Jest no se pudo ejecutar en este entorno (dependencia no instalada pese al lockfile) — no hay test automatizado nuevo para el flujo de idempotencia. Pendiente verificación en vivo — incluye aplicar la migración real y, como ya pedía la lista de pendientes de la sección Worker, forzar un corte de red a medio sync en el Time Tracker real.

**Evidencia adicional (2026-07-27):** se agrego una regresion automatizada en `apps/api/test/labor-engine.service.test.ts` que confirma que un replay del mismo `clientEventId` devuelve la entrada existente antes de aplicar el bloqueo de "ya hay un timer activo". La prueba dirigida de Labor Engine queda en **16/16** y la suite del store local del tracker en **5/5**. La frase historica de este parrafo que indicaba que no habia test queda superada por esta prueba nueva. La prueba E2E en Railway paso: se corto la segunda mutacion despues de permitir `start`, el retry envio solo `pause` y quedo exactamente una entrada con la nota de prueba; la limpieza detuvo la sesion.

### 0.21 — ALTO — Herramientas internas abiertas a cualquier rol
- **Qué:** `/anatomy`, `/knowledge`, `/repo-map`, `/runtime-map` están gateadas por `knowledge:read`, permiso que `rbac.ts` otorga a TODOS los roles.
- **Dónde:** `apps/api/.../{anatomy,knowledge,repo-knowledge,runtime-knowledge}/*.controller.ts` · `packages/auth/src/rbac.ts:47,94,110,191`
- **Fix:** crear un permiso `internal:architecture:read` exclusivo de roles internos/admin, o gatear por rol directamente.
- **Estado:** [x] Corregido (2026-07-22) — se agregó el permiso `internal:architecture:read` en `packages/auth/src/rbac.ts`, otorgado únicamente a `OPS_ADMIN` (el único rol interno/admin de la matriz; `CLIENT`/`PRO`/`WORKER` son roles externos y no lo reciben). `knowledge:read` **no se tocó/removió** — sigue existiendo y sigue siendo amplio, porque `KnowledgeController` mezcla dos cosas bajo el mismo controller: los endpoints realmente arquitectónicos (`domains`, `overview` — mapa de dominios de conocimiento del repo y estado de servicios internos) y endpoints funcionales legítimamente multi-rol (`workspace-memory/*`, `skills/*`, `curate/*`, usados en runtime por agentes de cualquier rol). Se cambió el decorador de clase a `internal:architecture:read` en `anatomy.controller.ts`, `repo-knowledge.controller.ts` y `runtime-knowledge.controller.ts` (los tres son 100% arquitectura, ningún endpoint mixto). En `knowledge.controller.ts` se dejó el decorador de clase en `knowledge:read` y se agregó `@RequirePermissions("internal:architecture:read")` a nivel de método solo en `domains`/`overview` (el guard usa `getAllAndOverride`, así que el decorador de método gana sobre el de clase) — el resto de endpoints del controller no cambió de permiso. **Supuesto explícito:** `OPS_ADMIN` es el único candidato razonable a "rol interno" en `rolePermissions` (mismo patrón que `ops:dashboard:read`, exclusivo de ese rol); no se creó un rol nuevo. Verificado por lectura de código + `tsc --noEmit` limpio. Pendiente verificación en vivo (no hay servidor en esta sesión) y confirmar que ningún flujo legítimo de CLIENT/PRO/WORKER dependía de `/anatomy`, `/repo-knowledge`, `/runtime-knowledge` o `/knowledge/domains`+`/knowledge/overview` (no se encontró ninguno en `apps/web` fuera de las páginas standalone `/anatomy`, `/knowledge`, `/repo-map`, `/runtime-map`, que no tienen gating de rol en el frontend — es exactamente el patrón que describe el hallazgo).

### 0.22 — ALTO — Barridos programados pueden revertir un estado ya cambiado legítimamente
- **Qué:** `sweepExpired()` (reservas) y `reclaimStale()` (agent runs) hacen `findMany` + `update` sin repetir el filtro de estado en el WHERE, sin transacción — a diferencia de sus métodos hermanos.
- **Dónde:** `apps/api/.../reservations.repository.ts:380-403` · `agents.repository.ts:157-204`
- **Fix:** envolver en transacción y repetir el filtro de estado original en el WHERE del update (mismo patrón que `accept`/`release`/`expire`).
- **Estado:** [x] Corregido (2026-07-22) — ambos métodos ahora envuelven todo el ciclo find→update en `this.prisma.$transaction(...)`, y el `update` deja de confiar solo en el `id`: pasa a ser `updateMany({ where: { id, status: <estado esperado> }, ... })`, chequeando `result.count` antes de dar el registro por actualizado. `reservations.repository.ts` (`sweepExpired`): el `updateMany` de las reservas ahora exige `status: "ACTIVE"` (no solo `id: { in: staleIds }`), así una reserva que un actor real ya aceptó/liberó entre el `findMany` y el `updateMany` ya no puede ser pisada de vuelta a `EXPIRED`; `expiredCount` se reporta como `updateMany.count` real (antes reportaba `stale.length`, que podía sobreestimar). El paso de reabrir el job (`status: "RESERVED" → "POSTED"`) se cambió de `findFirst`+`update` incondicional a `updateMany({ where: { id: jobId, status: "RESERVED", deletedAt: null } })`, mismo principio. `agents.repository.ts` (`reclaimStale`): el `updateMany` de cada run exige `status: "RUNNING"`, así un run que un worker real ya completó/falló/canceló entre el `findMany` y el update ya no puede ser reclamado por el barrido; si `count === 0` el run se descarta del resultado sin reconstruir un registro falso. **Nota de exactitud:** los métodos "hermanos" (`accept`/`release`/`expire` en reservations, `claimNext` en agents) tampoco repiten literalmente el filtro de estado en el `where` del `update` (usan `update({ where: { id } })` dentro de una transacción, apoyándose en el chequeo previo + la ventana corta de la transacción de una sola fila) — se optó por el patrón más estricto (`updateMany` con el filtro de estado repetido) en los dos barridos porque operan en lote sobre múltiples filas con varios round-trips intermedios (el for-loop de reapertura de jobs), una ventana de carrera bastante más ancha que una operación de una sola reserva. Se corrigió además un error de tipos nuevo introducido por el propio fix (`Partial<StoredAgentRun>` no encajaba con el input de `updateMany` de Prisma) tipando `patch` como `PrismaTypes.AgentRunUpdateManyMutationInput`. Verificado con `pnpm --filter @semse/api exec tsc --noEmit -p .` (limpio) y `eslint` sobre los archivos tocados (limpio). **No se pudieron correr tests** — Jest no está instalado en `node_modules` en este checkout (lockfile desincronizado en este worktree); no hay un spec existente para `agents.repository.ts` ni `reservations.repository.ts` que cubra `sweepExpired`/`reclaimStale` específicamente. Pendiente verificación en vivo/con tests reales.

### 0.23 — ALTO — Aprobar change-order nunca valida contra el escrow restante
- **Dónde:** `change-orders.service.ts:158-169,229-233,251-312`
- **Fix:** consultar el módulo de pagos/escrow antes de aprobar/aplicar un aumento de presupuesto.
- **Estado:** [x] Corregido (2026-07-22) — `ChangeOrdersService` ahora inyecta `PaymentsRepository` (se agregó `PaymentsModule` a los `imports` de `ChangeOrdersModule`; sin dependencia circular, `PaymentsModule` no importa `ChangeOrdersModule`) y `assertEscrowCoversIncrease()` corre antes de `approve()` y antes de `applyToBuildOps()` (doble check — los fondos pueden liberarse/reembolsarse en el intervalo entre aprobar y aplicar). El chequeo reutiliza exactamente `getDepositedAmount`/`getReleasedAmount`/`getRefundedAmount` de `PaymentsRepository` (el mismo cálculo `depositado - liberado - reembolsado` que `PaymentsService.release()` ya corre como su pre-chequeo rápido, y el mismo que 0.35 fijó para no usar `escrow.totalAmount` inflado) — no se reimplementó matemática de saldo nueva. Si `costDeltaAvg` (promedio de `estimatedMin`/`estimatedMax`, mismo cálculo que ya usaba `computeImpact()`) excede el disponible, lanza `ConflictException` — mismo tipo y mismo estilo de mensaje ("insufficient escrow funds...") que `releaseFunds()`/`refundFunds()`, para que el caller lo reciba como 409 igual que un release/refund insuficiente. **Alcance deliberadamente limitado:** el chequeo solo corre cuando (a) el delta de costo es positivo — un change order que solo reduce o no cambia el presupuesto se deja pasar sin tocar, no hay fondos nuevos que cubrir; y (b) el candidate tiene `jobId` — los vinculados solo por `buildOpsProjectId`/`milestoneId` (relación legacy pre-migración, misma limitación ya documentada en el fix de 0.5) no se pueden resolver de forma confiable a un `Project`/`PaymentEscrow`, así que se dejan pasar sin chequeo en vez de adivinar un join. Si el `jobId` no resuelve a un `Project` (todavía no promovido / sin escrow creado), también se bloquea con `ConflictException` en vez de dejar pasar un aumento sin ningún fondo detrás. `pnpm --filter @semse/api exec tsc --noEmit -p .` corre limpio (0 errores) — el worktree no tenía `node_modules` instalado en absoluto al empezar; tras `pnpm install` hubo que además correr `prisma generate` y buildear varios paquetes del workspace cuyo `dist/` faltaba (`@semse/schemas`, `@semse/tools`, `@semse/knowledge`, `@semse/product-events`, `@semse/shared`, `@semse/autonomy`, `@semse/agents`, `@semse/auth`, `@semse/forge`) para que el typecheck completo de `apps/api` compilara sin los ~1500 errores previos no relacionados (todos preexistentes, de infraestructura del entorno, no de este fix). **No se pudo correr la suite de Jest**: no hay binario `jest` en `node_modules/.bin` pese a que `apps/api/package.json` declara `"test": "jest"`, y no existe ningún `jest.config.*` en el repo — gap de infraestructura de test preexistente, no introducido por este cambio. Validado por lectura de código contra el patrón ya usado por `releaseFunds`/`refundFunds`/`release()`. Pendiente: test de regresión dirigido (aprobar CO con escrow insuficiente → 409) y verificación en vivo. **Limitación adicional encontrada en revisión (2026-07-22):** `getDepositedAmount`/`getReleasedAmount`/`getRefundedAmount` cuentan solo transacciones `SUCCEEDED`, a diferencia del chequeo atómico dentro de `releaseFunds()`/`refundFunds()` (fix 0.14), que cuenta `PENDING`+`SUCCEEDED` para cerrar la ventana de carrera. Esto significa que este chequeo de change-order puede ser optimista durante una release/refund `PENDING` en curso (ve el dinero como "disponible" mientras en realidad ya está reservado). No se corrigió porque no existe un método público equivalente que cuente `PENDING`; esa lógica vive inline dentro de `releaseFunds`/`refundFunds`. No es una brecha de seguridad crítica: la barrera financiera real sigue siendo el chequeo atómico dentro de `releaseFunds()`/`refundFunds()` mismos, que rechazará correctamente cualquier liberación/reembolso que exceda el saldo real aunque este pre-chequeo de aprobación haya sido optimista — este hallazgo es sobre UX de aprobación temprana, no la última línea de defensa del dinero.

### 0.24 — ALTO — Una entrada manual no puede representar un turno que cruza medianoche
- **Dónde:** `apps/api/.../labor-engine.service.ts:124-131`
- **Fix:** permitir `endedAt` en el día siguiente cuando `startTime > endTime`.
- **Estado:** [x] Corregido (2026-07-22) — ver detalle completo en 0.20 (mismo commit/módulo). **Corrección posterior (CI, 2026-07-23):** el fix inicial rolaba `endedAt` al día siguiente para *cualquier* `startTime > endTime` sin límite, lo que rompió el test preexistente `labor-engine.service.test.ts:109` (`13:00`→`09:00` = turno de 20h, que ese test espera que siga siendo rechazado como entrada invertida/typo, no como turno real). Se agregó un tope `MAX_ROLLOVER_HOURS = 16`: solo se interpreta como turno nocturno legítimo si el resultado, tras cruzar medianoche, dura 16h o menos (cubre casos reales como 22:00-06:00 = 8h); por encima de eso sigue lanzando `BadRequestException`. Se agregó además el test positivo que faltaba (`22:00`→`06:00` rola correctamente al día siguiente) — el commit original no incluía ningún test para el caso que arreglaba. Verificación en vivo confirmada (2026-08-01) contra el stack local vía API real (`POST /v1/labor/entries/manual`, token de `worker@demo.semse`): `22:00→06:00` fue aceptado (`201`, `durationMinutes: 480`, `endedAt` correctamente en el día siguiente); `13:00→09:00` (20h) fue rechazado (`400`, "endTime must be after startTime") — el tope de 16h se comporta exactamente como documentado.

### 0.25 — ALTO — El esquema de evidencia diseñó una banda de "revisión manual" que la decisión real ignora
- **Qué:** `ValidationScore.status` distingue 3 bandas, pero `validateEvidenceAsync` calcula su propio estado, ignora `score.status`, y usa un corte binario duro en 0.65.
- **Dónde:** `apps/api/.../evidence-gateway.service.ts:136,295`
- **Fix:** usar `score.status` (que ya tiene la banda de revisión manual) en `validateEvidenceAsync` en vez de recalcular con un binario propio.
- **Estado:** [x] Corregido (2026-07-22) — confirmado por lectura de código: `scoreAgainstRequirements` (línea 295) ya calculaba correctamente las 3 bandas (`passed`/`manual_review`/`failed`) y las devolvía en `score.status`, pero `validateEvidenceAsync` (línea 136) ignoraba ese campo y recalculaba `overall >= 0.65 ? "passed" : "failed"` — colapsando silenciosamente la banda de revisión manual en `"failed"`. Cambiado a `const status = score.status;`, una línea. El repositorio (`evidence-gateway.repository.ts`) ya soporta `"manual_review"` como valor válido de `validationStatus` en su tipo y en Prisma, y `payment-governance.service.ts` ya trata `"manual_review"` de forma razonable (no cuenta como `"passed"` para el score de readiness, pero tampoco dispara el blocker `rejected_evidence`, que solo mira `"failed"`) — **Corrección (2026-07-22, revisión posterior):** sí había un consumidor downstream afectado — `getMilestoneEvidenceValidationStatus` (`evidence-gateway.repository.ts`) calculaba `isComplete`/`isReady` como `failed === 0 && pending === 0` / `passed >= 80% && failed === 0`, sin contar `manual_review` en absoluto; como esa banda antes nunca se producía (el binario de arriba), el hueco era latente — con este fix activo, un milestone con una foto en `manual_review` sin resolver pasaba a leerse como completo/listo para pago. Ambos gates ahora también exigen `manualReview === 0`; se agregó `manualReviewCount` al shape que devuelve `getMilestoneValidationStatus`. Verificado con `tsc --noEmit` limpio (copiando los 2 archivos a un checkout hermano con `node_modules` instalado — este worktree no lo tiene). **Pendiente: test real y verificación en vivo.**

### 0.26 — ALTO — El score de calidad de evidencia no verifica el sujeto de la foto
- **Qué:** solo combina nitidez/luz/contraste. El comparador de referencia y el detector de oficio existen pero no alimentan el score numérico.
- **Dónde:** `apps/vision-service/app/services/scoring.py:37`
- **Fix:** considerar (a mediano plazo) blender la señal de `reference_match`/`trade_detector` al score numérico, no solo a metadata.
- **Estado:** [x] Corregido — parcial, mid-term conservador (2026-07-22) — `evaluate_quality()` (`scoring.py`) ahora acepta un parámetro opcional `trade_match` (el dict que ya devuelve `trade_detector.detect_trade()`) y, cuando `trade_match["match"] is False`, resta una penalización de `min(0.15, confidence * 0.15)` al `quality_score` — **peso elegido explícitamente así**: tope de 0.15 (15% de la escala 0.0-1.0) porque la señal es heurística (densidad de bordes/color/textura, no un clasificador entrenado) y nunca debe poder tumbar por sí sola una foto nítida y bien iluminada de "passed" a "failed"; sí alcanza para empujarla a la banda `manual_review` en casos límite (ver 0.25). Escalado por la confianza del propio detector para que un mismatch de baja confianza casi no mueva el score. `app/routes/evidence.py` (`/analyze`) se reordenó para ejecutar `detect_trade` antes de `evaluate_quality` (antes corría después, su resultado solo llegaba a `rawResult.tradeMatch`/`reasons`) y pasarle el resultado. **Alcance explícitamente NO cubierto, a propósito:** la señal de `reference_match` no se blendeó aquí — `match_reference()` no se invoca dentro de `/analyze` en absoluto (es un endpoint separado, `POST /match-reference`, y `EvidenceAnalyzeRequest` no tiene campo `referenceImageUrl`); cablearla habría requerido agregar un campo nuevo al request y una carga de imagen adicional dentro de `/analyze`, un cambio de alcance mayor al pedido como "conservador". Dicho esto, la similitud de referencia **ya llega a un score numérico, en otra capa**: `evidence-gateway.service.ts` (`apps/api`, ver 0.25 arriba) ya blendea `referenceSimilarity` con peso 0.3 en su propio `overall` cuando el milestone tiene `checklistSchema.referenceImageUrl` — preexistente a esta sesión, no se tocó. No hay tests de Python en `vision-service` (`find` no encontró ningún `test_*.py`) y no hay intérprete Python disponible en este entorno — el diff se revisó por lectura cuidadosa línea por línea (compatibilidad de firma por defecto `None`, orden de ejecución, tipos) en vez de ejecutarse. **Pendiente: agregar tests unitarios de `evaluate_quality`/`/analyze` y verificación en vivo.**

### 0.27 — ALTO (algoritmo) — Matching penaliza sistemáticamente a profesionales experimentados
- **Dónde:** `apps/api/.../matching/matching.algorithm.ts:67-75` (Jaccard) · pesos: `20-25`
- **Fix:** cambiar a una métrica de cobertura (`intersección / palabras del job`) en vez de Jaccard simétrico.
- **Estado:** [x] Corregido (2026-07-22) — reemplazado `jaccard(a, b)` (intersección/unión, simétrico) por `coverage(job, candidate)` = `|job ∩ candidate| / |job|` en `matching.algorithm.ts`. Solo cuenta qué fracción de las palabras del job están cubiertas por el historial del candidato — un candidato con historial largo y detallado ya no infla el denominador y pierde puntos por palabras que el job nunca pidió. El peso (`WEIGHTS.text = 0.40`) y el punto de integración en `scoreCandidate`/`rankCandidates` no cambiaron. Actualicé también el mirror test standalone en `tests/unit/matching-algorithm.test.ts` (que reimplementa la función para no depender de compilación) con la misma métrica y un test de regresión explícito que compara un candidato con vocabulario grande vs. uno con vocabulario exacto — ambos ahora dan cobertura 1.0. Verificado por lectura de código + 28/28 tests unitarios pasando (`node --test tests/unit/matching-algorithm.test.ts tests/unit/materials-calculator.test.mjs`) y `tsc --noEmit` limpio sobre los archivos tocados. Pendiente verificación en vivo con casos reales de profesionales con historial extenso.

### 0.28 — ALTO (algoritmo) — Cold-start: profesional nuevo entierra en 0 real, no neutral
- **Dónde:** `packages/db/prisma/schema.prisma:230` (`trustScore` default 0) · `matching.repository.ts` (loadCandidates, donde se lee `trustScore` para armar `CandidateInput`)
- **Fix:** definir un prior neutral en vez de 0, o una rampa de arranque para profesionales nuevos.
- **Estado:** [x] Corregido (2026-07-22) — **decisión: rampa a nivel de lectura en `matching.repository.ts`, no cambio del default en el schema.** Confirmé por lectura de código que nada en el repo escribe nunca `User.trustScore` (solo se lee — en matching, ai-models/context, intake-operations-bridge); es un campo que se queda en su default para siempre, tanto para un profesional nuevo como para uno con 100 trabajos completados, así que cambiar el `@default(0)` del schema no diferenciaría "nuevo" de "experimentado" — ambos seguirían en 0 — y solo afectaría filas nuevas (no hay backfill sin una razón real para tocar datos existentes). En cambio, en `loadCandidates()` ahora se aplica un prior neutral (`COLD_START_TRUST_PRIOR = 0.5`, punto medio de la escala 0-1) únicamente cuando `completedJobs === 0 && rawTrustScore === 0` — es decir, solo al profesional genuinamente cold-start en este tenant; cualquier `trustScore` distinto de 0, o cualquier profesional con al menos un trabajo completado, pasa sin modificar. Esto no toca el valor almacenado ni su significado en ningún otro lugar donde se lee `trustScore` (risk scoring, vistas de admin). Verificado por lectura de código + `tsc --noEmit` limpio. Sin test unitario dedicado nuevo para este branch específico (no hay suite de matching.repository.ts ejecutable en este checkout — ver nota de entorno en el commit). Pendiente verificación en vivo con un profesional recién registrado.

### 0.29 — ALTO (algoritmo) — Estimador de presupuesto no usa área ni ubicación; fallback mezcla trabajos sin relación
- **Qué:** confirmado en vivo — un job de "reparación de fugas" (referencia $80) recibió sugerencia de $2,074–$4,839, auto-aplicada sin confirmar.
- **Dónde:** `apps/api/.../budget-intelligence.service.ts:39-45,65-101,122-140`
- **Fix:** incorporar área/sqft y el multiplicador de `LocationCostService` (ya existe, no está conectado); no promediar categorías no relacionadas en el fallback de pocos datos.
- **Estado:** [x] Corregido (2026-07-22) — tres cambios en `budget-intelligence.service.ts`, conservador a propósito por ser un estimador con impacto de negocio real:
  1. **Fix principal (el que causó el incidente):** el fallback de "pocos datos similares" (`mids.length < 3`) ya no promedia `referenceJobs` completo sin filtrar por categoría. Ahora primero filtra por `input.category`; si hay trabajos de la misma categoría, promedia solo entre ellos; si no hay ninguno, **no inventa un número** — devuelve el mismo estado explícito de "sin base confiable" que ya usaba el caso de cero-datos-históricos (`min=max=median=0`, `confidence: "low"`, narrativa de IA recomendando 3 cotizaciones), en vez de mezclar categorías no relacionadas. Esto es exactamente lo que hubiera evitado el caso del leak repair de $80 → $2,074-$4,839.
  2. **`LocationCostService` conectado:** el servicio inyectado (opcional) ahora se llama con un `zipCode` explícito del input o extraído por regex de `location` (5 dígitos); si resuelve, se aplica el promedio de `materialMultiplier`/`laborMultiplier` al rango final (min/max/median) y se agrega un `factor` explicando el ajuste regional. Añadí `PricingModule` a los imports de `IntelligenceModule` para exponer el servicio.
  3. **`areaSqft` incorporado — con una limitación honesta:** agregué `areaSqft`/`zipCode` opcionales al input de `suggestBudget` (y a ambos endpoints del controller). **Los registros de `Job` no persisten área/sqft en el schema actual** (no existe esa columna — solo existe en el flujo separado de `smart-intake` para categorías específicas), así que no hay una base histórica de $/sqft de la que derivar un ajuste numérico sin un cambio de schema, que dejé fuera de alcance de este parche. Cuando se provee `areaSqft`, se usa como señal cualitativa honesta (un `factor` de "alcance pequeño" si ≤50 sqft o "alcance amplio" si ≥1500 sqft) en vez de fabricar un precio que los datos no respaldan.
  - **No tocado a propósito:** el auto-apply de la sugerencia sin confirmación del usuario, que el hallazgo también menciona. Es una decisión de flujo/UX (¿debería el estimador auto-rellenar el campo de presupuesto o solo sugerirlo?) separada de que el número en sí esté mal — no la rediseñé silenciosamente; queda como pregunta de producto abierta.
  - Verificado por lectura de código y `tsc --noEmit` limpio sobre los archivos tocados (incluyendo el nuevo import de `PricingModule`). **No hay test unitario ejecutable para `budget-intelligence.service.ts` en este checkout** (requiere Nest DI + Prisma real; el único test existente lo mockea con un stub que devuelve `null`). Pendiente verificación en vivo reproduciendo el caso original del leak repair.

### 0.30 — ALTO (algoritmo) — Calculadora de madera usa espaciado no estándar (18" en vez de 16" o.c.)
- **Dónde:** `packages/tools/src/materials/materials-calculator.ts:305`
- **Fix:** cambiar `lengthFt / 1.5` a `lengthFt / 1.333` (16" on-center).
- **Estado:** [x] Corregido (2026-07-22) — cambio de una línea en `estimateLumber()`: `input.lengthFt / 1.5` → `input.lengthFt / 1.333` (16" on-center, el estándar de framing en EE.UU.). Actualicé el texto de la nota del ítem ("1 stud cada 1.333 ft (16\" o.c.)") y el test existente en `tests/unit/materials-calculator.test.mjs` que verificaba el conteo viejo (14 studs para un muro de 16ft → ahora 16 studs), agregando un comentario explicando por qué cambió. Verificado: `tsc --noEmit` limpio en `@semse/tools` y 28/28 tests unitarios pasando.

### 0.31 — CRÍTICO — Función caída: "Calcular estimado" de ProTools da 404 en cada intento
- **Qué:** confirmado en vivo — `POST /api/semse/agents/protools/estimate` → 404. El frontend muestra el error crudo de parseo JSON al usuario.
- **Dónde:** `/client/protools` (frontend) — falta encontrar/crear la ruta backend correspondiente
- **Fix:** implementar o corregir la ruta del BFF/backend para ese endpoint; agregar manejo de error decente en el frontend para cuando la respuesta no sea JSON válido.
- **Estado:** [x] Corregido (2026-07-22) — causa real: el backend (`SemseAgentsController @Post("protools/estimate")` bajo `v1/agents/semse`) siempre existió y estaba bien; el 404 era puramente del BFF de Next.js — el archivo de ruta vivía en `agents/protools/route.ts`, que solo matchea `/api/semse/agents/protools` (sin `/estimate`), mientras el frontend pedía `/api/semse/agents/protools/estimate`. Se movió el handler a `agents/protools/estimate/route.ts` (ruta anidada correcta); no había otro caller usando la ruta vieja. También se agregó chequeo de `content-type` antes de parsear JSON en `/client/protools/page.tsx`, para no mostrar el error crudo de parseo si el backend responde algo no-JSON. Pendiente verificación en vivo.

### 0.34 — CRÍTICO — El flujo de subida "single_put" (la ruta normal para fotos/archivos livianos) nunca sube el archivo real a storage — confirmado en `/worker/evidence` y `/worker/travel`

- **Qué exactamente, confirmado en vivo:** subí una foto de evidencia real (JPEG) en `/worker/evidence` para el job `cmqvqsol40023k101j9fh81mq`. Traza de red completa capturada: `POST /api/semse/uploads/plan` → `200` (presign correcto, entrega `key` real + `uploadUrl` real hacia `/v1/uploads/files/:key`), **cero peticiones `PUT` en toda la sesión** (verificado sobre el log completo de red, no solo filtrado), luego `POST /api/semse/jobs/:jobId/evidence` → `400`. El archivo nunca llegó al storage — solo se intentó "registrar" una evidencia fantasma.
- **Causa raíz exacta (confirmada por código):** `apps/web/app/(app)/worker/evidence/page.tsx:120-154` (`handleUpload`). Para `recommendedStrategy !== "multipart"` (el caso normal — `"single_put"`, cualquier archivo bajo 25MB, es decir prácticamente toda foto/documento real) el código:
  1. Nunca usa `plan.uploadUrl` (el endpoint real de subida que devuelve el backend, `apps/api/src/modules/evidence/evidence.controller.ts:107`) — ninguna llamada `fetch`/`PUT` a esa URL en ninguna parte del bloque `single_put`.
  2. Nunca usa `plan.key` (la key real generada por `buildTenantStorageKey` en el backend) — en su lugar fabrica una key completamente distinta y local: `` `jobs/${selectedJobId}/evidence/${Date.now()}_${file.name}` `` (línea 131), que no corresponde a ningún objeto real en storage.
  3. Llama directo a `registerJobEvidence(jobId, { key: bucketKey, kind, ... })` (línea 150) con esa key fabricada — de ahí el 400, y en un escenario donde el backend no valide la existencia de la key, se registrarían evidencias apuntando a archivos que nunca existieron.
  4. Solo la rama `multipart` (archivos >25MB) sí hace `uploadMultipartPart` real — es decir, la ruta menos común es la única que funciona.
- **Mismo bug, mismo patrón, módulo distinto:** `apps/web/app/(app)/worker/travel/[travelId]/page.tsx:261-295` (`prepareReceiptUpload`, usado por comprobantes de viaje/hospedaje). Para la rama no-multipart, la función retorna directamente `planned.uploadUrl` (o `planned.key`) **como si fuera el `receiptUrl` final** (línea 273) y lo guarda tal cual en el formulario de gasto/hospedaje (líneas 302-307) — sin jamás hacer el `PUT` que subiría el archivo a esa URL. El campo `receiptUrl` que termina persistido apunta a un endpoint de subida vacío, no a un comprobante real. `missingReceipts` (page.tsx:422-424) solo valida que `receiptUrl` sea un string no vacío, así que esta URL falsa satisface igual la condición para poder cerrar la liquidación del viaje — la prueba del gasto se pierde en silencio, siempre, sin bloquear el cierre.
- **ACTUALIZACIÓN 2026-07-21 (ronda de 5 agentes de código sobre `apps/web/app/(app)/worker/**`) — la causa raíz exacta y el alcance real son peores de lo documentado originalmente:**
  1. **Causa raíz exacta del 400 en evidencia:** el backend (`apps/api/.../evidence.repository.ts:485-511`, `normalizeEvidenceBucketKey`) exige que la key cumpla `isTenantScopedStorageKey` (`tenants/{tenantId}/.../evidence/...`) o, solo fuera de producción, `isLegacyDomainStorageKey` (debe empezar literal con `evidence/`). La key fabricada por el frontend (`jobs/${selectedJobId}/evidence/...`) no cumple ninguno de los dos patrones **en ningún ambiente** — no es una falla específica de producción, el registro de un solo archivo fallará siempre, en cualquier entorno.
  2. **La rama "multipart" (que el documento original describía como la única que sí funciona) es igual de falsa, en ambos lados:** el frontend `uploadMultipartPart` (`apps/web/app/semse-api.ts:626-640`) hace `PUT` con solo un header `x-part-size` — **nunca adjunta el archivo real** (ni `.arrayBuffer()` ni `body`). Y el backend (`apps/api/.../evidence.controller.ts:282-319`) tampoco lee el cuerpo de la petición — solo marca la parte como `"uploaded"` y fabrica un `etag` a partir del header, sin escribir ningún byte a ningún lado. `completeMultipartSession` (`evidence.controller.ts:321-353`) ni siquiera devuelve `key`/`bucketKey`, así que el frontend vuelve a caer en la misma key fabricada del punto 1. **Conclusión: no existe ninguna ruta funcional para subir evidencia en `/worker/evidence`, ni pequeña ni grande — 0% de las subidas llegan a storage.**
  3. **El mismo patrón de "PUT falso" en la rama multipart se confirmó también en `/worker/travel/[travelId]`** (mismas funciones `uploadMultipartPart`/`completeMultipartUploadSession` compartidas) — igual, cero bytes reales en ningún caso, ni single-put ni multipart.
  4. **Contraste importante — SÍ existe una implementación correcta en el mismo repo:** `apps/web/app/semse-api.ts:720-764` (`uploadEvidenceFile`, usada correctamente por `apps/web/app/jobs/[jobId]/page.tsx:213`) hace el `PUT` real con el archivo, contra un endpoint (`POST v1/uploads/files/:key`, `apps/api/.../uploads.controller.ts:191-229`) que sí valida y guarda el archivo en disco. El endpoint funcional existe — las pantallas de worker simplemente nunca lo llaman.
  5. **Efecto en la vista de historial:** como el backend nunca devuelve `previewUrl`/`url`/`signedUrl`/`filename` en la respuesta de evidencia (`evidence.repository.ts` — `toEvidenceView`/`listByProject`/`findById` reconstruyen el objeto solo con `{id, tenantId, projectId, jobId, milestoneId, uploadedById, kind, key, metadata, createdAt}`), el link "Ver" nunca se renderiza para ninguna evidencia (ni siquiera una subida correctamente por otra pantalla) y el nombre mostrado es la key cruda de storage, no el nombre de archivo original. Además, `validationStatus`/`aiQualityScore` (columnas reales, usadas internamente por scoring/milestones) también se descartan en esa misma respuesta — el badge de estado de evidencia queda structuralmente pegado en "Pendiente" para siempre, sin importar qué diga la IA de calidad.
  6. **El "no hay evidencias registradas" que enmascaró un 500 real no es incidental — está garantizado estructuralmente:** `apps/web/app/(app)/worker/evidence/page.tsx:96-98` tiene un `.catch(() => [])` en `fetchJobEvidence` (convierte CUALQUIER error, incluido 500, en lista vacía) y además un `try{...} catch { /* keep empty */ }` exterior en la línea 105 sin ningún estado de error para el feed — no hay forma estructural de que un fallo del backend llegue nunca al usuario en esta pantalla.
- **Impacto real (revisado y ampliado):** la función de subir evidencia — el mecanismo que sustenta directamente la aprobación de milestones y la liberación de escrow — **no sube ningún archivo real bajo ninguna circunstancia** desde `/worker/evidence` ni desde `/worker/travel`. No se pudo confirmar en esta pasada si el módulo Cliente tiene un flujo de subida propio con el mismo patrón (no se encontró un `planUpload` client-side ahí en esta auditoría — a confirmar), pero si un cliente sube evidencia por `apps/web/app/jobs/[jobId]/page.tsx` (que sí usa el flujo correcto `uploadEvidenceFile`/`presignEvidence`), esa ruta específica no está afectada.
- **Fix esperado:** en `worker/evidence/page.tsx` y `worker/travel/[travelId]/page.tsx`, para la rama no-multipart, hacer `fetch(plan.uploadUrl, { method: "PUT", body: file, headers: { "content-type": contentType } })` antes de registrar/guardar, y usar `plan.key` real (no una key fabricada) — o, más simple, reusar directamente `uploadEvidenceFile`/`presignEvidence` de `semse-api.ts`, que ya funciona correctamente en el flujo de Cliente. Para la rama multipart, además, el backend (`evidence.controller.ts:282-353`) necesita leer y persistir el cuerpo real de cada parte, no solo simular el estado. Devolver `key`/`bucketKey` en `completeMultipartSession`. Agregar `previewUrl`/`filename`/`validationStatus`/`aiQualityScore` a la respuesta de `GET .../evidence`. Reemplazar el `.catch(() => [])` silencioso por un estado de error visible.
- **Estado:** [x] Corregido — parcial, ver desglose (Crew B, 2026-07-21):
  - **Confirmado por código: la rama "multipart" nunca era alcanzable desde `worker/evidence`** — `POST /v1/uploads/plan` solo devuelve `recommendedStrategy: "single_put" | "external_transfer"`, nunca `"multipart"`; el `if (strategy === "multipart")` de esa pantalla era código muerto. La causa raíz real y única para el caso común era el `single_put` nunca haciendo el `PUT`.
  - **`worker/evidence/page.tsx` y `worker/travel/[travelId]/page.tsx`:** ahora obtienen `plan.key` real, hacen `PUT` real del archivo a `/api/semse/uploads/files/:key` (el mismo proxy BFF que ya usaba `uploadEvidenceFile`, no `plan.uploadUrl` directo — ese apunta al backend interno y el navegador no puede alcanzarlo sin los headers de auth que solo agrega el BFF), y registran con la key real. `worker/evidence` además manda `filename` real al registrar.
  - **`recommendedStrategy: "external_transfer"` (archivos >25MB):** en vez de dejarlo simulando éxito (peor que fallar), ahora **falla con un mensaje claro** en ambas pantallas — arreglar la subida real para archivos grandes (persistencia real de partes multipart en `evidence.controller.ts:282-353`, que hoy no lee ni guarda ningún byte) queda **fuera de esta pasada**, es un cambio de backend más grande y afecta un caso de uso menos común.
  - **2.18/2.21/2.22 corregidos junto con esto** (mismo root cause) — ver su propia nota abajo.
  - **`.catch(() => [])` silencioso reemplazado** por un estado de error visible en `/worker/evidence` (banner rojo cuando falla la carga del historial).
  - Typecheck + lint + 87 tests de evidencia + suite completa de la API pasan.
  - **No corregido a propósito, deferido:** 2.45 (paquete de evidencia simulado en disputas) — ver su nota.
  - **Verificación en vivo (2026-08-01) — encontró y corrigió una regresión real que dejaba el fix roto de punta a punta, nunca detectada porque nunca se había probado con un servidor real:**
    - **Bug nuevo #1 — Fastify rechaza el `PUT` con 415 antes de llegar al controller.** El API corre sobre Fastify (`NestFastifyApplication`, `apps/api/src/main.ts`), que por defecto solo auto-parsea `application/json`/`*-urlencoded` — cualquier otro `Content-Type` (incluido `image/png`, el caso normal de una foto de evidencia) se rechaza con `415 Unsupported Media Type` **a nivel de framework, antes de que el controller se ejecute**. El propio whitelist de tipos permitidos en `uploads.controller.ts` (`ALLOWED_CONTENT_TYPES`) nunca llegaba a evaluarse. **Fix:** se registró un content-type parser pass-through en `main.ts` (`fastify.addContentTypeParser("*", (req, payload, done) => done(null, payload))`) que entrega el stream crudo sin consumirlo, dejando que `putFile` siga haciendo su propia validación/sniffing de magic bytes como ya estaba diseñado.
    - **Bug nuevo #2 — `@Req()` bajo Fastify no es el stream crudo.** `putFile` estaba tipado `@Req() req: IncomingMessage` (asumiendo Express) y hacía `for await (const chunk of req)` directo — pero con `FastifyAdapter`, `@Req()` inyecta el objeto `FastifyRequest`, no el `IncomingMessage` de Node; el stream real vive en `req.raw`. Sin el fix de arriba esto nunca se ejecutaba (moría antes en el 415); con el 415 resuelto, esto producía `500 — "stream is not async iterable"`. **Fix:** `putFile` ahora tipa `@Req() req: FastifyRequest` y pasa `req.raw` a `validateUploadStream`.
    - **Bug nuevo #3 — `getFile` (descarga) usaba `res.set()`, método de Express que no existe en `FastifyReply`.** Nunca antes probado en vivo — moría con `500 — "res.set is not a function"` en cualquier intento de ver/descargar una evidencia ya subida (afecta directamente el link "Ver" de **2.21**). **Fix:** tipado `@Res() res: FastifyReply` + `res.headers({...})` (equivalente Fastify de `res.set(obj)` de Express).
    - **Los 3 bugs son el mismo patrón** — este archivo se escribió/mantuvo asumiendo semántica de Express en una app que corre sobre Fastify; un grep encontró un caso más con el mismo síntoma en `trust.controller.ts:66` (`verifyPassport`, `POST /v1/trust-passport/verify`, endpoint `@Public()`), tipado `{setHeader(...):void}` hand-rolled — Fastify tampoco tiene `.setHeader()` (eso es el `ServerResponse` crudo de Node/Express; Fastify usa `.header()`/`.headers()`). **Corregido también (2026-08-01):** tipado `@Res() res: FastifyReply` + `res.header(...)`. Verificado en vivo: `POST /v1/trust-passport/verify` con un token inválido ahora devuelve `201` con el header real `x-semse-crypto-profile: unknown` presente en la respuesta — antes moría con `500 — res.setHeader is not a function` en cualquier llamada, para cualquier caller (endpoint público, sin autenticación). `tsc --noEmit`/`eslint` limpios; suite completa 2042/2042 verde (sin test dedicado nuevo para este endpoint — no hay convención de test HTTP end-to-end para controllers en este repo, solo se agregó cobertura de stream/headers en `uploads.controller.test.ts` por ser el caso más crítico).
    - **Hallazgo adicional del mismo grep, código muerto (no un bug en producción):** `apps/api/src/middleware/security.ts` — un archivo completo de "seguridad" (helmet, CORS, rate-limit, sanitización de input) escrito 100% con la API de Express (`import { Express } from 'express'`), con `res.set(...)` en su `cacheMiddleware`. Confirmado por grep que **`setupSecurity`/`cacheMiddleware` no se importan en ningún otro archivo** — nunca estuvo cableado a la app real (`main.ts` ya monta su propio `@fastify/helmet`/`@fastify/cors` nativos de Fastify). Confirmado también que `express`/`cors`/`helmet`(Express)/`express-rate-limit` **no están declarados como dependencia de `apps/api`** en absoluto (solo `apps/assistant-portal`, una app distinta, depende de `express`) — solo "compilaba" por el hoisting plano de `node_modules` del workspace. Eliminado (2026-08-01) por ser código muerto que además da una falsa sensación de que hay una capa de seguridad Express activa cuando la real vive en `main.ts`. `tsc --noEmit`/`eslint`/suite completa (2042/2042) sin cambios tras el borrado — cero referencias rotas.
    - **Verificación end-to-end real, contra el stack local (Postgres/Redis/API/web, cuenta `worker@demo.semse`):** `POST /v1/uploads/plan` → `PUT` del archivo real (PNG de prueba, 73 bytes) → `200` con `sizeBytes` exacto → `POST /v1/evidence` con la key real → `201`, `validationStatus:"pending"` → `GET /v1/uploads/files/:key` → `200`, archivo descargado **byte-idéntico** al original. Los 3 pasos que el hallazgo original describía como rotos (`plan.uploadUrl` nunca llamado, key fabricada, subida nunca llega a storage) ahora sí funcionan de punta a punta.
    - Regresión cubierta con un test nuevo en `apps/api/test/uploads.controller.test.ts` (stream Fastify-shaped real + `reply.headers()` real, sube y vuelve a descargar bytes idénticos) — antes solo había asserts de metadata de permisos, nada que ejerciera el stream/headers real, por eso esto pasó desapercibido. Suite completa de `@semse/api`: 2042/2042 verdes. `tsc --noEmit`/`eslint` limpios.
    - **No verificado en esta pasada:** la subida real vía la UI de `/worker/evidence` en el navegador (la cuenta demo no tiene un job en el estado que ese selector específico requiere — gap de datos semilla, no de código) y el flujo `external_transfer`/multipart (ya documentado arriba como explícitamente no implementado).

### 0.32 — CRÍTICO — El flujo de "olvidé mi contraseña" nunca envía el correo (confirmado en producción, bloqueó a un usuario real)
 - **Qué:** `requestPasswordReset` genera el token, lo guarda en `PasswordResetToken`, registra el audit log — y ahí termina. No hay ninguna llamada a un servicio de correo/notificación en toda la función ni en ningún import de `auth.service.ts`. Confirmado contra la base de datos real: 4 tokens `ACTIVE`/`consumedAt: null` creados para una cuenta real (`[REDACTED_ACCOUNT_EMAIL]`, rol PRO) en un lapso de 9 minutos, ninguno llegó nunca por correo. **Confirmado de nuevo el 2026-07-21** contra la misma cuenta: 4 solicitudes más en 9 minutos, otra vez ninguna consumida — el bug sigue vivo y ya bloqueó a este mismo usuario real dos veces en menos de 24 horas.
- **Dónde:** `apps/api/src/modules/auth/auth.service.ts:304-336` (`requestPasswordReset`)
- **Impacto real:** un usuario real quedó completamente bloqueado de su cuenta — sin contraseña correcta y sin forma de resetearla por la vía normal del producto. Se le desbloqueó manualmente escribiendo un hash de contraseña temporal directo en la base de datos (2026-07-20, y de nuevo 2026-07-21) como parche de emergencia, no como fix.
- **Fix:** conectar `requestPasswordReset` a un servicio de envío de correo real (revisar si `apps/api/src/modules/notifications` ya tiene un canal de email reutilizable) que mande el link con el `rawToken` (el mismo que hoy se descarta al no ser producción `resetTokenPreview`).
- **Estado:** [x] Corregido (Crew D, 2026-07-21) — **no existía ningún módulo de notifications con canal de email reutilizable** (se verificó explícitamente, cero infraestructura de email en todo el backend). Se creó `apps/api/src/infrastructure/email/email.service.ts` (wrapper delgado sobre Resend, paquete `resend` agregado a `apps/api`), y `requestPasswordReset` ahora arma el link real (`${SEMSE_WEB_BASE_URL}/reset-password?token=...`) y lo envía. **Preserva explícitamente la propiedad de seguridad ya confirmada como correcta** ("Password reset — no filtra si un email existe"): la respuesta siempre es `{status: "accepted"}` sin importar si el usuario existe o si el envío falló — un fallo de envío solo se loguea (`logger.error`) para ops, nunca cambia la respuesta al cliente. **Requiere acción del usuario fuera de este código:** configurar `RESEND_API_KEY` y `EMAIL_FROM` (remitente verificado) como variables de servicio en Railway para el servicio de la API; sin ellas, `EmailService` loguea advertencia y no envía (mismo síntoma que antes, pero ahora visible en logs en vez de silencioso). — **La contraseña temporal debe revocarse/reemplazarse por el usuario en cuanto este flujo funcione en producción real.** **Verificación en vivo confirmada (2026-08-01)** contra el stack local (sin `RESEND_API_KEY` configurado, mismo estado que "no configurado"): `POST /api/semse/auth/forgot-password` devolvió `{"ok":true}` idéntico tanto para un email real (`worker@demo.semse`) como para uno inexistente — no filtra existencia de cuenta. El log del API confirma el comportamiento exacto documentado: `email.send skipped — provider not configured` + `auth.passwordResetEmail failed to send` se loguean como `warn`/`error` para ops, pero la respuesta al cliente siguió siendo `201`/`{ok:true}` — el fallo nunca cambia la respuesta.

### 0.33 — CRÍTICO — El chat de Prometeo y de los 16 agentes especializados está roto para los roles PRO y WORKER (permiso RBAC que ningún endpoint de chat acepta)

- **Qué exactamente:** confirmado en vivo y 100% reproducible — en `/worker/dashboard`, enviar cualquier mensaje a Prometeo o a Felix desde el widget flotante devuelve siempre `⚠ Insufficient permissions` (string cruda en inglés, sin traducir). El mensaje del usuario ni se agrega como enviado.
- **Causa raíz exacta:** `POST /v1/ai-models/prometeo/chat` (`apps/api/src/modules/ai-models/ai-models.controller.ts:213-214`) exige `@RequirePermissions("agents:run:create")`. La matriz de roles (`packages/auth/src/rbac.ts:3-103`) da ese permiso a `CLIENT` (línea 41) y `OPS_ADMIN` (línea 184), pero **no a `PRO`** (líneas 58-103) **ni a `WORKER`** (líneas 104-116) — esos dos roles reciben en cambio `"agents:run:worker"`/`"agents:run:manage"`, permisos distintos que el guard de este endpoint nunca compara.
- **Alcance — mismo decorador, mismo bloqueo, en TODOS estos controladores:** `agents.controller.ts` (`/agents/chat` y lectura de threads), `prometeo.controller.ts` (14 endpoints), `prometeo-copilot.controller.ts` (el módulo "Prometeo Copilot" backend), `forge.controller.ts` (chat con el agente Forge/dev), `browser-agent.controller.ts`, `orchestration.controller.ts`, y el resto de `ai-models.controller.ts`. Los 16 agentes listados en `/agents` ("Conversacionales") comparten el mismo endpoint con `agentId` como parámetro — no hay ninguna rama por agente antes del guard, así que los 16 fallan igual para PRO/WORKER.
- **Confirmado de nuevo en otra ubicación (ronda de agentes de código, 2026-07-21):** los dos botones principales de autogestión de `/worker/disputes` ("Pedir criterio" y "Pedir tercero humano", `DisputeResolutionWorkspace.tsx:352-448`) llaman `POST /v1/agents/copilot`, gateado por el mismo `agents:run:create` — fallan siempre para cualquier PRO, en un flujo que la propia pantalla describe como su herramienta central de autogestión ("evidencia, criterio del copiloto y escalación asistida"). Ver 2.46.
- **Efecto colateral de UX:** el ítem de sidebar "Asistente IA" (`apps/web/app/(app)/layout.tsx:90`, `labelKey: nav.aiSettings`) en realidad enlaza a `/worker/settings` (configuración de tono del asistente), no a un chat — la traducción española de esa clave dice "Asistente IA" mientras la inglesa dice, correctamente, "AI Settings" (`apps/web/lib/language-context.tsx:81` vs `:739`). Generó confusión real durante la auditoría.
- **Impacto:** todo profesional real en producción que use el botón de ayuda de Prometeo (visible de forma permanente en toda la UI de `/worker/*`) recibe un error técnico sin explicación — la función de IA conversacional no funciona en absoluto para el rol que más la usaría en campo.
- **Fix:** agregar `"agents:run:create"` al array de `PRO` y de `WORKER` en `packages/auth/src/rbac.ts` (fix mínimo de una línea cada uno). A mediano plazo, extender `RbacGuard`/`@RequirePermissions` para aceptar alternativas (OR) en vez de solo AND, y que el frontend traduzca/oculte errores 403 crudos. Corregir además la traducción de `nav.aiSettings` en español.
- **Detalle completo:** ver `docs/specs/ui/pro-flows-remediation.spec.md` → G-PRO-05.
- **Estado:** [x] Corregido (Crew A, 2026-07-21) — `agents:run:create` agregado a `PRO` y `WORKER` en `packages/auth/src/rbac.ts`. Corregida también la traducción de `nav.aiSettings` en español (`apps/web/lib/language-context.tsx:81`, ahora "Configuración del asistente"). La extensión OR de `RbacGuard` queda como mejora a mediano plazo, no bloqueante — el permiso aditivo ya resuelve el 403. **Verificación en vivo confirmada (2026-07-31/08-01):** stack local completo, login como `worker@demo.semse`, `POST /api/semse/cortex/chat` (endpoint real usado por `chatWithPrometeo`/`agent-chat-panel.tsx`, no `/v1/ai-models/prometeo/chat` — ese es el endpoint del backend, la UI pasa por el BFF `cortex/chat`) devuelve `200` con una respuesta real de Prometeo ("Hola, soy Prometeo. ¿En qué puedo ayudarte hoy?"), no el `403`/"Insufficient permissions" documentado originalmente.

### 0.35 — CRÍTICO (dinero) — El saldo disponible para liberar un escrow se calculaba con depósitos fallidos incluidos, encontrado auditando Admin/Finance

- **Qué exactamente:** encontrado revisando `apps/web/app/(app)/admin/finance/page.tsx` (panel de escrow de Admin) durante Crew G — el KPI "En escrow" ahí resultó estar filtrando por `status === "PENDING"` en vez de `"SUCCEEDED"` (mostraba como "en escrow" justo el dinero que **todavía no se confirmó**, no el que sí). Al rastrear el campo real que alimenta esos números se encontró el problema de fondo, más serio, en el propio backend que reescribí en Crew D: `PaymentEscrow.totalAmount` se incrementa de forma optimista en el momento de la **reserva** de un depósito (`depositFunds`, `payments.repository.ts:289`), y `finalizeDeposit` (`payments.repository.ts:337-349`) **nunca lo revierte** si ese depósito termina en `FAILED` — el campo queda inflado para siempre por cualquier intento de depósito que no se haya completado (tarjeta rechazada, etc.).
- **Dónde exactamente se explotaba esa inflación:** `releaseFunds()` (`payments.repository.ts:351-425`, dentro de la transacción atómica) y `release()` a nivel de servicio (`payments.service.ts:566-...`, el pre-chequeo antes de esa transacción) calculaban ambos el saldo disponible como `Number(escrow.totalAmount) - liberado - reembolsado` — usando directamente el campo inflado. El camino paralelo de reembolso (`getRefundContext()`/`refundFunds()`, mismo archivo) sí lo hacía bien: `sumTransactions(transactions, "DEPOSIT", ["SUCCEEDED"])`, contando solo depósitos confirmados. Es decir, ya existía el patrón correcto dos funciones más abajo en el mismo archivo — solo `release` no lo seguía.
- **Impacto:** un intento de depósito fallido (tarjeta rechazada, 3DS cancelado, etc.) inflaba permanentemente el saldo "disponible" de ese escrow por el monto del intento fallido, permitiendo liberar a un profesional más dinero del que un cliente llegó a depositar exitosamente. Este bug se introdujo en el propio rework de integridad de pagos de **Crew D** (0.12-0.17) de esta misma sesión — no es preexistente, es una inconsistencia entre dos funciones hermanas escritas en momentos distintos del mismo trabajo.
- **Fix:** `releaseFunds()` ahora calcula el saldo disponible sumando solo transacciones `DEPOSIT` con `status: "SUCCEEDED"` (nueva query, mismo patrón que `getRefundContext`), en vez de leer `escrow.totalAmount` directo. `release()` en el service usa el mismo criterio vía un nuevo `getDepositedAmount()` (paralelo a `getReleasedAmount`/`getRefundedAmount`, que ya filtraban correctamente por `SUCCEEDED`). También se corrigió `finance.repository.ts` → `getProjectFinancialSummary()` (usado por `/v1/finance/:projectId/summary`, consumido por `client/projects/[projectId]/copilot`, `worker/jobs/[jobId]`, y varias pantallas de Admin — pmo/qa/semse-x/ai-mission-control): comparaba `status === "COMPLETED"`, un valor que **no existe** en el enum real `PaymentTxnStatus` (`PENDING|SUCCEEDED|FAILED|REVERSED`), así que `escrowReleased` siempre daba 0 y `pendingRelease` siempre mostraba el escrow completo (inflado además por el mismo `totalAmount`) como pendiente de liberar, sin importar cuánto ya se hubiera liberado realmente.
- **Frontend (Admin Finance):** `admin/finance/page.tsx` — KPIs "En escrow"/"Liberado"/"Reembolsado" del panel de escrow ahora filtran `status === "SUCCEEDED"` (antes "En escrow" usaba `"PENDING"` invertido, y "Liberado"/"Reembolsado" no filtraban por status en absoluto, mismo patrón que 2.39/G-PRO-12). Cada transacción listada ahora muestra un segundo badge con su `status` real, y las filas `FAILED`/`REVERSED` se muestran tachadas/atenuadas en vez de indistinguibles de una exitosa.
- **Hallazgo adicional en la misma pantalla (UX/confirmación), ver también 3.2:** el botón "Liberar" del panel de escrow disparaba la liberación real de fondos con un solo clic, sin ningún paso de confirmación — a diferencia del botón "Reembolsar" en la misma pantalla, que sí abre un formulario con monto/razón antes de confirmar. Se agregó el mismo patrón de confirmación explícita a "Liberar". Además, los botones de aprobar/rechazar gasto y enviar/cobrar factura en esa misma pantalla ignoraban el resultado de la petición (`.catch(() => null)`, sin ningún error visible) — un fallo real quedaba indistinguible de un éxito; ahora cada fila muestra el error real si la acción falla.
- **Estado:** [x] Corregido (Crew G, 2026-07-21). Validado: `@semse/api` typecheck limpio, suite completa de tests (104 tests dirigidos a payments/finance/escrow + suite completa 1984/1985, único fallo el preexistente no relacionado de `graphify.service.test.ts`) en verde. **No probado en vivo** — no hay credencial OPS_ADMIN ni sandbox de Stripe en esta sesión; el escenario exacto (depósito fallido seguido de intento de liberación) tampoco tiene un test unitario nuevo que lo reproduzca end-to-end, solo se corrigió por lectura de código contra el patrón ya correcto de `refundFunds`. Recomendado: agregar ese test de regresión específico antes de dar esto por cerrado del todo.

---

## Sección 1 — Módulo Cliente (auditado completo, código + en vivo, 2026-07-20)

### Dinero sin confirmación (UI)
- **1.1 CRÍTICO** — Fondear escrow / liberar pago sin confirmación ni monto visible. `apps/web/app/(app)/client/jobs/[jobId]/page.tsx` — handleFundEscrow:288-301 · handleRelease:333-350 · botones:728-745,923-930. [x] Corregido (2026-07-23) — `handleFundEscrow` ahora abre el `EscrowFundModal` ya existente (mismo componente cableado en `client/payments`) en vez de llamar `fundJobEscrow` directo; muestra monto, proveedor/método y un paso de confirmación explícito antes de procesar. `handleRelease` ya no libera el pago al primer clic — abre un `ConfirmDialog` nuevo (`apps/web/components/ui/confirm-dialog.tsx`) que muestra el hito y el monto exacto (`formatMoney`) y solo llama `releaseMilestoneEscrow` tras confirmar explícitamente. Se agregó `ConfirmDialog` como componente genérico reutilizable para este tipo de acción (money/dispute), en vez de un modal ad-hoc por sitio.
- **1.2 CRÍTICO** — Mismo patrón en `apps/web/app/jobs/[jobId]/escrow/page.tsx:75-94` y `packages/ui/src/components/EscrowTimeline.tsx:256-264` — el modal seguro (`EscrowFundModal`) ya existe pero solo está cableado en `client/payments`. [x] Corregido (2026-07-23) — `apps/web/app/jobs/[jobId]/escrow/page.tsx`: se quitó el formulario ad-hoc de la barra lateral (input de monto + botón que llamaba `fundJobEscrow` directo) y se reemplazó por el botón "Fondear escrow" abriendo el `EscrowFundModal` existente, exactamente como pide el hallazgo. `EscrowTimeline.tsx:256-264` (botón "Liberar pago", compartido por esta pantalla vía `onReleaseMilestone`) es una acción de **liberación**, no de fondeo — `EscrowFundModal` no es reusable ahí (endpoint distinto, monto por milestone en vez de por job, sin paso de "proveedor/método"). En su lugar se agregó un paso de confirmación inline dentro del propio `MilestoneRow` (mismo componente compartido, sin nuevo componente de modal): el clic en "Liberar pago" abre un estado de confirmación con el monto exacto del milestone y botones Cancelar/Confirmar, en vez de disparar `onRelease` directo. Se documenta esta desviación explícita del "no construir un modal nuevo": se interpreta como "no reinventar la infraestructura de confirmación segura" — se reusó `EscrowFundModal` donde aplica (fondeo) y se extendió el patrón de confirmación (mostrar monto + segundo clic) donde no aplica (liberación).
- **1.3 CRÍTICO** — "Resolver disputa" del cliente fijo a `pro_favor`, sin opción ni confirmación. `apps/web/app/jobs/[jobId]/page.tsx:276-293`. [x] Corregido (2026-07-23) — se investigó si se podía dar a elegir el resultado real, tal como pide el hallazgo. `apps/api/src/modules/disputes/disputes.policy.ts` (`assertDisputeResolvable`) confirma que el backend **solo permite** a un actor CLIENT resolver con `resolutionType: "pro_favor"` — reembolsos, splits y escalamiento legal exigen `OPS_ADMIN` y son rechazados con `ForbiddenException` para cualquier otro actor. Construir un selector de resultado en esta pantalla habría ofrecido opciones que la API rechaza. El fix implementado es el "como mínimo" que el propio hallazgo permite: `handleResolveDispute` ya no dispara `resolveDispute` al primer clic — abre un `ConfirmDialog` que explicita el único resultado disponible ("a favor del profesional") y su consecuencia real (libera los fondos retenidos en escrow), con botón de confirmación explícito antes de llamar a la API.

### Navegación y datos desconectados (confirmado en vivo)
- **1.4 CRÍTICO — patrón sistémico, CAUSA RAÍZ CONFIRMADA (ver 0.0)** — Dashboard y Trabajos→Activos muestran "0" mientras la cuenta tiene jobs reales `ACCEPTED`: confirmado que es el bug de comparación mayúsculas/minúsculas de **0.0** (`client/dashboard/page.tsx:119`, `client/jobs/page.tsx:113`). Mis hitos/Proyectos/Pagos en cero es un caso aparte y legítimo — no hay milestones creados todavía para este job, eso sí parece dato real, no bug (verificar si acaso hay una segunda causa ahí antes de cerrar). [x] Corregido (Crew A, 2026-07-21) — fix en 0.0. Verificación en vivo confirmada (2026-07-31/08-01): login como `client@demo.semse`, `/client/dashboard` mostró "Trabajos activos: 1", "Completados: 1", "Presupuestos activos: $2,800" — no cero, con la lista "Trabajos recientes" mostrando los 5 jobs reales con sus estados en español ("Publicado"/"Completado"/"En progreso").
- **1.5 ALTO** — El rol "Cliente" mezcla dos personas (dueño que contrata vs. contratista con CRM propio) — `/client/leads`, `/client/bids` (copy de "aplica a trabajos"), `/client/marketplace` (cliente ve su propio job con botón "Aplicar"). [x] Corregido (2026-07-27) — **decisión de producto explícita del usuario: mantener un solo rol Cliente, agregar un selector/agrupación de contexto explícito en vez de dividir en dos roles.**
  - **El bug concreto de "Aplicar" sobre el propio job, corregido de raíz:** `MarketplaceService.listOpenJobs()`/`getStats()` nunca filtraban por organización — un cliente browseando el marketplace como contratista veía (y podía loguear un bid sobre) los jobs publicados por su propia org. Ambos métodos aceptan ahora `excludeOrgId` (el `orgId` real del actor, resuelto server-side en `marketplace.controller.ts` — nunca confiar en algo que mande el cliente), que agrega `clientOrgId: { not: excludeOrgId }` al `where` de Prisma. Sin efecto para PRO/WORKER/OPS_ADMIN (sus orgs nunca coinciden con `clientOrgId` de un job real). 3 tests nuevos en `test/marketplace.service.test.ts` (excluye la org propia, es retrocompatible sin el parámetro, y lo mismo para `getStats`).
  - **El selector de contexto:** se descubrió que el campo `section` en los items de navegación (`apps/web/app/(app)/layout.tsx`) existía desde antes pero **nunca se renderizaba en ningún lado** — metadata muerta. Se implementó el render real (encabezados de grupo que aparecen cuando cambia `item.section` entre un ítem y el siguiente) en **los dos sistemas de sidebar paralelos que ya documenta 1.17** (el `Sidebar` de estilos inline usado en mobile+desktop-sin-AppShell, y el `AppShell`/`shellNavModel` de Tailwind usado en desktop para roles no-admin — se extendió `ShellNavLink`/`buildShellNavItems()` en `lib/navigation-shell.ts` para propagar `section` hasta ahí, ya que antes se descartaba). El array de navegación del rol Cliente se reordenó y etiquetó en dos grupos contiguos y claramente rotulados: **"Como comprador — contratás profesionales"** (publicar trabajo, mis proyectos, copiloto IA, hitos, profesionales, documentos, reseñas, pagos, finanzas) y **"Como contratista — ofrecés tus servicios"** (leads/CRM propio, marketplace, mis propuestas, ProTools), con un grupo final "Cuenta" para seguridad de cuenta. No se dividió en dos roles/dashboards — mismo login, mismo rol `CLIENT`, solo agrupación visual explícita para que sea obvio qué "sombrero" corresponde a cada pantalla.
  - `tsc --noEmit`/`eslint` limpios en `@semse/web` y `@semse/api`. Suite completa de `@semse/api`: 1975/1975 verdes. **Agrupamiento del sidebar confirmado en vivo (2026-07-31/08-01):** login como `client@demo.semse`, `/client/dashboard` mostró los dos grupos exactos ("Como comprador — contratás profesionales" / "Como contratista — ofrecés tus servicios") con los ítems correctos en cada uno. **Sigue pendiente:** confirmar en vivo que un job propio ya no aparece en el marketplace propio (`/client/marketplace`) — no se probó en esta pasada (la cuenta demo no tenía un job propio + marketplace navegado en la misma sesión).
- **1.6 ALTO** — `/dashboard` (ruta huérfana) carga con todo en cero y expone un banner de migración interna ("Mission Control") a cualquier cliente. [x] Corregido (2026-07-27) — a diferencia de la ruta huérfana de field-ops (1.7/2.7), acá el destino correcto de redirect depende del rol real de quien visita (`/client/dashboard` vs `/worker/dashboard` vs `/admin/dashboard`), así que el fix vive en `middleware.ts`, que ya decodifica la sesión para el resto de su lógica de enforcement por rol — se agregó un caso nuevo para `pathname === "/dashboard"` (antes de la resolución de rutas públicas/protegidas) que redirige a `defaultDashboardForRole(role)` si hay sesión válida, o a `/login` si no la hay, reusando `roleFromRoles`/`defaultDashboardForRole`, ya importados en el archivo. **Hallazgo adicional confirmado al investigar:** `app/dashboard/page.tsx` nunca miraba la sesión de quien visitaba — leía `SEMSE_TENANT_ID`/`SEMSE_ORG_ID`/`SEMSE_USER_ID`/`SEMSE_ROLES` (default `OPS_ADMIN`) de variables de entorno del servidor para decidir qué jobs mostrar, sin relación con el usuario real. Como el middleware ahora intercepta `/dashboard` antes de que el componente de página se ejecute nunca (su matcher ya cubre todo excepto assets estáticos), se reescribió `app/dashboard/page.tsx` como un fallback inerte (`redirect("/login")`, mismo patrón que 1.7/2.7) y se eliminó `app/dashboard/dashboard-client.tsx` (325 líneas, sin ninguna otra referencia en el repo) en vez de dejarlo como código muerto sin alcanzar. `tsc --noEmit`/`eslint` limpios en `@semse/web` para `middleware.ts` y `app/dashboard/page.tsx`. Sin test dedicado para `middleware.ts` (no existe suite para ese archivo en el repo); validado por lectura de código contra el patrón ya usado en las líneas 107-111/154-158 del mismo archivo. Verificación en vivo confirmada (2026-08-01): `/dashboard` como `worker@demo.semse` redirige a `/worker/dashboard`; como `client@demo.semse` redirige a `/client/dashboard` — el redirect por rol funciona como se diseñó. (No se probó el caso sin sesión → `/login` en esta pasada.)
- **1.7 MEDIO** — `/field-ops` (tercera implementación huérfana) carga sin nav, callejón sin salida. [x] Corregido — **mismo archivo que 2.7** (`apps/web/app/field-ops/page.tsx`), encontrado independientemente desde dos ángulos de auditoría (navegación en vivo del lado Cliente aquí; auditoría de código del módulo Worker en 2.7) contra la misma ruta huérfana de nivel superior. Ver el detalle completo del fix (reescrito como redirect a `/worker/field-ops`, ~929 líneas de UI duplicada eliminadas) en **2.7**. No se necesitó ningún cambio adicional. Verificación en vivo confirmada (2026-08-01): `/field-ops` como `worker@demo.semse` redirige correctamente a `/worker/field-ops`.
- **1.8 MEDIO** — Error de hidratación de React (`#418`) en `/client/milestones`, zona vacía sin estado vacío real. [x] Corregido — se agregó un estado vacío explícito en `/client/milestones/page.tsx` que se muestra cuando no hay trabajos activos con hitos, evitando el panel `HtmlInCanvasPanel` vacío de 380 px. Incluye mensaje orientador y link a "Ver mis trabajos". Pendiente verificación en vivo.

### Interacción / usabilidad (confirmado en vivo)
- **1.9 MEDIO** — Tema claro/oscuro no sobrevive un refresh/URL directa (solo persiste en navegación SPA). [x] Corregido — `AppLayoutInner` ahora sincroniza el tema con `localStorage` mediante `useEffect` (lee al montar y persiste en cada cambio) y aplica `data-theme` al `documentElement`, de modo que el modo claro/oscuro se restaura tras refresh o acceso directo a una URL. La selección del `Topbar` refleja el valor persistido. Verificación en vivo confirmada (2026-08-01): se cambió el tema a "Claro" y se hizo un `page.reload()` (recarga real, no navegación SPA) — el tema siguió en "Claro" después del refresh.
- **1.10 MEDIO** — FAB de asistente tapa el monto de una propuesta en mobile (~390-500px) en `/client/jobs/[jobId]`. [x] Corregido — se agregó `pb-24 md:pb-0` al contenedor raíz de `/client/jobs/[jobId]/page.tsx` para asegurar que el último contenido (incluyendo el monto de la propuesta en la última tarjeta) quede por encima del FAB de Prometeo en viewports móviles. En desktop se mantiene el padding normal de la página. Pendiente verificación en vivo.
- **1.11 MEDIO** — Catálogo de 24 agentes en `/agents`: la mayoría de las tarjetas no hacen nada al clic (sin `aria-label`); el FAB que sí abre chat ignora cuál tarjeta se clickeó y siempre abre Prometeo; solo 6 de 24 agentes son alcanzables. [x] Ya resuelto — confirmado por código 2026-07-27, no requirió cambios. `apps/web/app/(app)/agents/page.tsx` no coincide con la descripción del hallazgo en el estado actual del repo (única entrada en el historial de git de este archivo es el commit baseline del monorepo — es decir, este es el código real desde el primer commit de este checkout, no un fix reciente sin documentar): las 24 tarjetas son clicables (`onClick={() => setSelected(...)}`) y expanden un panel de detalle real con un botón de acción; para los 16 "Conversacionales", `PANEL_AGENT_ROUTE_MAP` enruta cada uno a uno de los 6 agentes reales del panel de chat (`assistant/marta/planner/felix/pulse/justus`, definidos en `agent-panel-state.tsx` y consumidos por `agent-chat-panel.tsx`) — 6 abren chat directo, los otros 10 muestran explícitamente "Canalizado vía X" y el botón "Abrir en X" sí llama `openPanel(panelAgentId)` con el agente **correcto**, no un Prometeo hardcodeado; los 8 "Especializados" muestran su descripción con indicador "Backend activo" (son backend-only por diseño, sin chat, consistente con el resto de la documentación de este mismo plan). El texto visible de cada botón (`agent.name`/`agent.role`/`agent.desc`) ya constituye su nombre accesible — no hace falta un `aria-label` adicional. `/agents` tiene entrada de navegación real para los 3 roles (`layout.tsx`), no es una ruta huérfana. Conclusión: el hallazgo probablemente describía una versión anterior del componente ya superada; no hay ningún cambio de código pendiente aquí.
- **1.11b CRÍTICO** — El chat de Prometeo (real, vía Anthropic) confirma el bug de "cero" por una tercera vía: preguntado directo, responde "Tienes 0 trabajos activos" para una cuenta con jobs `Aceptado`/`ACCEPTED` reales. Además, sin que se le pida, menciona un **"nivel de confianza del sistema: 26/100, nivel crítico"** ligado a "1 hito activo" — una métrica que no aparece en NINGUNA otra pantalla del producto (ni Dashboard, ni Hitos). Hay que averiguar de dónde saca Prometeo ese número y por qué no se muestra en la UI normal.
  - **Investigación (2026-07-24):** no existe ningún campo/métrica real llamado "nivel de confianza del sistema" en el backend — se buscó en todo `apps/api/src` (`confianza`, `confidence`, `crítico`, cualquier fórmula 0-100) y no aparece. La fuente real casi segura es la dimensión **Trust** del **Lente Ecosistema 5D** (`ecosystem-5d.service.ts`, `trustScore` real 0-100, `statusForScore()` marca `<40` como `"critical"` — coincide con un 26/100 "crítico" con datos por defecto), inyectada tal cual en el contexto de Prometeo por `operational-context.service.ts:formatContextBlock` y `prometeo-orchestrator.service.ts:buildOperationalReport` como `Trust: X/100 (critical) — <resumen>`, a nivel **proyecto/tenant**, no del sistema completo. El modelo la parafraseó ("Trust"→"confianza", "critical"→"crítico") y agregó "del sistema" por su cuenta — ninguna instrucción del prompt le pedía ese nombre exacto, pero sí lo invitaba a "interpretar métricas, no solo repetirlas" (persona de Pulse). El "1 hito activo" es una línea vecina en el mismo bloque de contexto, no está causalmente ligado al score. Reporte completo del agente de investigación disponible en la sesión.
  - **Fix aplicado:** no se construyó UI nueva para exponer esta métrica (decisión: la superficie/nombre-de-cara-al-usuario de un score de "confianza" es una llamada de producto, no algo que un fix de prompt deba decidir por su cuenta). En su lugar, se corrigió la causa concreta del hallazgo — que el modelo inventa un nombre y un alcance que el dato real no tiene: (1) el header del bloque de contexto ahora dice explícitamente `## Lente Ecosistema 5D (nivel proyecto/tenant — no es una métrica global del sistema)` en ambos lugares donde se genera (`operational-context.service.ts:formatContextBlock`, `prometeo-orchestrator.service.ts:buildOperationalReport`); (2) `buildSystemPrompt()` (usado por todos los agentes/personas, no solo Prometeo) ahora incluye una regla explícita: si el modelo cita una dimensión del Lente Ecosistema 5D, debe usar su nombre real tal cual aparece en el contexto y aclarar que es una métrica de ese proyecto/tenant específico, nunca inventar un nombre como "confianza del sistema" ni presentarla como global de la plataforma. El bug de "cero trabajos activos" reportado en el mismo hallazgo es la causa raíz **0.0** (JobStatus casing) por una tercera vía no confirmada explícitamente antes — su fix centralizado ya debería cubrir cualquier ruta que Prometeo use para contar jobs, pero **no se verificó en vivo específicamente contra el chat de Prometeo** en esta pasada.
  - **Estado:** [x] Corregido (2026-07-24) — parcial: la causa del nombre/alcance inventado está corregida a nivel de prompt; **pendiente verificación en vivo** de que (a) el chat ya no dice "nivel de confianza del sistema" sino algo como "Trust: 26/100 (crítico) a nivel de este proyecto", y (b) el chat ya no responde "0 trabajos activos" para una cuenta con jobs `ACCEPTED` reales (cubierto en teoría por 0.0). Si en el futuro se decide construir una superficie de UI real para el Trust score, ver `ecosystem-5d.service.ts` como fuente de verdad — queda fuera de alcance de este fix.
- **1.11c ALTO** — El segundo widget flotante "Abrir Prometeo Copilot" (distinto del chat principal de Prometeo, que sí funciona) está roto: su botón de acción rápida "Preguntar a Prometeo" es un stub que solo responde `Acción "Preguntar a Prometeo" ejecutada.` sin hacer nada real; escribir una pregunta real en su chat devuelve el error crudo del backend `Authentication required for SEMSE API route`, mostrado tal cual al usuario. [x] Corregido — parcial (2026-07-27):
  - **Error crudo de sesión, corregido:** `unwrap()` en `lib/bff/prometeo.ts` (compartido por las 6 llamadas del módulo: orchestrate, consultAgent, detectCopilotContext, sendCopilotMessage, executeCopilotAction, createMissionFromCopilot) ahora detecta `res.status === 401` — que **siempre** es el body genérico de sesión expirada que arma `buildSemseApiUnauthorizedBody()` (`lib/semse-api-auth.ts`, devuelto por el propio `middleware.ts` para cualquier ruta `/api/semse/*` sin sesión válida, no un error específico de Prometeo — confirmado leyendo el origen exacto del string) — y lanza un mensaje en español, accionable ("Tu sesión expiró. Actualiza la página e inicia sesión de nuevo..."), en vez de reenviar el string crudo en inglés. Se corrigió en el punto compartido, no solo en `PrometeoCopilot.tsx`, así que cubre los 16 agentes/Prometeo consumidos por este módulo, no solo el chat de este widget.
  - **Botón de acción rápida "stub", NO corregido a propósito — evaluado y diferido.** Investigado hasta la causa raíz: `PrometeoCopilotService.executeAction()` (`apps/api/.../prometeo-copilot/prometeo-copilot.service.ts:229-266`) trata **6 tipos de acción** (`job.summary`, `project.status`, `status.summary`, `listing.compare`, `copilot.ask`, `workspace.focus`) como "inline" y para los 6 devuelve un `summary` **100% fabricado** (`Acción "${action}" ejecutada sobre ${resourceType} ${resourceId}.`) sin consultar ningún dato real — no es que "Preguntar a Prometeo" sea el único roto, es que ninguna de las 6 acciones inline hace nada real todavía; el frontend además ni siquiera usa ese `summary` fabricado, escribe su propio string aparte (`PrometeoCopilot.tsx:99`). Además, el label "Preguntar a Prometeo" no está hardcodeado en ningún lado del frontend — viene de `context.suggestedActions`, generado dinámicamente por el backend, así que el catálogo real de textos que un usuario puede ver varía. Hacerlo real requiere implementar 6 integraciones de datos distintas (una por tipo de acción) contra los módulos correspondientes (jobs, projects, listings) — es trabajo de feature nuevo, no un fix de una línea, mismo criterio que **2.28**. Se deja señalado aquí para que una sesión futura lo aborde con alcance de producto claro (¿cuáles de las 6 acciones vale la pena hacer reales primero?), no a ciegas.
  - `tsc --noEmit`/`eslint` limpios en `@semse/web` para `lib/bff/prometeo.ts`.
- **1.12 ALTO — patrón repetido, CAUSA RAÍZ CONFIRMADA 2026-07-21, y es peor de lo que parecía** — Breadcrumb duplicado "Dashboard > Dashboard" en `/client/professionals` **y también en `/worker/agenda`** (confirmado en vivo el 2026-07-20). Causa exacta: `apps/web/app/components/client/ClientBreadcrumbs.tsx:12-21` — el componente compartido siempre antepone su propio crumb `{ label: "Dashboard", href: CLIENT_ROUTES.dashboard }` (`includeDashboard` por defecto `true`, nunca sobreescrito fuera de `/client/*`), y `CLIENT_ROUTES.dashboard` está hardcodeado a `"/client/dashboard"` (`apps/web/app/lib/client-routes.ts:13`). `worker/agenda/page.tsx:182-186` ya pasa su propio crumb "Dashboard" → `/worker/dashboard`, así que el componente termina agregando un segundo "Dashboard" **que además apunta a la ruta equivocada** (`/client/dashboard`, no `/worker/dashboard`). No es solo un duplicado visual: **un PRO que hace clic en el primer breadcrumb "Dashboard" es enviado al dashboard del rol Cliente** — bug de navegación cross-rol, no solo cosmético. Sube de MEDIO a ALTO por esto. [x] Corregido — `ClientBreadcrumbs` ahora detecta si el primer `items` ya es "Dashboard" y no lo antepone, conservando el `href` que la página le pasa. Esto cubre `/client/professionals` y `/worker/agenda` sin tocar cada página individualmente. Verificación en vivo confirmada (2026-08-01): `/worker/agenda` como `worker@demo.semse` muestra el breadcrumb "Dashboard › Agenda" — un solo crumb "Dashboard", apuntando a `/worker/dashboard` (correcto), no el duplicado "Dashboard › Dashboard › Agenda" con destino cruzado documentado originalmente.
- **1.13 MEDIO** — Nombre interno del algoritmo ("matching Jaccard + trust") expuesto en UI de cliente. [x] Corregido — en `/client/professionals` se reemplazó el subtítulo por "Encuentra profesionales compatibles con tu trabajo", el badge por "Matching SEMSE", el ordenamiento por "Ordenados por confianza" y la etiqueta del puntaje por "confianza".
- **1.14 MEDIO** — Wizard de "Publicar trabajo" pierde el 100% del progreso al refrescar, sin guardado de borrador (`client/jobs/new`). [x] Corregido — se agregó persistencia local del borrador en `localStorage` (`semse-job-wizard-draft`) con helpers en `lib/job-intake.ts`; el wizard restaura paso, categoría, subcategoría, título, descripción, ubicación, presupuesto, urgencia y fecha límite al recargar (cuando no hay un `intakeId` o prefill de URL), guarda con debounce de 500 ms y limpia el borrador tras publicar exitosamente. Se muestra un banner verde cuando se recupera. Verificación en vivo confirmada (2026-08-01): login como `client@demo.semse`, `/client/jobs/new`, se seleccionó la categoría "Plomería" (sin completar más pasos) y se recargó la página — el borrador persistió en `localStorage` (`categoryId:"plomeria"`) y al recargar apareció el banner verde "Recuperamos tu borrador guardado..." con "Plomería" pre-seleccionada.

### Sistema de diseño / consistencia (código)
- **1.15 ALTO** — Dos librerías de componentes paralelas sin solaparse (`@semse/ui` vs `apps/web/components/ui/`), 22 archivos reinventan su propio spinner. [x] **Parcialmente resuelto (2026-07-27, PR TBD).** Investigación previa (leer antes de repetir el análisis): `@semse/ui` (`packages/ui/src`) y `apps/web/components/ui/` en realidad **no compiten por el mismo contenido** — el primero son componentes compuestos de feature (AgentChatPanel, JobCard, AppShell...), el segundo son átomos de diseño (button, badge, spinner...). No hay dos implementaciones del mismo componente en ambas libraries; "consolidar" en el sentido de fusionar ambos paquetes en uno no aplica tal como está redactado el finding. El síntoma concreto y real que sí encontramos: 18 archivos definían su propio bloque `<style>{`@keyframes spin {...}`}</style>` de forma idéntica en vez de usar la utilidad `animate-spin` de Tailwind — que ya compila ese mismo keyframe globalmente (confirmado en `.next/static/css/*.css`: `@keyframes spin{to{transform:rotate(1turn)}}`, usado por `apps/web/components/ui/spinner.tsx` y `button.tsx`). Se eliminaron esos 18 bloques `<style>` redundantes (código muerto, cero cambio visual — el nombre de keyframe ya resolvía globalmente antes y después). Verificado con `tsc --noEmit`, `eslint`, `next build`, y en vivo en dev local (admin logueado): se confirmó por `getComputedStyle` que un elemento con `animation: spin ...` en `admin/consciousness` (que usa `SimulationPanel`, uno de los 18 archivos tocados) sigue rotando correctamente tras el cambio. Excluido a propósito de este pase: `admin/semse-x/page.tsx` (usa `spin-slow`/`fade-in`, animaciones custom reales, no duplicados) y `admin/agents/page.tsx` conservó su `@keyframes pulse` local (tiene una curva de opacidad distinta a la utilidad `animate-pulse` de Tailwind — no es un duplicado exacto). Pendiente como pieza separada, de mayor riesgo (no abordada en este pase): convertir los ~56 sitios que usan `style={{ animation: "spin..." }}` inline a `className="animate-spin"`, y la pregunta de fondo de si `@semse/ui` y `apps/web/components/ui` deberían fusionarse en un solo paquete (dado que no se solapan en contenido, el valor de hacerlo no está claro sin una decisión de producto/arquitectura explícita).
- **1.16 MEDIO** — 3,306 colores hardcodeados que duplican tokens ya existentes (`#10b981`×290, `#3b82f6`×190, `#ef4444`×232). [ ] Pendiente — **evaluado (Crew F, 2026-07-21) y diferido a propósito.** Mismo motivo que 1.15: un find/replace masivo de 3,306 instancias sin QA visual humano corre el riesgo real de introducir diferencias sutiles de color (algunos hex podrían ser semánticamente distintos del token más parecido, no solo un duplicado accidental) en toda la app de una sola vez. Requiere migración a tokens de diseño con verificación visual, no un sweep automatizado.
  - **Re-evaluado 2026-07-30.** El conteo de "3,306" está desactualizado — hoy `apps/web` tiene ~2,900 colores hex en total, y **los 3 colores nombrados explícitamente en el hallazgo original ya se resolvieron** (PR #463, "tokenize top 3 hardcoded hex colors") — solo quedan residuos legítimos (SVG, `manifest.ts`, la definición del token en `globals.css`). Se investigó si había otro subconjunto igual de seguro para tokenizar (8 colores más que coinciden carácter por carácter con un token existente, 586 apariciones) y **no lo hay**: una porción significativa de esas 586 (140 confirmadas) ya es el patrón `var(--token, #hexFallback)` — el uso *correcto* del token con un fallback CSS explícito, no un hardcode a corregir; convertirlo le quitaría el fallback de seguridad. El resto aparece mezclado en tablas de lookup de JS (`riskColors[level] ?? "#hex"`) donde no se puede confirmar sin abrir cada archivo si el valor solo se consume como CSS (seguro de tokenizar) o se pasa a un contexto no-CSS como canvas/SVG (donde un `var()` no serviría). **Conclusión: la parte segura de este hallazgo ya está hecha; lo que queda genuinamente necesita revisión archivo por archivo, tal como ya advertía el hallazgo original — sigue diferido, no por falta de intento sino porque se confirmó que no hay atajo mecánico seguro restante.**
- **1.17 MEDIO** — Shell de navegación duplicado: `AppShell` (desktop, Tailwind) vs `Sidebar` (mobile, estilos inline) para el mismo dato de nav. `apps/web/app/(app)/layout.tsx`:145-304. [x] Corregido — parcial, ver desglose (2026-07-31) — **re-evaluado (2026-07-28) y diferido a propósito, con hallazgo más preciso que el original.** A diferencia de 1.15/1.16, acá no hay una porción chica y mecánicamente segura para arreglar sin tocar el sistema real: la duplicación no es código muerto, es la lógica de renderizado en sí. Investigación (`apps/web/app/(app)/layout.tsx`, leído completo):
  - `Sidebar` (mobile-only, se monta solo cuando `mobileOpen`) lee `NAV[role]` directo y arma su propio JSX + su propia lógica de section-header inline (líneas ~262-281).
  - `AppShell` (desktop-only, oculto por CSS media query en mobile) recibe datos vía `buildShellNavItems()`, pero `AppLayoutInner` vuelve a implementar el renderizado de ítems **dos veces más**, inline, en dos ramas separadas: una para admin (datos ya agrupados) y otra para worker/client (lista plana) — cada una con su propio JSX y, en el caso no-admin, la misma lógica de section-header duplicada de nuevo (líneas ~512-513).
  - O sea: 3 renderers paralelos para el mismo dato de nav, viviendo dentro del layout raíz del que depende el 100% del tráfico autenticado (los 3 roles, ambos breakpoints).
  - Único extract seguro que encontramos (deduplicar el ternario `showSectionHeader` entre `Sidebar` y la rama desktop no-admin) es cosmético y no resuelve lo que el audit señaló de fondo — se descartó por no valer la pena solo.
  - Decisión del usuario (2026-07-28, ver AskUserQuestion): documentar y diferir en vez de intentar el refactor real ahora, dado que no hay testing de regresión visual automatizado disponible — solo spot-checks manuales — y el radio de impacto de un error acá es el navbar completo para todos los roles.
  - **Re-evaluado 2026-07-30 (usuario aceptó explícitamente avanzar sin QA visual disponible).** Investigación más profunda reveló que la divergencia es más grave de lo documentado: **Admin agrupa su nav de forma distinta en mobile vs. desktop.** En mobile, `Sidebar` agrupa por el campo `section` declarado en `NAV.admin.items` (`section.core`, `section.modules`, `section.verticals`, `section.quick` — 4 grupos). En desktop, la rama admin de `AppLayoutInner` usa `buildAdminSidebarGroups()`/`adminGroupForHref()` (`lib/navigation-shell.ts`), que clasifica por `href` contra una tabla completamente aparte — **6 grupos con otros nombres** (`os.missionControl`, `os.operations`, `os.marketplace`, `os.governance`, `os.ai`, `os.system`). No es solo JSX duplicado: son dos taxonomías de agrupación independientes para el mismo rol, así que un admin ve categorías distintas según el breakpoint. Nunca antes documentado explícitamente.
  - **Hecho ahora (bajo riesgo, cero cambio visual):** se extrajo `isNewNavSection()` a `lib/navigation-shell.ts` — la única lógica realmente compartida entre `Sidebar` (mobile, los 3 roles) y la rama desktop no-admin (worker/client): la pregunta pura "¿este índice inicia una sección nueva según el campo `section`?". Los dos call sites ahora llaman la misma función en vez de reimplementar el mismo ternario. Verificado por trazado lógico exhaustivo (no solo lectura) que el booleano resultante es idéntico al original en cada índice, para ambos call sites — cero cambio de comportamiento. No toca la rama admin (que nunca usó esta lógica, al agrupar distinto). `tsc --noEmit`/`eslint` limpios.
  - **Seguirá diferido:** unificar los 3 renderers JSX en uno (el objetivo real del hallazgo, en el sentido de un solo bloque de markup) sigue sin hacerse — el header de sección de admin en desktop ya usa un tamaño/padding distinto del de worker/client desktop (`text-[11px] ... pt-2` vs `text-[10px] ... pt-3 pb-1`), una inconsistencia visual preexistente que una fusión ingenua de los 2 estilos (inline vs Tailwind) heredaría o "arreglaría" de forma visible en una de las dos ramas — no hay forma de saber cuál es la intencional sin verlo en pantalla. Sigue diferido por el mismo motivo que 2026-07-30.
  - **Corregido ahora (2026-07-31): la divergencia de comportamiento real, no solo el JSX duplicado.** El hallazgo del 07-30 (Admin ve 2 taxonomías distintas — 4 secciones hardcodeadas en mobile vs. 6 grupos por `adminGroupForHref` en desktop) sí se resolvió: `Sidebar` (mobile) para `role === "admin"` ahora llama `buildAdminSidebarGroups(nav.items)` — la misma función que ya usaba la rama desktop — en vez de agrupar por un campo `section` hardcodeado propio. Se quitaron los 4 marcadores `section: "section.core"|"section.modules"|"section.verticals"|"section.quick"` de `NAV.admin.items` (quedaban muertos: nada más los leía). Un admin ve ahora los mismos 6 grupos (`os.missionControl`/`os.operations`/`os.marketplace`/`os.governance`/`os.ai`/`os.system`) con los mismos labels en mobile y desktop — la inconsistencia de comportamiento real (no solo estética) que motivó subir esto de prioridad en el re-evaluado del 07-30 queda cerrada. **Deliberadamente no tocado:** worker/client (que nunca tuvieron el problema de taxonomía — ambos ya usaban el mismo campo `section` en mobile y desktop vía `isNewNavSection` compartida desde el fix del 07-30) y la diferencia de estilo inline-vs-Tailwind entre los 3 renderers JSX, que sigue siendo la pieza de mayor riesgo sin QA visual. Se extrajeron `SidebarSectionHeader`/`SidebarNavLink` dentro de `Sidebar` (mismos estilos inline exactos, cero cambio visual para worker/client) solo para no duplicar el JSX entre la rama admin y la rama no-admin de este mismo componente — no toca ni fusiona con la rama desktop. `tsc --noEmit`/`eslint` limpios en `@semse/web`. Sin test de componente (no hay convención de tests React en este repo, ver nota ya repetida en varios hallazgos de esta sección).
  - **Verificación visual en vivo (2026-07-31): CONFIRMADO.** A diferencia de las sesiones anteriores de este documento, esta sí tuvo navegador (Chromium/Playwright preinstalado) — se levantó Postgres/Redis locales, se corrieron las migraciones + seed (`admin@demo.semse`/`demo1234`), y se arrancaron `apps/api`+`apps/web` en local (no contra producción — no había credencial OPS_ADMIN de producción esta sesión tampoco). Con Playwright: login como admin en viewport desktop (1440px) capturó los 6 headers de grupo del `AppShell`; login como admin en viewport mobile (390px, iPhone) abrió el `Sidebar` vía el botón hamburguesa y capturó sus headers de grupo (filtrando por `:visible` para excluir el `AppShell` de escritorio, que sigue en el DOM pero oculto por media query a ese ancho). **Resultado: ambos arrays son idénticos y en el mismo orden** — `["Mission Control","Operations OS","Marketplace OS","Governance OS","AI Mission Control","System"]` — confirmado también por captura de pantalla de ambos. Cierra el ítem pendiente de verificación visual que quedó abierto en el corte anterior de este mismo hallazgo.
  - **Nota (2026-08-02) — cambio de contenido, no del renderer diferido.** El kit `.semse-sdd` (Epic A1 de su backlog) pedía que `apps/web/lib/admin/admin-navigation.ts` (`ADMIN_MODULES`) fuera la única fuente de verdad del sidebar de Admin; hasta ahora sólo alimentaba las páginas hub, y `NAV.admin.items` seguía siendo una lista de 18 rutas curada a mano por separado, agrupada por los 6 grupos `os.*` descritos arriba. Se reemplazó `NAV.admin.items` por una lista plana derivada de `ADMIN_MODULES` (9 módulos + 5 quick-links para rutas que `ADMIN_MODULES` no cubre: `/admin/account`, `/agro`, `/buildops`, `/tools`, `/agents`) y se retiraron `buildAdminSidebarGroups()`/`adminGroupForHref()`/`ADMIN_GROUP_ORDER` de `navigation-shell.ts` — ya no tienen caller. Las rutas legacy que antes eran links directos de 1 clic (Jobs, Users, Contractors, Change Orders, Disputes, etc.) siguen disponibles, ahora como cards de 2 clics en la página hub de su módulo (Epic A3, ya construido antes de este cambio). Esto **reduce** la duplicación de renderers que este hallazgo documenta — Admin deja de tener su propio branch agrupado en `Sidebar`/`AppLayoutInner` y pasa a compartir el mismo renderer plano por `section` que ya usan worker/client — sin intentar la fusión de los 2 renderers JSX restantes (inline `Sidebar` vs. `AppShell` Tailwind), que **sigue diferida sin cambios**, por el mismo motivo de riesgo (navbar de los 3 roles, sin QA visual automatizada) ya documentado arriba. Ver `docs/specs/ui/admin-modular-navigation.spec.md`.
- **1.18 MEDIO** — 24 `<div onClick>` sin `role`/`tabIndex`; solo 22 archivos usan `aria-label` en toda la app. [x] Investigado (Crew F, 2026-07-21) — el hallazgo original (conteo por grep) resultó ser en su mayoría falso positivo: 22 de los 24 son overlays/paneles de modal (`className="modal-overlay"`/`"modal-panel"`) cuyo `onClick` solo cierra el modal al hacer click fuera o detiene la propagación — no son controles interactivos que un usuario de teclado deba poder enfocar, y agregarles `role="button" tabIndex={0}` habría sido un anti-patrón (un backdrop de pantalla completa no debe ser foco-navegable). El único caso real — `admin/browser-agent/page.tsx:325`, un div con `cursor-pointer`/`hover:underline` que abre una URL, funcionando como link sin serlo — se corrigió reemplazándolo por un `<a href target="_blank" rel="noopener noreferrer">` real (accesible por teclado de forma nativa, y cierra de paso el mismo hueco de reverse tabnabbing que 2.48). El resto de la app usa control estándar (`<button>`) para acciones reales, confirmado al revisar cada ocurrencia de `<div onClick`. No se investigó por separado el conteo de `aria-label` (22 archivos) — no hay un hallazgo accionable único detrás de esa cifra sin auditar caso por caso qué elemento necesitaría la etiqueta.
- **1.19 MEDIO** — Dos sistemas de notificación: `NotificationBanner` (46 archivos) vs toasts a mano en `client/finance/page.tsx:199-210`. [ ] Pendiente — **evaluado (Crew F, 2026-07-21) y diferido a propósito.** Consolidar en un solo sistema de notificaciones toca potencialmente 46+ archivos; es una migración de patrón UI, no un fix puntual, y conlleva el mismo riesgo de regresión silenciosa sin QA visual que 1.15/1.16/1.17.
- **1.20 BAJO** — `BookingCard`/`AgentBubble` exportados desde `packages/ui`, cero referencias en `apps/web` — código muerto. [x] Corregido (Crew F, 2026-07-21) — confirmado cero referencias en todo el monorepo (no solo `apps/web`); se eliminaron ambos componentes (`packages/ui/src/components/{BookingCard,AgentBubble}.tsx`) y sus exports en `packages/ui/src/index.ts`. `@semse/ui`/`@semse/web` typecheck limpio tras el borrado.

### Marca / identidad (confirmado en vivo)
- **1.21 ALTO** — Landing dice "SEMSE Project" (tema claro), app autenticada dice "SEMSE OS" (tema oscuro) — dos identidades de marca, logos distintos. [x] Corregido (2026-07-27) — **decisión de producto explícita del usuario: "SEMSE Project" gana en toda la superficie.** Se ubicaron y reemplazaron **todas** las menciones visibles/generadas por IA del nombre "SEMSE OS" (y la variante "SEMSE-OS") en `apps/web` y en las 6 fuentes del lado backend que producen texto que un usuario real llega a ver:
  - **Pantallas de auth (el caso más visible del hallazgo):** el wordmark grande de `login/page.tsx`, `register/page.tsx`, `forgot-password/page.tsx`, `reset-password/page.tsx` — las 4 pantallas de tema oscuro previas a entrar a la app — pasan de "SEMSE OS" a "SEMSE Project".
  - **UI autenticada:** subtítulos/headers en `admin/ecosystem`, `admin/mission-control`, `admin/settings` (incluida la fila "Versión del sistema"), `admin/html-in-canvas` (texto de una demo), `admin/semse-x` (footer decorativo, variante "SEMSE-OS"), `agents/page.tsx` (subtítulo del catálogo), comentarios de código en `admin/dashboard`/`client/dashboard`.
  - **Documentos generados:** el pie de página de **todo PDF generado por el backend** (`build-pdf.ts`, "Generated by SEMSE OS" → "SEMSE Project") y el footer de la vista de factura (`client/finance/invoices/[id]/page.tsx`, ya cubierto también del lado cliente).
  - **Lo que dice la IA de cara al usuario — la parte más fácil de pasar por alto:** los system prompts de Prometeo (`prometeo-orchestrator.service.ts`, `prometeo.controller.ts`, `buildops.controller.ts`), de Cronos (`labor-chat.service.ts`), y el bloque de contexto operativo (`operational-context.service.ts`) le decían literalmente al modelo "eres el asistente de SEMSE OS" — sin este fix, cualquier usuario que le preguntara a Prometeo "¿qué plataforma es esta?" habría reintroducido la dualidad de marca en la propia respuesta del chat. También el mock de fallback sin backend configurado (`api/semse/cortex/chat/route.ts`).
  - `workspace/page.tsx` (`<title>` de la pestaña del navegador) y el comentario de `globals.css`.
  - **Deliberadamente fuera de alcance, documentado para no repetir la búsqueda a ciegas después:** `apps/angular` y `apps/assistant-portal` (dos apps separadas en el monorepo, ninguna de las dos aparece en el `CLAUDE.md` como servicio desplegado en Railway — a diferencia de `apps/web`, no se confirmó que sean parte del producto en vivo; tocar su branding sin esa confirmación es riesgo innecesario). Tampoco se tocó el subsistema interno `admin/consciousness` (`consciousness.service.ts`/`consciousness.types.ts`/su test) — ahí "SEMSE OS" es un identificador propio de esa feature de introspección/auto-referencia (ya marcada como métricas mayormente sintéticas en 3.29), no una superficie de marca de cara al cliente. Igual se dejaron intactos comentarios internos sin audiencia de usuario (`packages/db/prisma/seed.ts`, `packages/schemas/src/domain-events.schema.ts`, prompts del curador en `apps/worker`, los `.md` de skills de `apps/api/skills/`).
  - `tsc --noEmit`/`eslint` limpios en `@semse/api` y `@semse/web`. Suite completa de `@semse/api`: 1972/1972 tests en verde (cero test dependía de los strings de marca que se tocaron — sí existe un test que verifica el identificador interno `"SEMSE OS"` de `consciousness.types.ts`, que a propósito no se tocó). Pendiente verificación visual en vivo de las 4 pantallas de auth + el footer de PDF generado.
  - **Hallazgo nuevo (2026-07-29, pasada de re-verificación por código):** quedó una instancia no documentada de "SEMSE OS" en `apps/api/src/modules/ops/ops.controller.ts:159` — `systemPrompt: "You are a SEMSE OS health check assistant."` dentro del endpoint admin-only `POST ai-mission-control/ollama/test`. Impacto bajo: ese texto nunca se muestra a un usuario (la respuesta solo expone `provider`/`model`/`latencyMs`); no es una regresión de ningún merge posterior, es un caso que el pase original de búsqueda no cubrió. [x] Corregido (PR #475, 2026-07-30) — cambiado a "You are a SEMSE Project health check assistant."

**Re-verificación de código (2026-07-29):** tras los merges grandes de fines de julio (PR #470 agro offline, #471 sidebar Labor Engine, #472 Project Lifecycle Projection), se re-leyó el código actual de 1.5, 1.6, 1.8, 1.9, 1.10, 1.11c, 1.19, 1.21 — los 8 siguen con el comportamiento descrito, sin regresión introducida por esos merges. La parte de verificación visual en pantalla (temas, padding en mobile, etc.) sigue **sin confirmar en vivo** — no se pudo usar el navegador esta sesión (extensión Claude in Chrome inestable); queda pendiente para una sesión con navegador funcional.

---

## Sección 2 — Módulo Worker / Profesional (PRO)

> **Nota de nomenclatura:** el rol interno en la base de datos es `PRO`, no `WORKER`. La UI vive bajo `/worker/*` y se etiqueta a sí misma "Profesional" en el sidebar. Es la misma confusión de identidad de persona que ya documentamos del lado Cliente — aquí es al revés (una sola etiqueta de rol, dos nombres distintos usados sin criterio).

 Auditado con código + navegación en vivo el 2026-07-20, usando una cuenta profesional real (`[REDACTED_ACCOUNT_EMAIL]`, la que estaba bloqueada por el bug 0.32 — se desbloqueó a mano para poder auditar).

- **2.1 CRÍTICO — confirmado en vivo** — Dos trackers de tiempo desconectados bajo el mismo menú Worker: `/worker/tracker` (real, Labor Engine, 6 tabs correctas) vs `/worker/field-ops` → pestaña "Tracker" (legacy `FieldOpsService`/`TrackerSession`, completamente funcional — cronómetro propio con "Iniciar nueva sesión" real). `apps/web/app/(app)/worker/field-ops/page.tsx:895-1120`. [x] Corregido (2026-07-27) — **decisión de producto explícita del usuario: deprecar/ocultar el legado ahora, el Labor Engine gana.** Se removió la pestaña "Tracker" de `field-ops/page.tsx` (fuera de `TABS`, ya no se monta `TrackerTab`) y se eliminó el componente completo (~226 líneas: `TrackerTab` + las interfaces `TrackerSessionView`/`TrackerJob`/`TrackerSummary` + el helper `fmtDuration`, únicos consumidores/consumidos entre sí). Se agregó un link visible "⏱ Time Tracker" → `/worker/tracker` en el header de `/worker/field-ops`, junto al de "Movilidad y estancia" ya existente, para que un profesional que buscaba el tracker ahí tenga una salida clara en vez de que la pestaña desaparezca sin explicación. **Deliberadamente NO tocado:** el backend `FieldOpsService`/`TrackerSession` y cualquier dato histórico — solo se cerró la vía de acceso desde la UI; borrar el modelo/datos del todo es una decisión aparte que requeriría confirmar primero que nada más lo lee (ver detalle completo y el riesgo de sesiones activas huérfanas en `docs/specs/ui/pro-flows-remediation.spec.md` → G-PRO-01). `tsc --noEmit`/`eslint` limpios en `@semse/web`. **Pendiente antes de desplegar:** revisar si hay alguna `TrackerSession` con `status: RUNNING`/`PAUSED` real en producción — quedaría huérfana (sin UI para pausar/detener) tras este cambio.
- **2.1b CRÍTICO — confirmado en vivo, CAUSA RAÍZ CONFIRMADA (ver 0.0)** — El mismo trabajo (`cmqvqsol40023k101j9fh81mq`) que el cliente ve `Aceptado`, el profesional asignado lo ve `Publicado` en `/worker/jobs/[id]`. Causa exacta: `worker/jobs/[jobId]/page.tsx:150-151` — la variable `normalizedStatus` nunca aplica `.toLowerCase()`, así que `JOB_STATUS_META["ACCEPTED"]` no matchea nada y cae al fallback `posted`. Mismo bug de fondo que 1.4/0.0, en un archivo distinto. [x] Corregido (Crew A, 2026-07-21) — fix en 0.0. Pendiente verificación en vivo.
- **2.1c ALTO — confirmado en vivo** — Esta cuenta profesional real no tiene cuenta Stripe Connect conectada (`/worker/payments` → "Método de cobro": "Sin cuenta conectada"). Conecta directo con **0.16**: si el escrow de su trabajo se liberara hoy, el pago caería en la cuenta legacy compartida en vez de llegar a este profesional. Ya no es un riesgo teórico — es el estado real de una cuenta real hoy. [ ] Pendiente — ver 0.16
- **2.1d MEDIO — confirmado en vivo** — El perfil del profesional muestra "Trust 0%" sin ningún contexto ni explicación (`/worker/profile`). Efecto directo del sesgo de cold-start del algoritmo de matching (ver 0.28) sobre una persona real. [x] Corregido (2026-07-27) — **alcance distinto de 0.28 a propósito:** 0.28 corrigió el algoritmo de matching (prior neutral de 0.5 en `matching.repository.ts`, solo para el candidato genuinamente cold-start), pero nunca tocó el valor crudo que `UserView.trustScore` expone en `/worker/profile` — un profesional nuevo real sigue viendo "Trust 0%" en su propio perfil, indistinguible de alguien con historial real y un puntaje bajo genuino. Como `UserView` no expone `completedJobs`, se usó una señal ya disponible en esta misma pantalla (`ratings.length === 0`, ya cargado via `fetchRatings` para la sección de reseñas) para distinguir los dos casos: cuando `trustScore === 0 && ratings.length === 0`, el badge pasa de "Trust 0%" a "Trust — nuevo en la plataforma" con un `title` (tooltip nativo) explicando que el puntaje sube con trabajos completados y buenas reseñas; para cualquier otro valor, el badge sigue mostrando el porcentaje real, con un tooltip explicando qué lo mueve (sube con trabajos/reseñas, baja con disputas/cancelaciones). No se agregó ningún campo nuevo al backend/API — todo el fix es de presentación sobre datos que la pantalla ya tenía. `tsc --noEmit`/`eslint` limpios en `@semse/web`. Pendiente verificación en vivo con esta cuenta real.
- **2.1e CRÍTICO — confirmado en vivo, causa raíz confirmada (ver 0.33)** — El chat de Prometeo y los 16 agentes especializados (widget flotante, siempre visible en toda la UI de `/worker/*`) no funcionan en absoluto para esta cuenta: cualquier mensaje devuelve `⚠ Insufficient permissions` crudo. Probado con Prometeo y con Felix, 100% reproducible. Causa: el rol `PRO` no tiene el permiso `agents:run:create` que exige el endpoint de chat. [x] Corregido — ver **0.33** (mismo fix, `agents:run:create` agregado a `PRO`/`WORKER` en `rbac.ts`; este ítem quedó sin reconciliar como checkbox propio hasta 2026-07-27). Verificación en vivo confirmada (2026-07-31/08-01) con una cuenta demo `PRO` — ver el detalle en 0.33.
- **2.1f CRÍTICO — confirmado en vivo subiendo un archivo real, causa raíz confirmada (ver 0.34)** — `/worker/evidence`: subir una foto real completa el presign (200) pero nunca dispara el `PUT` real a storage (0 peticiones PUT en toda la sesión de red) y el registro de evidencia falla (400). Causa: `handleUpload` en `worker/evidence/page.tsx:120-154` nunca usa `plan.uploadUrl` ni `plan.key` en la rama `single_put` (el caso normal). Mismo patrón confirmado por código en `/worker/travel/[travelId]` (comprobantes). [x] Corregido — parcial, ver **0.34** (mismo fix: rama `single_put`, el caso normal de esta cuenta real, ya sube el archivo de verdad; la rama `external_transfer` para archivos >25MB sigue sin implementar y falla con un mensaje claro en vez de simular éxito — este ítem quedó sin reconciliar como checkbox propio hasta 2026-07-27). Pendiente verificación en vivo con esta cuenta real.
- **2.2 ALTO** — `fetchActiveTimer()` sin `.catch()` en el `Promise.all` de carga — un solo request inestable tumba todo el tracker. `apps/web/app/(app)/worker/tracker/page.tsx` — loadTracker:341-347. [x] Corregido (2026-07-23) — `fetchActiveTimer()` ya no es una llamada "pelada" dentro del `Promise.all`: ahora se envuelve en `.then/.catch` propio que resuelve a `{ ok: true, value }` o `{ ok: false, error }`, así que su fallo nunca rechaza el `Promise.all` completo — jobs/proyectos libres/resúmenes semanal/mensual se siguen cargando con normalidad aunque este único endpoint falle. Importante: en el `catch` NO se asume "sin timer activo" (eso habría podido pisar una sesión real en curso, o una recién recuperada de `trackerLocalStore` al montar) — se conserva el `activeEntry` que ya había en memoria y se muestra un aviso (`syncNotice`) indicando que no se pudo confirmar el estado del timer contra el servidor. `pnpm typecheck`/`pnpm lint` limpios; sin test unitario de frontend para este archivo (no existe suite Jest/RTL para `worker/tracker/page.tsx` en el repo).
- **2.3 ALTO** — Registros/Resumen/Reportes no leen `trackerLocalStore` — totales se subcuentan en silencio mientras el trabajador está offline. [x] Corregido (2026-07-23) — se agregó `pendingLocalEntries(state, now)` en `trackerUi.tsx`, que convierte la sesión activa aún no confirmada por el backend y las entradas manuales encoladas offline (`trackerLocalStore`) en pseudo-`TimeEntryView` con un nuevo status `"pending_sync"` (agregado a la unión `TimeEntryView["status"]` en `labor-api.ts`). Las tres pestañas ahora leen `readTrackerLocalState(window.localStorage)` al montar y mezclan ese trabajo pendiente en sus agregaciones: `ResumenTab` (KPIs "Esta semana"/"Este mes"/"Horas hoy", costo estimado, horas por propósito/proyecto, últimos registros — con badge "Pendiente de sincronizar" visible en cada fila pendiente vía el nuevo componente `PendingSyncBadge`), `RegistrosTab` (totales del filtro, barras por propósito, listado, CSV — `STATUS_LABELS` ya incluye `pending_sync: "Pendiente de sincronizar"`) y `ReportesTab` (KPIs de la semana seleccionada, gráficos por propósito/proyecto, CSV). No se tocó `weekly.byDay`/`monthly.byDay` (los gráficos de tendencia por día siguen siendo 100% backend) — solo los totales/listas que ya se computaban client-side sobre `entries`. `pnpm typecheck`/`pnpm lint` limpios.
- **2.4 MEDIO** — Timer tab no es responsive como sus hermanas (grid fijo vs. `auto-fit/minmax`). `page.tsx:1269,1276,1336`. [x] Corregido (2026-07-23) — los 4 grids de rejilla fija del Timer tab (`repeat(4, 1fr)` en las métricas semana/mes/días/liberado, `1fr 1fr` en el resumen semanal + notas, `1.2fr .8fr` en el panel de job conectado + entrada manual, `repeat(3, 1fr)` en los mini-stats de escrow) ahora usan `repeat(auto-fit, minmax(Npx, 1fr))`, el mismo patrón que ya usan Resumen/Registros/Reportes/Proyectos. Los números de línea citados en el hallazgo (1269/1276/1336) ya no aplican tal cual porque el archivo se movió por los cambios de 2.2/2.15; los 4 grids fijos correspondientes sí se localizaron y corrigieron.
- **2.5 MEDIO** — Dos formularios de "Entrada manual" con offline-fallback distinto (uno lo tiene, el otro no). [x] Corregido (2026-07-23) — el formulario del Timer tab (`page.tsx handleManualSave`) sí tenía fallback offline (encola el evento en `trackerLocalStore` si `shouldPreserveLocalEvent()`); el de Registros (`RegistrosTab.tsx handleManualSave`) no lo tenía y perdía el registro en silencio si fallaba por conexión. Se centralizaron `isLikelyConnectionError`/`shouldPreserveLocalEvent`/`friendlyConnectionMessage` (antes duplicadas solo en `page.tsx`) como exports de `trackerUi.tsx`, y se les dio el mismo comportamiento a ambos formularios. Como el formulario de Registros sí captura `hourlyRate`/`currency`/`location` (el del Timer tab no), se extendió el variante `manual_session` de `TrackerPendingEvent` (`trackerLocalStore.ts`) con esos 3 campos opcionales, y el `case "manual_session"` de `syncPendingEvents` en `page.tsx` ahora los reenvía a `createManualEntry` — sin este cambio, un registro con tarifa/moneda encolado desde Registros se habría sincronizado más tarde sin esos datos. `pnpm typecheck`/`pnpm lint` limpios.
- **2.6 MEDIO** — Visual language split: `/worker/tracker` claro/CSS-var vs. `/worker/field-ops` oscuro/Tailwind — dos productos distintos bajo un nav. [ ] Pendiente — fuera de alcance de este batch (no listado entre los ítems asignados).
- **2.7 MEDIO** — `apps/web/app/field-ops/page.tsx` (top-level, huérfano) ~90% idéntico a `/worker/field-ops`, sin back-link, error handling divergente. **Detalle ampliado 2026-07-21:** también le falta por completo la pestaña Tracker (solo tiene 4 de las 5 tabs del canónico), usa extracción de error distinta (`json?.error?.message` crudo vs. `normalizeErrorMessage` en el canónico — puede mostrar `undefined`/`[object Object]`), **esconde por completo** los errores que contienen "not configured" en vez de mostrarlos (`if (message.includes("not configured")) return null;`), no tiene i18n (título hardcodeado "Field Ops"), no tiene `NotificationBanner`, y usa un wrapper de renderizado distinto (`HtmlInCanvasPanel` de `@semse/ui` vs. `div`s planos) — es una superficie visual y funcionalmente distinta, no un simple duplicado con path distinto. [x] Corregido (2026-07-23/24) — se reescribió `apps/web/app/field-ops/page.tsx` completo como un Server Component mínimo que hace `redirect("/worker/field-ops")` (`next/navigation`), eliminando las ~929 líneas de UI duplicada/peor. Se verificó primero que no hubiera ningún link/nav en vivo apuntando a este path: el único hallado (`apps/web/app/nav.tsx`, componente `Nav`) resultó ser código muerto — nada en el repo lo importa — así que no había ningún back-link real que preservar; aun así se optó por un redirect (no un 404 duro) por si hay URLs con bookmark. **Nota de coordinación:** el crew de `fix/audit-client-orphans-and-ux` reportó haber agregado un redirect equivalente a nivel de `middleware.ts` para el mismo path (ángulo cliente/UX) en su propia rama — ambos cambios son independientes y no conflictivos hoy (ramas distintas, mismo `origin/main` base), pero al fusionar ambas ramas el redirect de middleware probablemente intercepte la request antes de que el Server Component de esta rama se ejecute, dejando este archivo como redundante-pero-inofensivo (sigue siendo un redirect correcto, solo que inalcanzable). No se tocó `apps/web/app/nav.tsx` (fuera de alcance — es código muerto que no forma parte de los hallazgos asignados). `pnpm build:web` compila y genera la ruta `/field-ops` como página dinámica de tamaño mínimo (1.32 kB).
- Ver también **0.19, 0.20, 0.24** (nómina/idempotencia — comparten causa raíz con el módulo worker).

### Ronda de 5 agentes de código en paralelo sobre `apps/web/app/(app)/worker/**` (2026-07-21)

> Auditoría estática de código (sin ejecutar), la misma metodología de 5 agentes en paralelo ya aplicada al módulo Cliente. Cada agente cubrió una franja del módulo con instrucciones explícitas de no repetir hallazgos ya documentados y de profundizar en los patrones sistémicos ya confirmados (mayúsculas/minúsculas de `JobStatus`, el bug de subida `planUpload`, y el RBAC roto de `agents:run:create`). 42 hallazgos nuevos, organizados por franja.

#### Time Tracker / Labor Engine (`/worker/tracker`)

- **2.8 CRÍTICO** — Minutos de descanso negativos permiten fabricar horas pagables por encima del rango real fichado. El preview en pantalla clampa correctamente (`manualDurationSeconds`, `page.tsx:204-213`, `Math.max(0, breakMinutes)`), pero el valor crudo sin clamp es lo que realmente se envía (`page.tsx:873,885`) y el backend nunca lo valida tampoco: `labor-engine.repository.ts:74-77` solo hace `Math.max(0, ...)` sobre el resultado final, no sobre `breakMinutes` en sí — un descanso de `-30` en una sesión de 4h se guarda como `270 min` (4.5h) con `breakMinutes: -30`, invisible en la lista porque el chip de descanso solo se muestra si `breakMinutes > 0`. Explotable por cualquier PRO contra sus propias horas pagables/job-linked. [x] Corregido (PR #414, 2026-07-24) — `breakMinutes` se clampea a `Math.max(0, ...)` en el origen, tanto en `labor-engine.repository.ts:createTimeEntry` (backend, antes de calcular `duration` y de persistir) como en `page.tsx` (`manualBreakMinutes`, antes de enviar), así que el valor crudo negativo ya no puede llegar a guardarse ni mostrarse en el preview.
- **2.9 ALTO** — `jobId`/`freeProjectId` en inicio de timer y entradas manuales nunca se validan contra quien hace la llamada. `labor-engine.service.ts:65-82` (`startTimer`) y `:106-148` (`createManualEntry`) aceptan estos IDs directo del cliente sin verificar que el job esté asignado a este worker o pertenezca a su tenant — solo hay FK a nivel DB (`onDelete: SetNull`). Un PRO que conoce/adivina el `id` de cualquier job puede registrar tiempo `job_linked` pagable contra un trabajo que nunca le fue asignado. [x] Corregido (PR #414, 2026-07-24) — nuevo `LaborEngineService.assertOwnership()`, llamado desde `startTimer`/`createManualEntry` antes de crear la entrada: `LaborEngineRepository.isJobAssignedToWorker()` reutiliza la misma regla de asignación que `FieldOpsRepository.trackerJobAssignmentWhere` (bid `ACCEPTED` o reservation `ACTIVE`/`ACCEPTED` del worker o su org), y `isFreeProjectOwnedByWorker()` verifica `createdBy` del `FreeProject`. Cualquier mismatch lanza `ForbiddenException`. Cubierto por 2 tests nuevos en `labor-engine.service.test.ts` (16/16 verdes). Verificación en vivo confirmada (2026-08-01) contra el stack local: `POST /v1/labor/timer/start` con el token de `worker@demo.semse` contra `job_demo_002` (job `PUBLISHED`, nunca asignado a este worker) devolvió `403` — `"This job is not assigned to you."`; el mismo request contra `job_demo_001` (job real asignado a este worker, `IN_PROGRESS`) devolvió `201` y creó el timer con normalidad.
- **2.10 ALTO** — `hourlyRate`/`currency` en entradas manuales son 100% controlados por el cliente, sin ningún límite. `labor-engine.controller.ts:204-205` acepta cualquier número/string sin mínimo, máximo, ni verificación de signo — alimenta directo `knownCost` en `getTeamSummary` (`repository.ts:252-253`), el KPI de costo estimado que ve un supervisor en `/admin/labor-engine`. Un worker puede declararse una tarifa absurda (o negativa) y contaminar esa cifra. [x] Corregido (2026-07-27) — nuevo `LaborEngineService.assertValidRate()`, llamado desde `createManualEntry` antes de `assertOwnership`: rechaza `hourlyRate` negativo o no finito, y cualquier valor por encima de un techo de cordura (`MAX_HOURLY_RATE = 100_000`, deliberadamente alto para no rechazar una tarifa real en una moneda de alto valor nominal, pero suficiente para bloquear un `999999999` fabricado); `currency`, si se manda, debe matchear `/^[A-Z]{3}$/` (ISO 4217 de 3 letras en mayúsculas) — ambos formularios del frontend (Timer tab y Registros) ya mandan códigos válidos (`USD`/`MXN`), así que no se tocó ningún caller real. 4 tests nuevos en `labor-engine.service.test.ts` (negativo, absurdamente alto, código de moneda mal formado, y el caso válido) — 15/15 verdes. `startTimer` no acepta `hourlyRate`/`currency` en su firma, así que no necesitó el mismo chequeo. Pendiente verificación en vivo.
- **2.11 ALTO** — Las horas "Personal" (explícitamente marcadas como privadas en la UI: *"Estas horas quedan solo para tu control personal... No se asocian a jobs ni pagos"*, `page.tsx:1203,1405`) sí se filtran hacia la vista de equipo del admin. `labor-engine.repository.ts:233-263` (`getTeamSummary`, detrás de `getAdminOverview`) no filtra por `purpose` — cuenta horas personales en el costo estimado y puede disparar alertas falsas de "overtime" visibles al supervisor, contradiciendo directamente lo que la UI le prometió al worker que registró esas horas. [x] Corregido (PR #415, 2026-07-24) — `purpose: { not: "personal" }` agregado al `where` de las tres queries admin-facing en `labor-engine.repository.ts`: `getTeamSummary` (costo/overtime), `listActiveEntriesForTenant` (timers activos) y `listLongEntries` (alerta de entradas largas) — las horas personales quedan excluidas de todo lo que ve `getAdminOverview`. Verificación en vivo confirmada (2026-08-01): se creó una entrada manual `purpose:"personal"` de 8h (480 min) para `worker@demo.semse` y se consultó `GET /v1/labor/admin/overview` como `admin@demo.semse` — el equipo mostró `totalMinutes: 0` (solo contando una entrada `job_linked` de segundos de otra prueba), la entrada personal de 8h quedó completamente invisible en el costo/horas del admin.
- **2.12 ALTO** — Los filtros "7 días"/"30 días" son ventanas de calendario fijas (mes/semana actual), no ventanas móviles reales, pese a que la UI los etiqueta como "Últimos 7 días"/"Últimos 30 días" en varios lugares (`page.tsx:1507`, `RegistrosTab.tsx:203-204`). `labor-engine.service.ts:4-21` (`weekBounds`/`monthBounds`) calcula lunes-domingo / día 1-fin de mes calendario. El día 1-2 de cada mes, "Últimos 30 días" devuelve solo 1-2 días reales de datos — sub-reporta horas/costo en KPIs, CSV, tendencias, y hasta en el contexto que usa el chat de Cronos (`labor-chat.service.ts:135`, que llega a decir "sin registros en los últimos 30 días" cuando en realidad solo consultó el mes calendario actual). [x] Corregido (2026-07-23) — se optó por el fix de ventana móvil real (preferido explícitamente sobre relabeling). Se agregó `rollingWindowBounds(days)` en `labor-engine.service.ts` (`now - N días` hasta `now`, sin alinear a calendario) y se usa en `listEntries()` cuando `range: "week"|"month"` (7/30 días respectivamente) — este es el mismo método que alimenta `GET /v1/labor/entries`, consumido por los filtros "Últimos N días" de `page.tsx`/`RegistrosTab.tsx` y por el contexto del chat de Cronos (`labor-chat.service.ts` llama a `listEntries({range:"month"})`), así que el fix cubre los tres síntomas citados en el hallazgo sin tocar `labor-chat.service.ts`. **Deliberadamente NO se tocó** `weekBounds`/`monthBounds` (el cálculo calendario lunes-domingo / día 1-fin de mes) — esas funciones siguen usándose tal cual para `getWeeklySummary`/`getMonthlySummary`/`getAdminOverview`/`getTeamSummary`, que alimentan KPIs correctamente etiquetados como "Esta semana"/"Este mes"/"Semana actual" (calendario, no "últimos N días") y la navegación por `weekOffset` en Reportes — cambiar esas también habría roto la semántica de "semana calendario X" que la navegación ◀▶ de Reportes depende. Ver también 2.13, que reutiliza el mismo `weekBounds(weekOffset)` calendario a propósito. Test unitario existente (`labor-engine.service.test.ts`) no cubre `listEntries` directamente (son 14/14 verdes igual, sin relación); no hay Postgres vivo en este entorno para probar contra datos reales — verificado solo por lectura de código + `pnpm typecheck`/`lint` limpios.
- **2.13 MEDIO** — En Reportes, los gráficos/CSV por semana quedan vacíos para cualquier semana fuera del mes calendario actual, mientras el KPI card justo arriba (misma semana) sí muestra números reales — porque `weekEntries` filtra sobre `entries`, que solo se cargó con `range:"month"` una vez al montar (mismo root cause que 2.12). Sin ningún aviso visible más allá de un tooltip al pasar el mouse. [x] Corregido (2026-07-23) — se agregó soporte de `weekOffset` explícito a `GET /v1/labor/entries` (`labor-engine.controller.ts` + `labor-engine.service.listEntries`): cuando se manda, tiene prioridad sobre `range` y ancla a `weekBounds(weekOffset)` (la misma semana calendario que ya usa `fetchWeeklySummary(weekOffset)`), en vez de la ventana móvil de 2.12. `fetchLaborEntries()` (`labor-api.ts`) ahora acepta ese parámetro. `ReportesTab.tsx` ya no carga `entries` una sola vez con `range:"month"` al montar y filtra sobre eso — ahora pide `fetchLaborEntries({ weekOffset, limit: 500 })` en el mismo efecto que `fetchWeeklySummary(weekOffset)`, así que navegar semanas hacia atrás con las flechas ◀▶ trae los registros reales de esa semana específica (gráficos "Por propósito"/"Por proyecto" y el botón "CSV semana" incluidos), no solo el mes cargado al inicio. Se corrigieron también los subtítulos "(registros del mes cargado)" que ya no aplicaban. `pnpm typecheck`/`pnpm lint` limpios.
- **2.14 MEDIO** — Los totales de costo suman entradas de distintas monedas y siempre se muestran con `$` (USD por defecto), sin importar la moneda real. `fmtMoney()` (`trackerUi.tsx:61-63`) no recibe argumento de moneda en ninguna de sus 6+ llamadas. Es explotable en la práctica: el formulario inline del Timer no tiene campo de moneda y termina siempre en MXN server-side (`repository.ts:96`), mientras el formulario de Registros sí lo tiene y por defecto usa USD (`RegistrosTab.tsx:65`) — un worker que usa ambos formularios termina con entradas MXN y USD sumadas bajo un solo número etiquetado `$`. [x] Corregido (2026-07-23) — se implementó la opción (a) (agrupar por moneda, preferida en el hallazgo sobre solo pasar el argumento): nuevo `groupCostByCurrency(entries)`/`formatCostSummary(entries)` en `trackerUi.tsx`, que suman `entryCost()` agrupado por `entry.currency` real (nunca mezclan MXN+USD bajo un solo número) y formatean cada grupo con `fmtMoney(amount, currency)` — si hay una sola moneda se ve igual que antes (`"$1,234"`), si hay varias se ven todas (`"$1,234 + MX$500"`). Reemplazó los 3 KpiCard "Costo estimado" que sumaban ciego (`ResumenTab.estimatedCost`, `RegistrosTab.totals.cost`, `ReportesTab.weekCost`), todos con `fmtMoney()` sin argumento de moneda. No se cambió el default `"MXN"` server-side de `repository.ts:114` (`data.currency ?? "MXN"`) ni se agregó un selector de moneda al formulario inline del Timer tab — el agrupamiento por moneda ya resuelve el síntoma real (la cifra ya no miente sobre qué moneda es), y agregar un selector de moneda al Timer tab habría sido un cambio de UI más allá de lo mínimo necesario para este hallazgo. `pnpm typecheck`/`pnpm lint` limpios.
- **2.15 BAJO** — Código muerto/inalcanzable en el selector de trabajo del formulario manual (`page.tsx:1100-1128`) — la rama `jobs.length === 0` dentro del `<select>` nunca puede ejecutarse porque el branch externo ya maneja ese caso. [x] Corregido (2026-07-23) — se eliminó la rama `{jobs.length === 0 ? <option value="">Sin trabajos aceptados</option> : null}` dentro del `<select>`, dejando solo el `.map()` de jobs reales; el caso "sin trabajos" ya lo cubre por completo el branch externo (`jobs.length === 0 && !loading`) que renderiza el estado vacío con CTA "Buscar oportunidades". Se dejó un comentario explicando por qué el código muerto era inalcanzable, para que no se reintroduzca.
- **2.16 BAJO** — `PATCH` de proyecto libre acepta cualquier string en `status`, incluido `"converted"`, sin exigir `convertedJobId` ni pasar por el flujo dedicado `/convert` (`labor-engine.controller.ts:57-73`) — puede dejar un estado inconsistente "convertido sin job destino" que la UI renderiza igual como "Convertido a job" sin referencia real. [x] Corregido (2026-07-23) — `LaborEngineService.updateFreeProject()` ahora rechaza (`BadRequestException`) cualquier `PATCH` que intente setear `status: "converted"` directamente, indicando que debe usarse `POST /free-projects/:id/convert` (el único camino que exige `jobId` y setea `convertedJobId` de forma atómica). Se optó por rechazar la transición directa en vez de "exigir que `convertedJobId` ya esté seteado" porque el PATCH genérico ni siquiera acepta `convertedJobId` como input (ni en el controller ni en el tipo del service) — permitir la transición solo cuando ya está seteado habría sido, en la práctica, siempre-nunca alcanzable desde este endpoint, así que un rechazo explícito y honesto es más claro que una condición muerta. `pnpm typecheck`/`pnpm lint` limpios; sin test unitario dedicado a `updateFreeProject` en `labor-engine.service.test.ts` (no se agregó uno nuevo — el resto de la suite, 14/14, sigue en verde).

#### Evidencia / Incidencias / Materiales / Reseñas / Tareas

- **2.17 CRÍTICO** — La función de reseñas del profesional está 100% rota, para todos los workers, siempre. `worker/review/page.tsx:84-85,104-108` depende de `job.clientUserId`, un campo que **no existe** en ningún lado del tipo `JobRecordView`/`jobRecordSchema` (`packages/schemas/src/job.schema.ts:53-67`) ni en la función que puebla la lista (`fetchMyJobs`, `semse-api.ts:363-378`) — de ahí el cast `(j as any)`, señal de que TypeScript ya sabía que el campo no existe. Resultado: `handleSubmit` siempre corta con "No se encontró el ID del cliente para este trabajo" — ningún worker puede enviar una reseña de cliente jamás, pese a que la UI (selector de estrellas, caja de comentario, botón enviar) se ve completamente funcional. [x] Corregido (Crew E, 2026-07-21) — se identificó `Contract.clientUserId` como el campo real y correcto (el `Job` en sí solo tiene `clientOrgId`, a nivel de organización). Se propagó de punta a punta: `bids.repository.ts` (`listByWorker`, incluye `job.contract.{clientUserId, clientUser.email}`), `domain-store.ts` (`BidRecord`), la ruta BFF `app/api/semse/my-bids/route.ts` (tenía su propio whitelist `toClientBid()` que habría descartado los campos nuevos aunque backend/tipos ya estuvieran arreglados — se actualizó también), `semse-api.ts` (`MyBidView`, sin casts `as any`), y `jobRecordSchema` (`packages/schemas`). Se eliminaron los casts `(j as any).clientUserId/clientEmail` en `worker/review/page.tsx`. Pendiente verificación en vivo.
- **2.18 CRÍTICO** — La respuesta de `GET /v1/jobs/:jobId/evidence` descarta los campos `validationStatus`/`aiQualityScore` (columnas reales, alimentadas por `evidence-gateway.service.ts:142-143` y consumidas internamente por scoring/milestones) — `EvidenceRepository.toEvidenceView`/`listByProject`/`findById` (`evidence.repository.ts:222-245,264-275,350-361`) reconstruyen la respuesta con solo `{id, tenantId, projectId, jobId, milestoneId, uploadedById, kind, key, metadata, createdAt}`. El badge de estado en `/worker/evidence` queda pegado en "Pendiente" para siempre, sin importar el resultado real de la validación por IA — la promesa en pantalla ("El agente valida cobertura y calidad antes de permitir la aprobación") nunca puede cumplirse visualmente. Relacionado directo con **0.34**. [x] Corregido (Crew B, 2026-07-21) — `EvidenceView` ahora expone `validationStatus`/`aiQualityScore` reales (columnas Prisma) en `create`/`listByProject`/`findById`. Pendiente verificación en vivo.
- **2.19 ALTO (seguridad, IDOR)** — Incidencias y solicitudes de material se pueden crear y leer contra cualquier job del tenant, sin verificar asignación. `POST /v1/incidents` y `POST /v1/materials` (`incidents.controller.ts:32-41`, `materials.controller.ts:34-43`) escriben el `jobId` que manda el cliente directo a la BD, solo gateado por el permiso genérico `jobs:create` — sin chequear que el actor esté asignado a ese job. `GET .../by-job/:jobId` tiene el mismo hueco (filtra solo por tenant+jobId). Cualquier worker del tenant que conozca/adivine el `jobId` de otro puede leer sus incidencias/materiales (incluidos montos en $) e inyectar registros falsos. Contraste: `ratings` sí valida participación real y estado `COMPLETED` antes de aceptar — este patrón no se replicó aquí. [x] Corregido (Crew C, 2026-07-21) — `incidents.service.ts` y `materials.service.ts` ahora verifican, antes de leer o crear, que el actor pertenezca a la org cliente o a la org profesional asignada del job (`assertJobAccess`, mismo patrón que `evidence.policy.ts`/`change-orders.service.ts`), con bypass para `OPS_ADMIN`. **Nota importante encontrada durante el fix:** con la configuración real de `packages/auth/src/rbac.ts` (incluso después de Crew A), ni `PRO` ni `WORKER` tienen el permiso `jobs:create` que gatea `POST /v1/incidents` y `POST /v1/materials` — es decir, hoy ningún profesional puede crear una incidencia o solicitud de material en absoluto (403), lo cual contradice el marco original del hallazgo ("cualquier worker... puede leer/inyectar"). Esto parece ser un gap de RC3 (permisos) no documentado como hallazgo propio — se deja señalado aquí, no se amplió `jobs:create` a PRO/WORKER como parte de este fix (mismo riesgo que motivó el permiso dedicado `travel:manage` en vez de `jobs:create` — ver 2.31/G-PRO-10). Pendiente verificación en vivo.
- **2.20 ALTO (seguridad, IDOR)** — Cualquier worker puede cambiar el estado de la tarea de otro worker. `PATCH /v1/tasks/:taskId/status` (`tasks.controller.ts:67-80`, `tasks.service.ts:124-140`) actualiza `where: { id: input.taskId, tenantId: input.tenantId }` sin verificar `assignedTo === actor.userId` — gateado solo por el permiso amplio `jobs:update`. Un `taskId` de otro worker (visto en URL, chat, captura de pantalla) es suficiente para marcarla "hecha"/"bloqueada" desde otra cuenta. [x] Corregido (Crew C, 2026-07-21) — `tasks.service.ts` ahora hace un `findFirst` previo y verifica `assignedTo === actor.userId` (con bypass para `OPS_ADMIN`); tareas sin asignar (`assignedTo: null`) se dejan editables por cualquiera con el permiso base, para no introducir una restricción nueva donde antes no existía. Pendiente verificación en vivo.
- **2.21 MEDIO** — El link "Ver" de cada evidencia nunca se renderiza, para ninguna evidencia, ni siquiera las subidas correctamente por otras pantallas — consecuencia directa de 2.18: `previewUrl`/`url`/`signedUrl` tampoco existen en la respuesta del backend, así que `evidence/page.tsx:343` siempre calcula `previewUrl = undefined`. [x] Corregido (Crew B, 2026-07-21) — en vez de agregar `previewUrl` al backend (acoplaría la API al patrón de rutas del BFF del frontend), se calcula en el cliente a partir del `key` que ya viene en cada registro: `/api/semse/uploads/files/${encodeURIComponent(ev.key)}`. Pendiente verificación en vivo.
- **2.22 MEDIO** — El historial de evidencias muestra la key cruda de storage (p. ej. `tenants/t_abc/evidence/1755-xyz.jpg`) como "nombre de archivo", nunca el nombre original — mismo root cause, `filename`/`originalFilename` tampoco existen en la respuesta (`evidence/page.tsx:340`). [x] Corregido (Crew B, 2026-07-21) — `filename` ahora se captura al registrar (`registerEvidenceSchema` + `metadataJson`) y se expone en `EvidenceView.filename`. Requiere que las evidencias se suban de nuevo para tener nombre real; las existentes seguirán mostrando la key hasta entonces (no hay migración retroactiva de datos históricos). Pendiente verificación en vivo.
- **2.23 MEDIO** — La sección "Reseñas recibidas" de `/worker/review` es código muerto/mal etiquetado: filtra sobre `myReviews` (reseñas que el worker **dio**, no las que recibió — `review/page.tsx:94`, `given = ...filter(r => r.fromUser.id === myUserId)`), así que una sección titulada "reseñas que recibí" nunca puede mostrar una reseña recibida bajo ninguna circunstancia — queda hardcodeada al mensaje "Las reseñas recibidas se muestran en tu perfil". [x] Corregido (Crew E, 2026-07-21) — se agregó `receivedReviews` (`ratingsData.items.filter(r => r.toUser.id === myUserId)`) y se reemplazó la sección placeholder por el render real (título del job, comentario, estrellas). Pendiente verificación en vivo.
- **2.24 MEDIO** — El formulario "Solicitar material" solo valida que la cantidad no esté vacía (`materials/page.tsx:82`), no que sea positiva — el backend sí rechaza `0`/negativos (`z.number().positive()`) pero sin ningún aviso inline antes de enviar, solo un error genérico post-submit. [x] Corregido — `handleSubmit` ahora valida `qtyNum > 0` y `estimatedCost >= 0` antes de enviar, mostrando un mensaje inline (`submitError`) sin llegar al backend. Pendiente verificación en vivo.
- **2.25 BAJO** — El badge de material "Rechazado" usa una variante neutral/gris en vez de un color de error, inconsistente con el esquema de colores ya usado en Incidencias (`materials/page.tsx:28`). [x] Corregido — `STATUS_MAP.rejected` ahora usa `variant: "error"` (StatusBadge rojo), consistente con la semántica de rechazo. Pendiente verificación en vivo.

#### Trabajos / Dashboard / Perfil / Agenda / Propuestas

- **2.26 CRÍTICO** — "Oportunidades abiertas" en `/worker/dashboard` ("Mi Panel", la pantalla de aterrizaje principal) está siempre vacía, sin importar cuántos jobs reales estén publicados. `dashboard/page.tsx:155` filtra `["posted","published"].includes(job.status)`, pero `job.status` llega en **mayúsculas** porque esta pantalla usa `fetch("/api/semse/jobs")` directo (no `fetchJobs()`), cuya respuesta pasa por `toVisibleJob()` (`apps/api/src/common/visible-response.ts:71-73`), que mayúsculiza intencionalmente el status. Mismo patrón que **0.0**, pero en una ubicación nueva y más dañina: es el descubrimiento principal de trabajo del PRO, siempre mostrando cero oportunidades. [x] Corregido (Crew A, 2026-07-21) — mismo fix que 0.0 (normalización en las rutas BFF `/api/semse/jobs*`). Verificación en vivo confirmada (2026-07-31/08-01): login como `worker@demo.semse` contra el stack local (3 jobs seed en estado `PUBLISHED`), `/worker/dashboard` mostró "Oportunidades abiertas: 3" — coincide exactamente con los 3 jobs `posted` reales devueltos por `/api/semse/jobs`, ya no queda en cero.
- **2.27 ALTO (seguridad, exposición de datos cross-org)** — La misma llamada de 2.26 (`fetch("/api/semse/jobs")` sin `?status=`) trae **todos los jobs del tenant**, de **todas las organizaciones cliente**, en **cualquier estado incluido `DRAFT`** (aún sin publicar) — `JobsRepository.listByTenant` (`jobs.repository.ts:62-69`) solo filtra por `tenantId + deletedAt: null`, sin filtro de status ni de org. Aunque la UI solo *muestra* los que pasan el filtro roto de 2.26, el payload completo (títulos, presupuestos, ubicaciones de jobs privados/borrador de otros clientes) ya llegó al navegador del PRO y es inspeccionable vía devtools — un tenant puede alojar múltiples organizaciones cliente no relacionadas entre sí. [x] Corregido (2026-07-23) — se leyeron los callers reales antes de decidir el filtro (no se adivinó): `GET /v1/jobs` (`jobs.controller.ts` → `JobsService.list` → `JobsRepository.listByTenant`) es consumido por `worker/dashboard/page.tsx` (PRO, único consumidor de la lista cruda sin `?status=` — confirma el hallazgo), y también por varias pantallas `client/*` (`jobs`, `dashboard`, `protools`, `professionals`, `jobs/new`) y `admin/*`, ninguna de las cuales filtraba por org tampoco — es decir, hoy el mismo hueco también expone jobs de otras organizaciones cliente a un CLIENT normal, no solo a PRO. `listByTenant` ahora recibe `roles` y aplica scoping por rol: OPS_ADMIN sin restricción adicional (visibilidad tenant-wide es intencional); CLIENT limitado a `clientOrgId === actor.orgId`; PRO/WORKER limitado a jobs postables (`status IN (POSTED, PUBLISHED)`, nunca `DRAFT`) más los jobs donde el actor ya está comprometido (bid propio, reserva activa/aceptada, contrato, o proyecto asignado a su org — mismo criterio de "asignado" que 2.9); cualquier otro rol cae al scoping por org propia en vez de tenant-wide, por seguridad por defecto. `jobs.controller.ts`/`jobs.service.ts` actualizados para propagar `actor.roles`. **Nota de incidente (per skill RC4):** no hay logging histórico suficiente para confirmar si esta exposición ya fue explotada — cualquier PRO o CLIENT con acceso a devtools pudo, en cualquier momento antes de este fix, ver en el payload de red títulos/presupuestos/ubicaciones de jobs `DRAFT` u de otras organizaciones cliente del mismo tenant; no se puede descartar que eso ya haya ocurrido. Typecheck + lint + suite dirigida (`jobs.service.test.ts`, `jobs.controller.test.ts`, `jobs.fsm.test.ts`) en verde — no se agregó un test unitario nuevo específico del filtro por rol (requeriría mockear Prisma `where` real, que esta suite no ejercita hoy para `listByTenant`; se validó por lectura de código). Verificación en vivo confirmada (2026-07-31/08-01): login como `worker@demo.semse`, `GET /api/semse/jobs` devolvió 5 jobs con statuses `["posted","completed","posted","posted","in_progress"]` — cero `draft` en el payload, confirmando que el scoping por rol para PRO/WORKER excluye `DRAFT` como se esperaba.
- **2.28 CRÍTICO** — Los botones "Verificar" en `/worker/profile` (Documento de identidad, Antecedentes, Teléfono) siempre fallan con 403, para cualquier PRO. `profile/page.tsx:134-153` llama `POST /v1/users/:userId/verify`, gateado por `@RequirePermissions("users:verify")` (`users.controller.ts:96-98`) — permiso que **solo tiene `OPS_ADMIN`** (`rbac.ts:165`), ausente en `PRO`. Además `users.policy.ts:16-18` (`canVerifyUser`) también exige `OPS_ADMIN` explícitamente. Distinto del stub DID ya documentado (G-PRO-04): aquí la petición ni siquiera llega a esa lógica, muere en el guard RBAC — el botón de "solicitar verificación" está conectado por error a un endpoint exclusivo de administración. [x] Corregido (2026-07-27) — implementado como feature nueva completa, no un fix de una línea, tal como pedía la nota de la Crew A original:
  - **Nuevo permiso** `users:verify:request` en `rbac.ts`, otorgado a `PRO`/`WORKER` — deliberadamente distinto de `users:verify` (que sigue siendo exclusivo de `OPS_ADMIN` y sigue siendo el único que ejecuta la verificación real).
  - **Nuevo flujo de solicitud, sin ejecutar nada:** `POST /v1/users/:userId/verify-request` (self-only, `UsersPolicy.canRequestVerification`) encola la solicitud — reutiliza el mecanismo de "workspace memory" ya usado para el método de cobro (2.44), con un id determinístico (`worker:{userId}:verification:decision:verification-request-{type}`) para que una segunda solicitud o la revisión del admin sobrescriban el mismo registro en vez de acumular duplicados.
  - **La cola de revisión, descubierta ya existente:** `/admin/trust/worker-applications` ya tenía una sección "Workers sin verificar" con acción directa "Iniciar verificación" (llama el `verifyUser` real) — no hizo falta construir una pantalla nueva desde cero. Se agregó una sección separada "Solicitudes de verificación" arriba de esa, alimentada por el nuevo `GET /v1/users/verify-requests` (`UsersService.listVerificationRequests`, `WorkspaceMemoryRepository.queryAcrossTenant()` — método nuevo, ya que `query()` solo buscaba dentro de un único workspace y esto necesita ver la solicitud de cualquier worker en todo el tenant) — con botones "Aprobar" (llama a `verifyUser` real, mismo efecto que "Iniciar verificación") y "Rechazar" (solo cierra la solicitud, nunca verifica al usuario).
  - `packages/auth`, `apps/api` (rbac, users.policy/service/controller/module, workspace-memory business-records + repository), `apps/web` (3 rutas BFF nuevas + `worker/profile` apuntando a `/verify-request` en vez de `/verify` + la nueva sección en `admin/trust/worker-applications`). 13 tests nuevos entre `test/users.service.test.ts` (8) y cobertura del resto ya verde. `tsc --noEmit`/`eslint` limpios en `@semse/api` y `@semse/web`. Suite completa de `@semse/api`: 1990/1990 verdes. Pendiente verificación en vivo del flujo completo (solicitar como PRO → aprobar/rechazar como OPS_ADMIN).
- **2.29 MEDIO** — El KPI "Aceptados o reservados" del dashboard siempre es idéntico a "Trabajos activos" — usa el campo equivocado. `dashboard/page.tsx:324-329` filtra `["accepted","reserved"].includes(job.status)` sobre `metrics.active`, pero ese array ya viene pre-filtrado por **estado de bid** (`b.status === "accepted"`), no por estado de job — el filtro es siempre verdadero para cada elemento, así que el KPI nunca refleja realmente cuántos jobs están `in_progress`/`review` vs. recién aceptados. [x] Corregido (Crew E, 2026-07-21) — cambiado a `.includes(job.jobStatus)` (el campo real de estado de job en el registro de bid), en vez del campo de estado del propio bid. Pendiente verificación en vivo.
- **2.30 MEDIO** — El badge secundario de estado de job en `/worker/bids` ("Mis propuestas") solo reconoce 5 de los ~9 valores reales de `JobStatus` (`bids/page.tsx:16-22` — le falta `posted`, `reserved`, `review`, `dispute`) — para esos casos cae a mostrar el string crudo en minúsculas sin traducir (p. ej. literal `reserved`) en vez de una etiqueta en español. No afecta el badge principal de estado del bid, que ya está confirmado correcto. [x] Corregido (Crew E, 2026-07-21) — `JOB_STATUS_STYLE` ampliado de 5 a 11 entradas, cubriendo `draft`, `posted`, `reserved`, `awarded`, `review`, `dispute`. Pendiente verificación en vivo.

#### Field-ops / Movilidad (Travel)

- **2.31 CRÍTICO** — El módulo de Movilidad y Estancia es completamente inalcanzable para cualquier PRO real — no es un bug de subida, es que **ningún profesional puede siquiera crear un viaje**. Todos los endpoints de escritura (`POST /v1/travel`, `PATCH .../status`, `POST .../expenses`, `.../lodging`, `.../advances`, `POST .../settlement/close`) exigen el permiso `jobs:create` (`travel.controller.ts`) — permiso que ni `PRO` ni `WORKER` tienen en `packages/auth/src/rbac.ts` (solo `CLIENT`/`OPS_ADMIN`). El botón "+ Nuevo viaje" siempre responde 403. El propio test del backend (`apps/api/test/travel.controller.test.ts`) usa un actor `roles: ["PRO"]` pero llama al controller directo, sin pasar por `RbacGuard` — por eso nunca se detectó este mismatch. [x] Corregido (Crew A, 2026-07-21) — en vez de agregar `jobs:create` a PRO/WORKER (que también habría abierto creación de job listings, tasks, materials e incidents — ver nota de alcance abajo), se introdujo un permiso dedicado `travel:manage` en los 6 endpoints de escritura de `travel.controller.ts`, otorgado a `CLIENT`, `PRO`, `WORKER` y `OPS_ADMIN`. Se actualizó `travel.controller.test.ts` (aserción de permisos, antes verificaba contra `dist/` desactualizado y no detectaba el mismatch real). Pendiente verificación en vivo.
- **2.32 CRÍTICO (seguridad, IDOR cross-tenant)** — El estado de cualquier `FieldUnit` de **cualquier tenant** se puede sobreescribir. `field-ops.repository.ts:122-127` (`updateUnitStatus`) recibe `tenantId` como parámetro pero **nunca lo usa** en el `where` del `update` (a diferencia de `findUnitById`, que sí lo hace) — cualquier usuario con `field-ops:write` (PRO o WORKER) puede cambiar el estado de una unidad de campo de otra organización con solo conocer/adivinar su `id`. [x] Corregido (Crew C, 2026-07-21) — igual que 0.4, `update()` con `id` no acepta un `tenantId` adicional en `where`, así que el fix es `updateMany({where: {id, tenantId}})` + verificación de `count` (404 si 0) + re-fetch, mismo patrón que `findUnitById` ya usaba para lecturas. Pendiente verificación en vivo.
- **2.33 ALTO** — Los cambios de estado de cumplimiento de proveedores (`ComplianceDoc`) nunca actualizan el registro existente — siempre crean uno nuevo. `field-ops.repository.ts:504-531` (`upsertComplianceDoc`) usa `where: { id: \`${vendorId}_${type}\` }` como key del upsert, pero `ComplianceDoc.id` es un `cuid()` real y no existe restricción `@@unique([vendorId, type])` en el schema — el `where` nunca matchea, así que cada cambio de estado (`MISSING→PENDING→APPROVED`) crea una fila nueva en vez de reemplazar la anterior. La UI (`VendorsTab`) toma la primera que encuentra sin `orderBy` garantizado — puede mostrar un estado de cumplimiento legal/de seguro obsoleto o incorrecto. [x] Corregido (Crew E, 2026-07-21) — en vez de agregar una restricción `@@unique([vendorId, type])` (requeriría migración de esquema y no hay garantía de que filas duplicadas históricas existentes la cumplan), se resuelve la fila objetivo con `findFirst({tenantId, vendorId, type})` y luego `update`/`create` según corresponda — mismo resultado sin tocar el schema. Nota: sigue existiendo una ventana de carrera teórica (TOCTOU) entre el `findFirst` y el `update`/`create` en escrituras concurrentes sobre el mismo vendor+type; aceptable dado que es una operación administrativa de baja frecuencia, no transaccional de dinero. Filas duplicadas ya creadas por el bug anterior no se limpian retroactivamente. Pendiente verificación en vivo.
- **2.34 ALTO (seguridad)** — Los endpoints de detalle de viaje (`getAssignment`, `listExpenses`, `listLodging`, `listAdvances`, `computeSettlement`, `closeSettlement`, `createExpense`, etc. en `travel.service.ts`) solo filtran por `tenantId`, nunca verifican `assignedTo === actor.userId` — cualquier persona del mismo tenant con permiso `jobs:read` que conozca un `travelId` ajeno (compartido en chat, URL, captura) puede ver el gasto/anticipos/saldo privado de otro worker; quien además tenga `jobs:create` (CLIENT/OPS_ADMIN) puede agregar gastos o cerrar la liquidación de un viaje que no es suyo. [x] Corregido (Crew C, 2026-07-21) — se centralizó la verificación en `getAssignment` (ahora exige `actorUserId`/`orgId`/`roles` y valida `assignedTo === actorUserId` o `orgId === job.clientOrgId`, con bypass `OPS_ADMIN`), y las 9 funciones restantes que operan sobre un `travelId` (`updateAssignmentStatus`, `listExpenses`, `createExpense`, `listLodging`, `createLodging`, `listAdvances`, `createAdvance`, `computeSettlement`, `closeSettlement`) llaman a `getAssignment` primero en vez de repetir el join 9 veces. Controller y las 10 llamadas actualizadas para pasar el actor completo. Pendiente verificación en vivo.
- **2.35 MEDIO** — `createAssignment` (crear viaje) confía en el `jobId` que manda el cliente sin verificar que exista, pertenezca al tenant, o esté asignado al solicitante (`travel.service.ts:284-333`) — el único intento de validación (`job.updateMany` con filtro de tenant) hace no-op silencioso en vez de bloquear la creación. Impacto bajo hoy porque PRO no puede llegar aquí (ver 2.31), pero CLIENT/OPS_ADMIN sí podrían crear asignaciones de viaje apuntando a jobs ajenos/inexistentes. [x] Corregido (2026-07-23) — `createAssignment` ahora captura el resultado de `job.updateMany({where:{id, tenantId}})` y lanza `NotFoundException` si `count === 0`, en vez de continuar en silencio hacia `travelAssignment.create`. **Nota de incidente (per skill RC4):** no hay logging histórico suficiente para saber si esta ruta ya fue usada para crear asignaciones de viaje apuntando a jobs ajenos/inexistentes antes de este fix — el impacto práctico ya era bajo (PRO no podía llegar aquí, ver 2.31) pero CLIENT/OPS_ADMIN sí podían, y no se auditaron datos existentes de `TravelAssignment` contra `Job` real (sin acceso a base de datos de producción en esta sesión). Suite dirigida (`travel.controller.test.ts`) en verde — no cubre este service method directamente (no existe `travel.service.test.ts` en este repo); validado por lectura de código. Pendiente verificación en vivo.
- **2.36 MEDIO (performance)** — `/worker/travel` dispara una tormenta N+1: por cada viaje en la lista hace 3 llamadas adicionales (`fetchTravelSettlement`, `fetchTravelExpenses`, `fetchTravelLodging`) solo para calcular contadores de badge — y por un bug de dependencias en `useCallback`/`useEffect` (`travel/page.tsx:125-170`), la carga completa (lista + todas las sub-llamadas) se dispara **dos veces** en el primer montaje. Para 15 viajes son ~180 llamadas HTTP solo para abrir la pantalla. [x] Corregido (2026-07-27) — dos fixes independientes, mismo hallazgo:
  1. **N+1 real:** nuevo endpoint batch `GET /v1/travel/summary` (`travel.controller.ts`, permiso `jobs:read` — igual que `listAssignments`, registrado *antes* de `@Get(":travelId")` para no quedar eclipsado por el param route) → `TravelService.listAssignmentsWithSummary()`, que hace `listAssignments()` una vez y luego **3 queries totales** (`travelExpense`/`travelAdvance`/`lodgingBooking`, cada una con `travelId: { in: [...todos los ids] }`), no 3 por viaje. La matemática de saldo/recibos faltantes es la misma que ya usaba `_buildSettlement` — se extrajo a `computeSettlementTotals()` compartida entre ambos, en vez de reimplementarla (ver principio "fix compartido" del skill de remediación). BFF nuevo `app/api/semse/travel/summary/route.ts` + cliente `fetchTravelAssignmentsSummary()` en `semse-api.ts`. `worker/travel/page.tsx` (`load()`) ahora llama solo ese endpoint en vez del loop `Promise.all` de 3 fetches × N viajes.
  2. **Doble carga en el montaje:** `load` (useCallback) dependía de `formJobId` solo para decidir si auto-seleccionar el primer job del formulario — como `load` mismo llamaba `setFormJobId(...)` cuando ese valor cambiaba, la identidad de `load` cambiaba a mitad de la propia carga, y el `useEffect(() => { void load() }, [load])` se disparaba una segunda vez. Cambiado a `setFormJobId((prev) => prev || rawJobs[0]?.id || prev)` (forma funcional, no lee `formJobId` del closure) — `load` ya no necesita `formJobId` en su lista de dependencias (`useCallback(load, [])`, estable).
  - 2 tests nuevos en `test/travel.service.test.ts` (nuevo archivo — no existía suite de servicio para este módulo) verificando que las 3 tablas se consultan exactamente una vez sin importar el número de viajes, más el cálculo correcto de saldo/recibos faltantes por viaje y el caso de lista vacía. `travel.controller.test.ts` actualizado con la entrada RBAC de la nueva ruta. Suite completa de `@semse/api`: 1975/1975 verdes. `tsc --noEmit`/`eslint` limpios en `@semse/web` para los archivos tocados. Pendiente verificación en vivo (contar requests de red reales en `/worker/travel` con >1 viaje).
- **2.37 BAJO** — El tab Tracker de `/worker/field-ops` mezcla dos prefijos de ruta BFF distintos para la misma función (`/api/semse/tracker/...` en pausa/resume vs. `/api/semse/time-tracker/...` en el resto) — ambos existen y funcionan hoy, pero es una inconsistencia sin explicación entre dos familias de rutas paralelas para lo mismo. [x] Corregido (2026-07-27) — se confirmó primero que ambas rutas realmente hacían lo mismo antes de tocar nada: los dos BFF (`app/api/semse/tracker/[sessionId]/pause|resume` y `app/api/semse/time-tracker/sessions/[sessionId]/pause|resume`) llaman a controllers de backend distintos (`FieldOpsController` en `v1/field-ops` vs. `TimeTrackerController` en `v1/time-tracker`), pero **ambos controllers inyectan el mismo `FieldOpsService` y llaman literalmente `pauseTrackerSession()`/`resumeTrackerSession()`** — dos URLs distintas para el mismo código, no dos sistemas distintos. `field-ops/page.tsx` (el tab Tracker canónico) ahora usa `/api/semse/time-tracker/sessions/${id}/pause|resume`, igual que ya usaba para `start`/`stop`/`summary`/`sessions`/`jobs` — sin cambio de comportamiento. **Ampliado más allá del hallazgo original:** al unificar, `app/api/semse/tracker/*` (7 archivos: `route.ts`, `bootstrap`, `manual`, `start`, `[sessionId]/{pause,resume,stop}`) quedó sin ninguna referencia real en todo el monorepo (confirmado por grep en `apps/`+`packages/`, excluyendo el caché autogenerado `.next/types`) — se eliminó el directorio completo en vez de dejar 7 rutas BFF muertas. `tsc --noEmit`/`eslint` limpios en `@semse/web` (tras limpiar el caché stale de `.next/types`, que todavía referenciaba las rutas borradas).
- **2.38 BAJO** — Texto confuso en el bloqueo de liquidación de viaje: `"sin hospedaje requerido"` se usa quiere decir "falta el hospedaje que sí es requerido", pero se lee como "el hospedaje no es requerido" — el significado literal es el opuesto del intencionado (`worker/travel/page.tsx:96-97`, `[travelId]/page.tsx:432-434`). [x] Corregido — reemplazado por `"falta el hospedaje requerido"` en ambas páginas para eliminar la ambigüedad. Pendiente verificación en vivo.

#### Disputas / Pagos / Oportunidades / Configuración / Tarifas

- **2.39 CRÍTICO (dinero)** — El estado visual de cada movimiento de pago en `/worker/payments` se calcula solo a partir de `type`, ignorando el campo `status` real (`PENDING|SUCCEEDED|FAILED|REVERSED`) que el backend ya envía. `payments/page.tsx:58-61`: cualquier `RELEASE` se pinta "Liberado" (verde) y suma a `totalReleased` **aunque su `status` real sea `FAILED` o `REVERSED`**; cualquier `DEPOSIT` se pinta "En escrow" **aunque haya fallado** (tarjeta rechazada). Un worker puede creer que ya le pagaron, o que hay fondos en escrow a su favor, cuando en realidad no. [x] Corregido (Crew D, 2026-07-21) — `worker/payments/page.tsx` ahora lee `row.status` real (ya llega en mayúsculas vía `toVisiblePaymentTxn`); un nuevo estado visual `"failed"` (badge rojo "Fallido / revertido") cubre `FAILED`/`REVERSED`, y `"released"`/`"in_escrow"` ahora exigen `status === "SUCCEEDED"` además del `type` correcto — `totalReleased` automáticamente deja de sumar transacciones fallidas. Pendiente verificación en vivo.
- **2.40 CRÍTICO** — "Mis Tarifas" (`/worker/rates`) le promete al profesional *"Tus tarifas reales reemplazan los promedios BLS en cada estimado"* y *"Se usarán en todos los estimados futuros"* — pero la tarifa guardada **nunca llega a ningún estimado real**. El único lugar que lee `ContractorRateService.getOverride()` es `protools.agent.ts:139-158`, invocado solo desde `POST /v1/semse-agents/protools/estimate`, cuya **única** UI consumidora es `client/protools/page.tsx` — del lado **cliente**, usando el `userId` del cliente (no del profesional asignado). El guardado/carga/borrado de la tarifa sí funciona técnicamente, pero la funcionalidad prometida (afectar estimados reales) no existe en ningún flujo alcanzable — ni por el worker ni por el cliente que vería ese estimado. [x] Corregido (2026-07-27) — **decisión de producto explícita del usuario: conectarla a un flujo real, no solo corregir el copy.**
  - **Hallazgo previo a implementar, importante:** ni `client/protools/page.tsx` (herramienta standalone, sin `jobId` ni worker asignado, se usa *antes* de que exista ningún job) ni `client/jobs/new/page.tsx` (wizard de publicar, usa `budget-intelligence.service.ts`, también *antes* de cualquier match/bid/asignación) son estructuralmente compatibles con "la tarifa del profesional YA asignado" — no existía ninguna superficie del producto donde un job tuviera a la vez un profesional asignado y un estimado visible al cliente. Se diseñó esa superficie nueva en vez de adivinar, con el usuario confirmando el diseño antes de implementar.
  - **Backend:** `BudgetIntelligenceService.suggestBudget()` acepta ahora un `jobId` opcional. Cuando se manda, resuelve el profesional asignado **del lado del servidor** — nunca confiando en un `userId` que mande el cliente — vía `Job.contract.professionalUserId` (el `Contract` real, que solo existe tras aceptar un bid), con `tenantId` + ownership (`clientOrgId === actor.orgId`, o bypass `OPS_ADMIN`) para que un cliente no pueda averiguar la tarifa de un profesional asociado al job de otra organización adivinando `jobId`s. Si hay tarifa (`ContractorRateService.getOverride`), se aplica su `laborMultiplier` sobre el rango ya calculado — mismo patrón exacto que el ajuste regional de `LocationCostService` que ya usaba esta función (0.29), con su propio `factors` explicando el ajuste ("Tarifa real del profesional asignado ($X/hr) en vez del promedio BLS (×Y)"). `IntelligenceModule` ya importaba `PricingModule` desde el fix de 0.29 — no hizo falta wiring nuevo de módulos.
  - **Endpoint:** `POST /v1/intelligence/budget/suggest` (mismo endpoint ya usado por `client/jobs/new`) ahora reenvía `jobId`/`orgId`/`roles` del actor al service. `client/protools` y `client/jobs/new` siguen funcionando exactamente igual (no mandan `jobId`, el ajuste nunca se activa ahí).
  - **Frontend (la superficie nueva):** en `client/jobs/[jobId]/page.tsx`, cuando el job ya tiene un `acceptedBid`, aparece una sección nueva "Estimado con tarifa real del profesional" con un botón "Recalcular con tarifa real" que vuelve a pedir el estimado con `jobId` — muestra el rango ajustado y la nota explicando el ajuste, o un mensaje honesto si el profesional no tiene tarifa configurada en "Mis Tarifas".
  - **Copy de `/worker/rates` corregido para ser exacto** (antes prometía "en cada estimado"/"todos los estimados futuros" sin condición): ahora dice que aplica "una vez que ese trabajo te lo asignen a vos" / "en el estimado de cualquier trabajo que se te asigne a partir de ahora" — ya no promete algo que no puede cumplir para trabajos sin asignar.
  - 5 tests nuevos en `test/budget-intelligence.service.test.ts` (archivo nuevo — no existía suite para este servicio): aplica el ajuste cuando el job es propio y tiene contrato; lo omite si el job es de otra org; lo aplica igual para `OPS_ADMIN` sin importar la org; lo omite si el job todavía no tiene contrato; y confirma que sin `jobId` el comportamiento es idéntico al de antes. `tsc --noEmit`/`eslint` limpios en `@semse/api` y `@semse/web`. Suite completa de `@semse/api`: 1977/1977 verdes. Pendiente verificación en vivo (requiere un job real con bid aceptado y un profesional con tarifa configurada).
- **2.41 ALTO** — 3 de los 5 controles de "Configuración del asistente" (`/worker/settings`, la pantalla mal etiquetada "Asistente IA" en el sidebar) son adornos sin efecto real. `buildSystemPrompt()` (`prometeo-orchestrator.service.ts:324-337`) solo lee `assistantTone` y `expertMode` — `assistantLanguage` y `assistantVerbosity` solo se interpolan en una línea de log interno de debug, nunca cambian ningún prompt/respuesta; `unifiedMode` se persiste de punta a punta pero jamás se lee como condición en ningún `if`/ternario de todo el repo (confirmado por grep completo). Los 5 muestran igual "✓ Configuración guardada." [x] Corregido (Crew E, 2026-07-21) — `buildSystemPrompt` ahora también deriva `languageInstruction` (es/en), `verbosityInstruction` (short/balanced/detailed) y `unifiedModeInstruction` (contexto cruzado entre proyectos) desde `ctx.assistantSettings`, e inyecta las tres al prompt del sistema. Verificación en vivo parcial (2026-08-01): `PATCH /v1/users/me/profile` con `assistantLanguage:"en"`, `assistantVerbosity:"detailed"`, `unifiedMode:true` devuelve `200` y los 3 valores persisten correctamente en el perfil (confirmado leyendo la respuesta). **No se pudo confirmar el efecto en una respuesta real de Prometeo** — no hay proveedor LLM configurado en este stack local (`LLMNarrativeService disabled — no LLM provider available` en los logs), así que el chat responde con contenido de demo fijo sin importar el prompt de sistema construido; verificar el efecto real requeriría un proveedor LLM real (Ollama local o un provider en producción).
- **2.42 ALTO** — El aviso "En disputa" de `/worker/payments` es código muerto permanente — nunca se activa aunque el job sí tenga una disputa real. `payments/page.tsx:47-51` intenta cruzar pagos con disputas usando `d.jobId`, pero `GET /v1/disputes` **nunca devuelve `jobId`** (`disputes.repository.ts:431-453` solo emite `projectId`) — el `Set` de jobs en disputa siempre queda vacío. `disputes/page.tsx` sí resuelve este mismo hueco de la API cruzando por `fetchProjects()`; `payments/page.tsx` no hace ese join. [x] Corregido (Crew E, 2026-07-21) — `worker/payments/page.tsx` ahora también llama `fetchProjects()` y construye `jobIdByProjectId` para cruzar `dispute.projectId → project.jobId`, replicando el patrón ya correcto de `disputes/page.tsx`. Pendiente verificación en vivo.
- **2.43 ALTO** — `/worker/disputes` solo reconoce 2 de los 5 estados reales de disputa (`open`/`assigned`/`resolved` esperados, pero el código de `disputes/page.tsx:37-42` solo mapea `resolved` y `assigned` explícitos, todo lo demás cae a `"open"`). Una disputa marcada **`REJECTED`** (cerrada, decidida en contra del worker) se muestra para siempre como "Abierta" — contada en el stat card "Abiertas" y visible bajo ese filtro, sin ninguna forma de que el worker se entere de que ya se cerró. `under_review` sufre el mismo problema, perdiendo la distinción "alguien la está revisando" vs. "nada ha pasado". [x] Corregido (Crew E, 2026-07-21) — `DisputeRow["status"]` ampliado a los 5 estados reales; `normalizeDisputeStatus` los mapea todos; nueva constante `ACTIVE_DISPUTE_STATUSES` (`open`/`assigned`/`under_review`) reemplaza el chequeo `!== "resolved"`; el stat card "Resueltas" ahora cuenta `resolved || rejected`. `DisputeResolutionWorkspace.tsx` (consumidor compartido con el detalle de disputa) también se amplió al mismo set de 5 estados para que el tipo siga siendo consistente end-to-end. Pendiente verificación en vivo.
- **2.44 ALTO (seguridad/cumplimiento, PCI-DSS)** — El formulario de "Método de cobro" (`PayoutMethodForm.tsx`) recolecta número de tarjeta completo y número de cuenta/routing bancario completo en inputs de texto plano propios (no Stripe Elements ni Plaid, pese a que la misma pantalla ya integra Stripe Connect para el otro riel de pago) y los envía sin tokenizar como JSON crudo desde el navegador → BFF Next.js → backend NestJS. El backend sí descarta los números completos y solo guarda `last4` (`payments.service.ts:145-154`), pero el PAN/routing completo igual transita en texto plano por toda la cadena — alcance PCI-DSS real independientemente de qué se guarde al final. [x] Corregido (2026-07-27) — **decisión de producto explícita del usuario: migrar ahora, solo con Stripe (sin agregar Plaid como proveedor nuevo)** — no había ninguna integración de Stripe del lado del navegador en todo el repo (`STRIPE_SECRET_KEY` solo existía server-side); se agregó desde cero.
  - **Tarjeta:** `PayoutMethodForm.tsx` reemplaza el `<input>` propio de número de tarjeta por el `<CardElement>` de `@stripe/react-stripe-js` — un iframe alojado por Stripe; el PAN nunca toca el DOM ni el JS de SEMSE en ningún momento.
  - **Cuenta bancaria:** Stripe no tiene un Element prefabricado para esto, así que se mantienen los `<input>` propios de routing/cuenta, pero en el submit se llama `stripe.createToken("bank_account", {...})` — la llamada va del navegador directo a los servidores de Stripe (nunca pasa por el BFF ni el backend de SEMSE); es el patrón que la propia documentación de Stripe prescribe para este caso. Solo el token resultante (`btok_...`) más el `last4` que devuelve Stripe llegan a nuestro backend.
  - **Backend:** `workerPayoutMethodSchema` ya no acepta `routingNumber`/`accountNumber` — solo `stripeToken` (opcional) + `last4`. `PaymentsService.saveWorkerPayoutMethod` ahora **verifica el token contra la API real de Stripe** (`StripeConnectService.verifyPayoutToken()`, nuevo método, reutiliza el cliente de Stripe ya instanciado ahí) para obtener el `last4` autoritativo en vez de confiar en lo que mande el navegador — si Stripe no está configurado (mock/dev), cae de vuelta al valor que mandó el cliente en vez de fallar duro fuera de un deploy real. El token en sí no se persiste (es de un solo uso; este registro es de estado/visualización, no el mecanismo real de payout — ver nota abajo).
  - **Hallazgo adicional durante la investigación:** este formulario **no es el mecanismo real de pago** — es un registro informativo separado (guardado en "workspace memory", no en una tabla dedicada) desconectado del panel "Cuenta Stripe Connect" de la misma pantalla, que sí es el riel real de payout (0.16/2.1c). El fix de todas formas cierra la brecha PCI-DSS real (los dígitos completos ya no transitan nuestro BFF/backend bajo ninguna circunstancia), independientemente de que este registro en particular sea solo informativo.
  - **Nuevo:** `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` (documentado en `apps/web/.env.example`) — **acción pendiente del usuario:** configurar el valor real en Railway (misma cuenta de Stripe que ya usa `STRIPE_SECRET_KEY` en el backend); sin esa variable el formulario muestra un mensaje claro de "no configurado" en vez de degradar de forma insegura.
  - 4 tests nuevos en `test/payments-payout-method.service.test.ts` (Stripe gana sobre el valor del cliente; fallback si Stripe no está configurado; nunca se llama para wallets digitales; funciona sin `StripeConnectService` inyectado). `tsc --noEmit`/`eslint` limpios en `@semse/api` y `@semse/web`. Suite completa de `@semse/api`: 1981/1981 verdes. Pendiente verificación en vivo (necesita la clave pública real configurada y un navegador).
- **2.45 MEDIO** — El flujo de "paquete de evidencia" en la resolución de disputas del worker es enteramente simulado — no hay ningún archivo real de por medio. `DisputeResolutionWorkspace.tsx:171-178` usa un input de texto libre + un número (nombre y tamaño en MB "declarados"), no un `<input type="file">` real; "Preparar paquete" llama `planUpload` con un `fileSizeBytes` inventado, y para `external_transfer` corre una sesión multipart completa que nunca envía bytes (mismo patrón de PUT falso que 0.34/2.18) mostrando "Sesión multipart completada" como si algo se hubiera subido. Más grave que 0.34 porque aquí ni siquiera hay un archivo real que el usuario haya elegido — y ocurre justo en el flujo que debería sustentar una disputa con evidencia real. [x] Corregido (2026-07-27) — reemplazado exactamente como sugería la nota de Crew B: `<input type="file">` real + el mismo patrón `planUpload`→`PUT`→`registerJobEvidence` ya usado en `worker/evidence` (0.34), sin reinventar el contrato de subida. `handleUploadEvidencePackage` (antes `handlePlanAttachment`) ahora lee `file.name`/`file.size`/`file.type` reales del archivo elegido, hace el PUT real al key presignado, y registra la evidencia real en el job vinculado a la disputa (`dispute.jobId`). **La sesión multipart falsa se eliminó por completo** (`handleCompleteMultipart`, los imports `createMultipartUploadSession`/`uploadMultipartPart`/`completeMultipartUploadSession`, el tipo `MultipartSessionView`) — igual que 0.34, `external_transfer` (archivos grandes) ahora falla con un mensaje claro en vez de fingir una subida completada con etags fabricados; no hay ruta de subida real para archivos grandes todavía en ningún flujo de este repo. El formulario separado de "referencia manual de evidencia" (`handleRegisterEvidenceReference`, key tecleada a mano) no se tocó — es una feature distinta y ya honesta (no finge subir nada, solo referencia una key ya existente). `tsc --noEmit`/`eslint` limpios en `@semse/web` (0 errores; 3 warnings de `useEffect` preexistentes, no introducidos por este cambio). Sin test dedicado — no hay convención de tests de componentes React en este repo (todos los tests existentes son de servicios/controllers backend); validado por lectura de código contra el patrón ya probado de 0.34. Pendiente verificación en vivo (requiere una disputa real con `jobId` vinculado).
- **2.46 MEDIO — confirma 0.33 en una ubicación nueva** — Las dos acciones principales de autogestión de disputa en el worker ("Pedir criterio" y "Pedir tercero humano", `DisputeResolutionWorkspace.tsx:352-448`) llaman `POST /v1/agents/copilot`, que exige `agents:run:create` — el permiso que **0.33** ya documentó como ausente para PRO. Ambos botones fallan siempre para cualquier profesional; la pantalla de disputas promete literalmente *"evidencia, criterio del copiloto y escalación asistida"* como las herramientas de autogestión del worker. [x] Corregido (Crew A, 2026-07-21) — ver 0.33. Pendiente verificación en vivo.
- **2.47 MEDIO** — El primer clic en "Enviar propuesta" (`/worker/opportunities`) no hace nada visible, sin error ni feedback. `opportunities/page.tsx:367` dispara `setBidForm(...)` (async) y `submitBid()` (síncrono) en el mismo click — `submitBid` corre contra el `bidForm` todavía `null` de un closure viejo y corta en silencio (`if (!bidForm) return;`). Se necesita un segundo clic para que aparezca siquiera la validación real ("Monto y días son requeridos"). [x] Corregido (Crew E, 2026-07-21) — `submitBid` ahora recibe el form como argumento explícito en vez de leerlo del closure de estado; el `onClick` calcula el form efectivo una sola vez y lo pasa directo a `setBidForm` y a `submitBid`, eliminando la dependencia del timing de commit de React. Verificación en vivo confirmada (2026-08-01): login como `worker@demo.semse`, `/worker/opportunities`, se llenó el formulario inline (monto $1500, 5 días) y se hizo **un solo clic** en "Enviar propuesta" — el botón pasó a "Enviando..." de inmediato y `POST /api/semse/bids` se disparó exactamente una vez (sin necesitar un segundo clic). Confirmado en `GET /v1/my-bids` que el bid se creó realmente (`amount: 1500, etaDays: 5, status: "submitted"`).
- **2.48 BAJO** — `window.open(r.onboardingUrl, "_blank")` en el botón de onboarding de Stripe (`payments/page.tsx:258`) no usa `noopener,noreferrer` — reverse tabnabbing, deja `window.opener` accesible a la página abierta. [x] Corregido (Crew F, 2026-07-21) — agregado el tercer argumento `"noopener,noreferrer"` a `window.open`.

**Pantallas recorridas en vivo (2026-07-20 y 2026-07-21):** Dashboard ("Mi Panel"), Oportunidades, Mis trabajos (+ detalle de trabajo), Time Tracker, Operaciones de campo (+ pestaña Tracker), Mi perfil, Mis pagos (+ Método de cobro), **Mis propuestas** (correcta — badge "Aceptada" bien mostrado, usa `BidStatus` no `JobStatus`), **Agenda** (badge "Aceptado" correcto pese a tener el mismo código roto que otras pantallas — ver nota en 0.0 sobre por qué esta sí funciona; breadcrumb duplicado igual que en cliente, ver 1.12), **"Asistente IA"** (en realidad `/worker/settings`, configuración de tono — no es un chat, ver 0.33), **widget flotante de Prometeo/agentes** (roto para este rol, ver 0.33 y 2.1e), **catálogo `/agents`** (16 conversacionales + 8 especializados backend-only, revisado completo), **Tareas** (`/worker/tasks` — correcta, estado vacío bien explicado: "las tareas aparecen cuando un cliente asigna un milestone"), **Evidencia** (`/worker/evidence` — subida real de una foto confirma bug crítico, ver 0.34/2.1f), **Materiales** (`/worker/materials` — correcta, formulario "Solicitar material" abre bien, no se envió para no ensuciar datos reales), **Incidencias** (`/worker/incidents` — correcta, todo en cero legítimo), **Movilidad** (`/worker/travel` — lista correcta, sin viajes activos para forzar la repro en vivo del bug de comprobantes, confirmado por código, ver 0.34), **Reseñas** (`/worker/review` — correcta, estado vacío claro; le falta el link "← Dashboard" que sí tienen las demás pantallas, inconsistencia menor). No se activó el cronómetro legacy ni se creó cuenta Stripe Connect real para no generar datos falsos en producción. **Nota transversal confirmada de nuevo en esta ronda:** el selector de Tema (claro/oscuro) cambia solo entre pantallas sin acción del usuario (Materiales/Movilidad/Reseñas en "Claro", Incidencias en "Oscuro", todo en la misma sesión) — refuerza 1.9.

**Cobertura en vivo y de código del módulo Worker: completa.** Pendiente para cerrar del todo:
- [x] ~~Lanzar la ronda de agentes de código (bugs + inconsistencias) enfocada exclusivamente en `apps/web/app/(app)/worker/**`~~ — hecho 2026-07-21, 5 agentes en paralelo (Tracker/Labor Engine; Field-ops+Movilidad; Trabajos/Dashboard/Perfil/Agenda/Propuestas; Disputas/Pagos/Oportunidades/Configuración/Tarifas; Evidencia/Incidencias/Materiales/Reseñas/Tareas). 42 hallazgos nuevos, ver ítems **2.8 a 2.48** arriba y actualizaciones a **0.0, 0.33, 0.34, 1.12**.
- [x] Verificar en vivo el bug de duplicación de horas (2.1/0.20) forzando un corte de red a medio sync en el Time Tracker real — E2E Railway PASS: 1 `start`, 1 `pause` fallido por corte, 1 `pause` en retry y 1 sola entrada persistida.
- [x] ~~Investigar la causa raíz compartida entre 1.4 (cliente) y 2.1b (worker)~~ — resuelto, ver **0.0**: comparación mayúsculas/minúsculas contra `JobStatus`, repetida en 8 archivos incluido Admin (pendiente de confirmar en vivo del lado Admin).
- [x] ~~Probar el chat de Prometeo y los agentes especializados como se hizo con Cliente~~ — hecho 2026-07-21: roto para PRO, causa raíz confirmada, ver **0.33** y **2.1e**.
- [x] ~~Recorrer las pantallas que faltaron: Tareas, Evidencia, Materiales, Incidencias, Movilidad, Reseñas~~ — hecho 2026-07-21, ver arriba. Evidencia reveló el bug crítico **0.34**.

**Re-verificación de código (2026-07-29):** tras los merges grandes de fines de julio (PR #470 agro offline, #471 sidebar Labor Engine, #472 Project Lifecycle Projection), se re-leyó el código actual de 2.1, 2.1c, 2.6, 2.24, 2.25, 2.28, 2.38, 2.40, 2.44, 2.45 — los 10 siguen con el comportamiento descrito, sin regresión introducida por esos merges (incluida una verificación puntual del pipeline de Stripe Elements de 2.44 contra el backend real). No se pudo confirmar en pantalla — sin navegador funcional esta sesión.

---

## Sección 3 — Módulo Admin (pendiente de auditoría dedicada)

Lo que ya sabemos, encontrado en la ronda inicial de UX (solo análisis de código — nunca se vio funcionando, falta acceso admin):

- **3.0 CRÍTICO — mismo patrón que 0.0, confirmado por código, falta solo verificación en vivo** — `apps/web/app/(app)/admin/dashboard/page.tsx` tiene el mismo patrón de comparación de `JobStatus` en minúsculas contra el enum real en mayúsculas (ver **0.0**). Por código se ve idéntico al bug de Cliente/Worker; falta credencial de OPS_ADMIN para confirmarlo en pantalla. Si se corrige 0.0, revisar si este archivo queda cubierto por el mismo fix o necesita su propio ajuste. **Actualización 2026-07-21 (Crew A):** este archivo usa `fetch("/api/semse/jobs")`, la misma ruta BFF que ya se corrigió en 0.0 — por código debería quedar cubierto por el mismo fix, pero sigue **sin confirmar en vivo** (sigue sin credencial OPS_ADMIN). No marcar `[x]` hasta esa confirmación. **Confirmación de código 2026-07-27:** se leyó `apps/web/app/api/semse/jobs/route.ts` línea por línea — el `GET` aplica `json.data = json.data.map(normalizeJobRecordStatus)` sobre la respuesta completa antes de devolverla, y **ambos** fetches de `admin/dashboard/page.tsx` (líneas 48 y 62 — hay dos llamadas redundantes a la misma ruta, una en `loadData()` y otra duplicada inline en el `useEffect` de montaje, no confundir con un bug nuevo, solo redundancia) pasan por esa misma ruta sin ningún parámetro que la desvíe. El filtro de la línea 92 (`["in_progress","reserved","accepted","review"].includes(j.status)`) ya compara contra minúsculas, que es exactamente lo que la ruta normalizada entrega. **Conclusión: sí está cubierto por 0.0, ningún cambio de código adicional necesario aquí.** [x] Corregido — confirmado en vivo (2026-07-31), ningún cambio de código requerido (ya cubierto por 0.0). Verificación: levantado el stack local completo (Postgres, Redis, `pnpm dev:api` + `pnpm dev:web`, seed con `admin@demo.semse`/`demo1234`), con `SEMSE_ENABLE_ACCESS_TOKEN_BOOTSTRAP=true` en `apps/web/.env` (necesario en local porque `resolveAuthorizationHeader()` en `_server.ts` solo bootstrapea el Bearer token automáticamente en producción — sin este flag, cualquier ruta BFF que dependa de `buildAuthorizedHeaders` devuelve 401 en dev, algo no relacionado con este hallazgo pero que bloqueaba poder verificarlo). Con la cuenta seed real (5 jobs: 3 `PUBLISHED`, 1 `IN_PROGRESS`, 1 `COMPLETED`), `/admin/dashboard` mostró "Trabajos activos: 1", "Completados: 1", "Total trabajos: 5", "Presupuesto activo: $2,800", "API Backend: Online" — y `/admin/jobs` mostró los mismos 5/1/1/0 (total/activos/completados/disputas), con cada fila mostrando su `status` normalizado en minúsculas correctamente etiquetado ("Posted"/"Completed"/"In Progress"). Capturas de pantalla verificadas.

- **3.1 ALTO** — `/admin/labor-engine` (pantalla insignia) invisible en el menú de Admin — ni en `navigation-registry.ts` ni en `ADMIN_MODULES`. Solo alcanzable vía tarjeta enterrada en `/admin/workops`. [x] Corregido (Crew G, 2026-07-21) — se agregó `{ id: "labor-engine", ... }` a los children de `workops` en `apps/web/lib/admin/admin-navigation.ts`, junto a `jobs`/`field-ops`/`worker`. Confirmado por código que la ruta ya existía (`apps/web/app/(app)/admin/labor-engine/page.tsx`), solo faltaba el link. Pendiente verificación en vivo.
- **3.2 ALTO** — Resolución de disputas en Admin: un clic, sin confirmación, notifica de inmediato. `apps/web/app/(app)/admin/disputes/page.tsx` — RESOLVE_OPTIONS:658-673 · handleApprovalDecide:351. [x] Corregido — `handleResolve` y `handleApprovalDecide` ahora muestran `window.confirm` con el texto de la resolución/decisión antes de llamar a la API y notificar a ambas partes.
- **3.3 ALTO** — Falla parcial de carga en labor-engine deja el KPI de costo estimado silenciosamente incompleto. `admin/labor-engine/page.tsx:139-157`. [x] Corregido — `load` ahora usa `Promise.allSettled`, detecta fallos individuales de overview/rates/jobs y muestra un mensaje de error parcial en lugar de dejar los KPIs en silencio.
- **3.4 MEDIO** — Alertas de QualityGuard son de solo lectura — sin botón para actuar desde la misma pantalla. `admin/labor-engine/page.tsx:246-267`. [x] Corregido — en `/admin/labor-engine` cada alerta ahora muestra un botón "Ver perfil" hacia `/admin/users/{workerId}` y, para `stale_timer` con `entryId`, botones "Pausar" y "Detener" con `window.confirm` que invocan los endpoints admin `POST /v1/labor/admin/timer/{id}/pause` y `stop` (gated por `ops:dashboard:write`) y recargan el overview; errores se muestran en un banner rojo.
- **3.5 MEDIO** — IDs crudos (UUID) en vez de nombres; ninguna lista de Admin revisada tiene paginación. [x] Corregido — `/admin/labor-engine`, `/admin/disputes` y `/admin/users` ahora mapean IDs de usuario a nombres legibles (parte local del email) y paginan listas largas; `labor-engine` también muestra el título del job en el timer activo cuando está disponible. Pendiente verificación en vivo.
- **3.6 MEDIO** — Solo 4 de ~55 páginas de Admin usan `ModuleShell` — el resto arma su propio header a mano. [x] Corregido — se creó el componente compartido `AdminPageHeader` (ahora con variantes `panel` y `showBack`) y se aplicó a todos los headers de página de Admin: `/admin/labor-engine`, `/admin/disputes`, `/admin/users`, `/admin/settings`, `/admin/coordinator`, `/admin/field-ops`, `/admin/change-orders`, `/admin/contractors`, `/admin/agents`, `/admin/qa`, `/admin/reports`, `/admin/dashboard`, `/admin/compliance`, `/admin/travel`, `/admin/memory`, `/admin/algorithm-engine`, `/admin/prometeo`, `/admin/ai-mission-control`, `/admin/trust`, `/admin/reputation`, `/admin/governance`, `/admin/marketplace`, `/admin/ecosystem`, `/admin/tools`, `/admin/worker`, `/admin/browser-agent`, `/admin/verticals/construction`, `/admin/llm-metrics`, `/admin/jobs`, `/admin/intelligence-rooms`, `/admin/pmo`, `/admin/product-intelligence`, `/admin/ops/loops`, `/admin/verticals/maintenance`, `/admin/verticals/cleaning`, `/admin/verticals/agro`, `/admin/html-in-canvas`, `/admin/browser-agent/missions`, `/admin/users/[id]`, `/admin/trust/worker-applications`, `/admin/verticals/vision`, `/admin/developer-runtime`, `/admin/finance`, `/admin/consciousness`, `/admin/vision`, `/admin/mission-control`, `/admin/travel/[travelId]`, `/admin/autonomy`, `/admin/communications`, `/admin/domain-events`, `/admin/ops`, `/admin/jobs/[jobId]` y `/admin/intelligence-rooms/[id]`. `/admin/semse-x` conserva su logo de sidebar custom por diseño de interfaz inmersiva; no es un header de página.
- **3.7 CRÍTICO (dinero) — mismo patrón que 3.2, encontrado en Finance** — El botón "Liberar" del panel de escrow en `/admin/finance` (pestaña "Pagos & Escrow") disparaba una liberación real de fondos con un solo clic y sin confirmación — a diferencia de "Reembolsar", en la misma pantalla, que sí exige abrir un formulario con monto/razón antes de confirmar. Encontrado junto con un bug de integridad de saldo más serio en el mismo flujo, ver **0.35** para el detalle técnico completo. [x] Corregido (Crew G, 2026-07-21) — se agregó el mismo patrón de confirmación explícita (formulario intermedio, botón "Cancelar") que ya tenía "Reembolsar". Ver 0.35. Pendiente verificación en vivo.
- **3.8 ALTO — recurrencia de 2.39/G-PRO-12, encontrado en Finance** — El panel de escrow de `/admin/finance` calculaba sus 3 KPIs ("En escrow"/"Liberado"/"Reembolsado") sin filtrar por el `status` real de cada transacción (`PENDING|SUCCEEDED|FAILED|REVERSED`) — "En escrow" incluso filtraba por `"PENDING"` en vez de `"SUCCEEDED"` (mostraba como confirmado justo el dinero que no lo estaba). Cada fila de transacción tampoco distinguía visualmente una `FAILED`/`REVERSED` de una `SUCCEEDED`. Detalle técnico en **0.35**. [x] Corregido (Crew G, 2026-07-21) — los 3 KPIs ahora filtran `status === "SUCCEEDED"`; cada fila muestra un segundo badge con su status real y las filas fallidas/revertidas aparecen tachadas/atenuadas. Pendiente verificación en vivo.
- **3.9 MEDIO** — Los botones de aprobar/rechazar gasto y enviar/marcar-cobrada factura en `/admin/finance` ignoraban el resultado real de la petición (`.catch(() => null)`), sin ningún error visible al usuario si la acción fallaba — un fallo silencioso queda indistinguible de un éxito real en una pantalla de dinero. `admin/finance/page.tsx` (`InvoiceRow`/`ExpenseRow`). [x] Corregido (Crew G, 2026-07-21) — cada fila ahora captura y muestra el error real si la acción falla, en vez de tragárselo. Pendiente verificación en vivo.

**Auditoría de código en paralelo (2026-07-22, ronda de 5 agentes, cubre las ~46 carpetas de `apps/web/app/(app)/admin/**`):** todos los hallazgos de abajo (3.10–3.45) son **solo código, sin confirmar en vivo** (sin credencial OPS_ADMIN esta sesión), mismo criterio que G-ADM-00→08.

#### Seguridad (permisos, IDOR, spoofing de identidad)

- **3.10 CRÍTICO (dinero/seguridad, identidad falsificable) — El voto de gobernanza DAO confiaba en un `voterId` enviado por el cliente** — `governance.controller.ts:95-97` (`castVote`) y `:40-42` (`createProposal`, mismo patrón con `authorId`) solo caían a `ctx.userId` cuando el campo venía **ausente** del body — cualquier actor con `ops:dashboard:read` podía votar/proponer como cualquier otro usuario, en una votación ponderada por reputación. Peor: `admin/governance/page.tsx:128` mandaba literalmente `voterId: "me"` en cada voto, así que todos los votos emitidos desde ese panel quedaban registrados bajo el mismo usuario ficticio `"me"` (colisionando en la unique key `proposalId_voterId`). [x] Corregido (Crew G, 2026-07-22) — `voterId`/`authorId` ahora se derivan siempre de `ctx.userId`, nunca del body; se quitó el literal `"me"` del frontend y se agregó manejo de error visible al votar (antes silencioso). Pendiente: separar `ops:dashboard:read` (lectura) de un permiso dedicado de voto/cierre — se deja como mejora, no bloqueante ya que la vulnerabilidad de identidad ya está cerrada. Pendiente verificación en vivo.
- **3.11 CRÍTICO (seguridad, cross-tenant) — IDOR en misiones de Browser Agent** — `browser-agent.service.ts` `getMission`/`updateMission`/`updateStep` no filtraban por `tenantId` (a diferencia de `createMission`, que sí lo guarda) — cualquier actor con `agents:run:create` (compartido por CLIENT/PRO/WORKER/OPS_ADMIN) que adivinara/enumerara un `missionId`/`stepId` de otro tenant podía leer su objetivo/pasos o sobrescribir su `status`/`error`/`evidenceRef`. [x] Corregido (Crew G, 2026-07-22) — las 3 funciones ahora exigen `tenantId` (via `resolveRequestContext`) y usan `findFirst`/`updateMany` con conteo de 0 filas → 404, mismo patrón RC4 ya usado en el resto del backend. Pendiente verificación en vivo.
- **3.12 CRÍTICO (seguridad) — Los endpoints de callback de Developer Runtime aceptaban una sesión/misión completamente fabricada** — `POST sessions/:id/worker/complete` y `/worker/fail` (`developer-runtime.controller.ts:249-327`) están gateados por `agents:run:manage`/`agents:run:worker`, permisos que además de al servicio interno legítimo (`apps/worker`, rol configurable vía `SEMSE_ROLES`) también tiene PRO y WORKER en `packages/auth/src/rbac.ts`. `completeWorkerExecution`/`failWorkerExecution` (`developer-runtime.service.ts:438-486`) pasaban `input.session`/`input.mission` (tomados **literalmente del body**) directo a un `upsertSession`/`upsertMission` sin verificar que `sessionId` correspondiera a una sesión real ya creada por el flujo gateado — cualquier PRO/WORKER podía inventar una sesión desde cero con diffs/validaciones falsas y verla aparecer en el panel de Admin como si fuera evidencia real de ejecución automatizada. [x] Corregido — parcial (Crew G, 2026-07-22) — se agregó verificación previa: `session.id` del body debe coincidir con el `sessionId` de la URL, y la sesión debe existir ya (creada por el flujo gateado) antes de aceptar el callback — cierra el vector de "inventar una sesión desde cero". **No se tocó el permiso** (`agents:run:manage`/`agents:run:worker` compartido con PRO/WORKER) porque no hay forma de confirmar en esta sesión con qué rol está configurado el `apps/worker` real en producción (`SEMSE_ROLES`, variable de entorno) — restringirlo a un permiso dedicado OPS_ADMIN-only sin esa confirmación arriesga romper la automatización real. Queda documentado como siguiente paso recomendado, no implementado a ciegas. Pendiente verificación en vivo y confirmación del rol real del worker service.
- **3.13 CRÍTICO (seguridad) — Módulo de verificación de workers: ni Admin puede usarlo, y donde funciona filtra sobre toda la tabla de usuarios sin scoping de tenant** — `/admin/trust/worker-applications` llama endpoints gateados por `worker:read`/`worker:write` (`worker-verification.controller.ts`, `worker-application.controller.ts`) — permisos que **no existen en ningún rol** de `packages/auth/src/rbac.ts`, ni siquiera OPS_ADMIN — 403 permanente para todo el mundo, peor que G-PRO-09 (ahí al menos el admin real podía usar su propio endpoint). Además, `worker-verification.repository.ts` `getUnverifiedWorkers`/`countVerifiedWorkers` ignoraban por completo el parámetro `_tenantId` y devolvían/contaban **todos los usuarios de la base de datos entera**, sin filtrar siquiera por rol worker/PRO ni por estado de verificación real — cualquier tenant veía la misma lista global. [x] Corregido — parcial (Crew G, 2026-07-22) — se agregó `worker:read`/`worker:write` a `OPS_ADMIN` en `rbac.ts`; `getUnverifiedWorkers`/`countVerifiedWorkers` ahora filtran por `tenantId` real (via `memberships.org.tenantId`) y por rol (`PRO`/`WORKER`) y estado (`verificationStatus`). **No corregido:** la verificación DID subyacente sigue siendo el stub ya documentado en **0.9** (`signature && publicKey && message`, sin criptografía real), y el flujo de Admin nunca completa el segundo paso requerido (`submitDidSignature`) — hacer que esto funcione de punta a punta es una decisión de producto (¿quién aporta la firma criptográfica real, el propio worker o un proceso separado?), no un fix de una línea; se deja señalado, no implementado a ciegas. Pendiente verificación en vivo.
- **3.14 ALTO (seguridad, recurrencia de G-ADM-07) — CRUD de la consola Prometeo gateado por un permiso que cualquier rol autenticado tiene** — `prometeo.controller.ts` (`POST /ingest`, `/ingest-file`, `DELETE /documents/:id`) usa solo `agents:run:create`, otorgado a CLIENT/PRO/WORKER/OPS_ADMIN — cualquier cliente o profesional puede, vía API directa, borrar documentos indexados de la base de conocimiento del tenant o crear registros, sin pasar por `/admin/prometeo`. [x] Corregido — se agregó el permiso `knowledge:manage` (OPS_ADMIN-only en `rbac.ts`) y se aplicó a `POST /v1/prometeo/ingest`, `/ingest-file` y `DELETE /v1/prometeo/documents/:id`. Las operaciones de lectura (search, rag-context, rag-query, trade-guide, listTools, invokeTool, listAssets, etc.) siguen usando `agents:run:create` para que CLIENT/PRO/WORKER las sigan consumiendo. Pendiente verificación en vivo.
- **3.15 ALTO (seguridad, recurrencia de G-ADM-07) — El feed de delegaciones del Coordinator es alcanzable por cualquier rol vía API directa** — `GET /v1/agents/delegations` y `/coordinator/snapshot` (`agents.controller.ts:626-665`) usan `agents:run:create` (compartido por CLIENT/PRO/WORKER). El middleware bloquea la *página* `/admin/coordinator` para esos roles, pero no agrega ningún chequeo extra en la ruta BFF (`api/semse/agents/delegations/route.ts`) — cualquier CLIENT/PRO/WORKER autenticado puede llamar la API directo y obtener el mismo feed tenant-wide (títulos de tarea, resultados completos) que ve Admin. [x] Corregido — se agregó el permiso `ops:coordinator:read` (OPS_ADMIN-only en `rbac.ts`) y se aplicó a `GET /v1/agents/delegations`, `/delegations/:id` y `/coordinator/snapshot`. Pendiente verificación en vivo.

#### Confirmación de acciones irreversibles/consecuentes (mismo patrón que 3.2/3.7, nuevas ubicaciones)

- **3.16 ALTO** — `/admin/ops`: "Aprobar" en Pending Approvals ejecuta la acción real al instante sin confirmación — cuando el `correlationId` es de tipo `copilot:*`, el backend (`agents.controller.ts:144-177`) ejecuta de verdad la acción pendiente vía `executeApprovedAction`, no solo cambia un estado. `SimulationPanel.tsx` (usado en `/admin/consciousness`) sí pide `window.confirm()` para una decisión comparable; este panel no. [x] Corregido (Crew G, 2026-07-22) — `window.confirm()` antes de aprobar, mismo patrón ya usado en `SimulationPanel.tsx`. Pendiente verificación en vivo.
- **3.17 ALTO** — `/admin/mission-control`: Resolve/Dismiss de señales operacionales (incl. `PAYMENT_BLOCKED`/`DISPUTE_RISK_HIGH`) es un clic sin confirmación, y el backend (`operational-signals.service.ts:131-143`) solo cambia el `status` sin verificar que la condición real ya se corrigió. [x] Corregido (Crew G, 2026-07-22) — `window.confirm()` antes de resolve/dismiss, con el título de la señal en el mensaje. Pendiente verificación en vivo.
- **3.18 ALTO** — `/admin/jobs/[jobId]`: los botones de "Admin override" (forzar `completed`/`cancelled`/`dispute`) disparan la transición al instante, sin confirmación. `jobs/[jobId]/page.tsx:194-207`. [x] Corregido (Crew G, 2026-07-22) — `window.confirm()` antes de cada transición forzada. Pendiente verificación en vivo.
- **3.19 ALTO (dinero)** — `/admin/change-orders`: "Aprobar"/"Rechazar"/"Aplicar a BuildOps" disparan al instante sin confirmación — decisiones de presupuesto/alcance — y además **tragan cualquier fallo del backend en silencio** (`if (res.ok) await load()` sin `else`, el botón simplemente vuelve a su estado normal sin explicar nada). `change-orders/page.tsx:112-164,316-341`. [x] Corregido (Crew G, 2026-07-22) — `window.confirm()` en las 3 acciones; `action()`/`applyToBuildOps()` ahora capturan `res.ok === false` y muestran el error real (reutilizando el `error` state ya existente en la página) en vez de fallar en silencio. Pendiente verificación en vivo.
- **3.20 ALTO** — `/admin/autonomy`: crear branch/aplicar cambio/commit/push/abrir PR se disparan con un clic sin confirmación — "Rehacer smoke staged" encadena automáticamente `change→commit→push→pr` en una sola llamada — y la UI nunca muestra si el run apunta al repo sandbox o a un repo real de GitHub (`SEMSE_AUTONOMY_GITHUB_TOKEN` configurado). `autonomy/page.tsx:444-461,677-747`. [x] Corregido (Crew G, 2026-07-22) — `window.confirm()` en las 3 acciones, con el texto advirtiendo explícitamente que puede tocar un repo real si está configurado. No se implementó mostrar en la UI si el run apunta a sandbox o a repo real (requeriría un nuevo endpoint de solo-lectura para exponer esa config al frontend) — se deja como mejora futura, el confirm ya cierra el riesgo de "un clic sin darse cuenta". Pendiente verificación en vivo.
- **3.21 ALTO** — `/admin/users`: verificar/suspender/activar un usuario se dispara con un clic — sin confirmación y sin mostrar ninguna evidencia (documento, resultado de background check) que sustente el "Verificar". `users/page.tsx:174-196,396-406`. [x] Corregido (Crew G, 2026-07-22) — `window.confirm()` en las 3 acciones, con el nombre/email del usuario en el mensaje. No se agregó ninguna evidencia de respaldo (no existe ese dato en esta pantalla ni en el backend hoy) — el mensaje de confirmación de "Verificar" señala explícitamente esa ausencia. Pendiente verificación en vivo.
- **3.22 ALTO** — `/admin/trust/worker-applications`: aprobar/rechazar una postulación de profesional al marketplace se dispara con un clic, sin confirmación — admite o rechaza a un aspirante sin vetear, de forma permanente. `worker-applications/page.tsx:154-169,233-250`. Hoy enmascarado por 3.13 (403 para todos), pero es un patrón independiente que persistirá una vez arreglado ese permiso. [x] Corregido (Crew G, 2026-07-22) — `window.confirm()` antes de aprobar/rechazar. Pendiente verificación en vivo (bloqueada además por 3.13 hasta esa confirmación).
- **3.23 MEDIO** — `/admin/prometeo`: borrar un documento indexado de la base RAG no pide confirmación — acción permanente. `prometeo/page.tsx:245-248,422-425`. [x] Corregido (Crew G, 2026-07-22) — `window.confirm()` antes de borrar, con el título del documento en el mensaje. Pendiente verificación en vivo.
- **3.24 MEDIO** — `/admin/travel/[travelId]`: "Cerrar liquidación" (finaliza el saldo de reembolso de gastos de un worker) se dispara sin confirmación, junto a `handleActivate`/`handlePending`. `travel/[travelId]/page.tsx:126-164`. [x] Corregido (Crew G, 2026-07-22) — `window.confirm()` antes de "Cerrar liquidación" (la acción de mayor riesgo, ya bloqueada además si faltan comprobantes); `handleActivate`/`handlePending` se dejaron sin confirmación por ser transiciones de estado reversibles de menor consecuencia. Pendiente verificación en vivo.

#### Recurrencias de bugs de causa raíz ya documentados (RC1/RC5), en ubicaciones nuevas

- **3.25 CRÍTICO — recurrencia de RC1, además de un endpoint muerto** — El hub `/admin/workops` compara `job.status` contra `"ACTIVE"`/`"PENDING"`/`"OPEN"` — valores que no existen en el `JobStatus` real ni en minúsculas ni en mayúsculas — y llama `fetch("/api/semse/bids")` con `GET`, ruta que **solo exporta `POST`** (siempre 405). El KPI de "Jobs activos/pendientes" y "bids pendientes" del hub central de WorkOps siempre da 0. `workops/page.tsx:32-51`. [x] Corregido (Crew G, 2026-07-22) — comparaciones alineadas a los valores reales en minúsculas (`in_progress`/`posted`+`published`/`completed`, `submitted` para bids) igual que el resto de la app post-fix-0.0; se agregó `GET /v1/bids` (tenant-wide, permiso dedicado `bids:read:tenant` solo OPS_ADMIN, distinto de `bids:read` que también tienen CLIENT/PRO/WORKER) + su ruta BFF, ya que no existía ningún endpoint de listado de bids a nivel tenant. Pendiente verificación en vivo.
- **3.26 ALTO (dinero) — recurrencia de RC5/2.39/3.8** — El KPI "Volumen escrow" de `/admin/reports` suma **todas** las transacciones de pago sin filtrar por `status`, incluyendo `FAILED`/`REVERSED`. `reports/page.tsx:52-57`. [x] Corregido (Crew G, 2026-07-22) — mismo fix aplicado en `admin/finance`: solo suma transacciones `status === "SUCCEEDED"`. Pendiente verificación en vivo.

#### Datos fabricados o presentados como reales sin serlo

- **3.27 ALTO — el "QA pipeline automatizado" fabrica evidencia de frescura sobre datos posiblemente falsos** — `/admin/qa` arranca con `FALLBACK_CHECKS` hardcodeados (3 filas fake pass/warn/fail); si la carga real falla, el `catch` **sella el timestamp `lastRun` con la hora actual** sobre esos datos de respaldo/obsoletos, sin mostrar ningún error — el admin ve "última ejecución: recién" sobre datos que pueden ser 100% ficticios. `qa/page.tsx:52-54,63,162-169`. [x] Corregido (Crew G, 2026-07-22) — el catch ya no toca `checks`/`lastRefresh`; en su lugar muestra un banner de error explícito aclarando que los checks visibles son de la última corrida exitosa (o datos de respaldo), no confirmados ahora. Pendiente verificación en vivo.
- **3.28 ALTO — "Resolver" de cumplimiento normativo es un no-op que finge un estado legal resuelto** — `/admin/compliance`: `acknowledge()` solo hace `setItems(...)` local, sin ningún `fetch` — un oficial de cumplimiento ve una violación (disputa abierta, worker sin verificar, vencimiento GDPR/fiscal) ponerse en verde sin que nada haya cambiado realmente, hasta el próximo reload manual (esta pantalla no auto-refresca). `compliance/page.tsx:173-175,261`. [x] Corregido (Crew G, 2026-07-22) — se eliminó el botón "Resolver": estos checks se recalculan en vivo desde datos reales en cada `load()` (no son entidades persistidas con un endpoint de "acknowledge" real), así que no hay ninguna acción genuina que el botón pudiera ejecutar — el link "Ver detalle" ya lleva a la pantalla real donde se corrige la causa subyacente, y el botón de recarga ya existente refleja el estado real después de corregirla ahí.
- **3.29 MEDIO — métricas fabricadas mezcladas con telemetría real, sin distinción visual** — `/admin/semse-x`: "Nodos Cognitivos" (`stats.total*1000 + 128402`) y "Amenazas Neutralizadas" (`snapshot.completed*1200 + 24000`) son fórmulas arbitrarias con una base hardcodeada, no conteos reales; "Memoria Comprimida: -84%" y "API en Evolución: v2.0" son literales fijos — todo mostrado junto a paneles genuinamente reales (success rate, health matrix) sin ninguna etiqueta que distinga lo sintético de lo real. `semse-x/page.tsx:140-155,199-203,550-551`. [x] Corregido — se añadió la etiqueta "simulado" junto a "Nodos Cognitivos" y "Amenazas Neutralizadas", se agregó el flag `synthetic` a `MetricCard` para "Memoria Comprimida" y "API en Evolución", y se dejaron los paneles reales (success rate, health matrix, decisiones) sin marca.

#### Fallos silenciados (mismo patrón que 3.9, ubicaciones nuevas)

- **3.30 MEDIO** — `mission-control/page.tsx:329-331` (`buildops/overview`, `prometeo-brief`, `ops/mission-control/summary`) y `reports/page.tsx:48-49,114` usan `.catch(() => null/[]/{...})` sin mostrar ningún error — los widgets/página simplemente muestran guiones o desaparecen, indistinguible de "sin datos todavía". [x] Corregido — `mission-control` usa `Promise.allSettled` y muestra falla parcial; `reports` captura errores por fuente (`disputes`, `autonomy runs`, pagos) y muestra un banner de error. Se eliminan los `.catch` silenciosos.
- **3.31 MEDIO** — `/admin/intelligence-rooms/[id]`: acknowledge/resolve/dismiss actualizan el estado local sin revisar `res.ok` — si el PATCH real falla (403/500), la UI igual marca la señal como resuelta. `intelligence-rooms/[id]/page.tsx:144-166`. [x] Corregido — los tres handlers ahora capturan `res`, solo actualizan el estado local si `res.ok`, y muestran `actionError` en un banner rojo cuando fallan.
- **3.32 BAJO** — `/admin/semse-x` traga los 4 fetches iniciales sin mostrar ningún banner de error (a diferencia de `/admin/developer-runtime`, que sí lo hace). `semse-x/page.tsx:140-155`. [x] Corregido — `load` usa `Promise.allSettled`, detecta fallos por fuente (stats/logs/contexto/coordinator) y muestra un banner rojo en la parte superior de la página.
- **3.33 MEDIO** — `/admin/communications`: cambiar el estado de una conversación (reabrir/cerrar/pendiente) falla en silencio (`catch { /* silent */ }`), sin ningún toast/banner. `communications/page.tsx:447-458`. [x] Corregido — `handleStatusChange` ahora guarda el mensaje de error en `statusError` y lo muestra debajo de los botones de estado; no actualiza el estado local si la llamada falla.

#### Páginas huérfanas (mismo patrón que G-ADM-01/3.1, ubicaciones nuevas)

- **3.34 ALTO** — `/admin/ops/loops` (panel OMEGA de Permanent Loops — pausa/reanuda agentes autónomos de fondo, acepta/rechaza sus propuestas) no está enlazado en ningún menú (`admin-navigation.ts` ni `navigation-registry.ts`), ni siquiera desde su propia página padre `/admin/ops`. Solo alcanzable escribiendo la URL — mayor riesgo que el resto por controlar agentes de fondo (kill switch `AUTONOMY_LOOPS_ENABLED`). [x] Corregido (Crew G, 2026-07-22) — agregado como child de `mission-control`. Pendiente verificación en vivo.
- **3.35 ALTO** — `/admin/travel` (+ `/admin/travel/[travelId]`) — pantalla completa y funcional (triage de viajes, cierre de liquidación) — no tiene entrada en `ADMIN_MODULES` (ni siquiera como child de `workops`, donde sí están `jobs`/`field-ops`/`labor-engine`/`worker`/`contractors`/`change-orders`/`pmo`/`qa`). `navigation-registry.ts` sí la lista pero ese archivo es código muerto (cero imports en toda `apps/web`). [x] Corregido (Crew G, 2026-07-22) — agregado como child de `workops`. Verificación en vivo confirmada (2026-08-01): `/admin/workops` como `admin@demo.semse` muestra la tarjeta "Travel" ("Worker travel assignments, expenses, lodging, advances, and settlements") con link a `/admin/travel`, junto a Labor Engine, Contractors, Change Orders, PMO y QA.
- **3.36 ALTO** — `/admin/product-intelligence` no está en ningún menú de Admin — el propio spec la cita como "control positivo" de gating correcto (G-ADM-07) pero nadie puede llegar a ella desde el sidebar. [x] Corregido (Crew G, 2026-07-22) — agregado como child de `intelligence`. Pendiente verificación en vivo.
- **3.37 ALTO** — `/admin/communications` (SEMSE Inbox — envío real de WhatsApp/SMS a clientes, 1005 líneas) no tiene entrada en `ADMIN_MODULES` — mismo patrón que el resto, pero para una pantalla con capacidad de enviar mensajes reales a clientes. [x] Corregido (Crew G, 2026-07-22) — agregado como child de `trust`. Pendiente verificación en vivo.
- **3.38 BAJO** — `/admin/vision` (top-level, con datos reales) no está en `ADMIN_MODULES` — solo alcanzable en un clic extra desde `/admin/verticals/vision` o `/admin/verticals/construction`. No es un huérfano total, solo un hueco menor de navegación. [x] Corregido — agregado `vision` ("Vision Console") como child del módulo `verticals` en `apps/web/lib/admin/admin-navigation.ts`, con href `/admin/vision`.

#### Otros

- **3.39 MEDIO** — `/admin/worker` promete "Perfiles, asignaciones y disponibilidad" de profesionales (`admin-navigation.ts`, `workops/page.tsx:17`) pero la página real es un monitor de cola de trabajos BullMQ, sin ningún dato de perfil/asignación de worker — no existe ninguna otra ruta admin para eso. [x] Corregido — se optó por relabel: `admin-navigation.ts` ahora dice "Worker Queue" con descripción de monitor BullMQ; el quick link en `workops/page.tsx` cambió a "Worker Queue" / "Monitor de cola BullMQ, handlers y ejecuciones de agentes". La página `/admin/worker` ya tenía el título "Worker — BullMQ".
- **3.40 CRÍTICO — UX honesta** — `/admin/settings` no persiste absolutamente nada — cero llamadas `fetch` en todo el archivo, ~10 controles (idioma, notificaciones, MFA, logs de sesión, integraciones OpenAI/GitHub) son puro `useState` local. El toggle de MFA en particular dice textualmente "Requiere TOTP al iniciar sesión como administrador" — activarlo da una falsa sensación de seguridad reforzada sin cambiar nada real. [x] Corregido — se agregó el modelo `TenantSettings` (migración `20260723000000_tenant_settings`) con `tenantId` único y `settingsJson`; nuevos endpoints `GET/PUT /v1/admin/settings` gated por `ops:dashboard:read/write`; BFF `/api/semse/admin/settings`; y `/admin/settings` ahora carga los valores reales, guarda con debounce y muestra estado de carga/guardado/errores. Los toggles de MFA, session log e integraciones ahora incluyen texto honesto que aclara que la preferencia se persiste pero el enforcement real depende de configuración del servidor (TOTP, middleware de sesiones, variables de entorno). Pendiente verificación en vivo.
  - **Actualización 2026-07-27 — bug real encontrado en la verificación en vivo, corregido:** con credencial OPS_ADMIN real, `/admin/settings` tiraba "Internal server error" en producción. Logs de Railway confirmaron la causa exacta: `The table "public.TenantSettings" does not exist in the current database` — la migración `20260723000000_tenant_settings` nunca se había aplicado a prod (coincide con el problema ya conocido de `db push` vs `prisma migrate deploy`, ver memoria de infra de Railway). Se aplicó la migración directamente contra prod (`prisma db execute` vía un proxy TCP temporal a Postgres, creado y cerrado en la misma sesión, con confirmación explícita del usuario antes de tocar la BD) — es aditiva (`CREATE TABLE`/`CREATE INDEX`/`ADD CONSTRAINT`, sin tocar datos existentes). Verificado en vivo: `/admin/settings` ya carga sin error. **Riesgo pendiente sin resolver:** esta migración se aplicó con `db execute`, no con `migrate deploy`, así que la tabla `_prisma_migrations` de prod no tiene un registro marcándola como aplicada — el próximo deploy que corra `prisma migrate deploy` (el fix de PR #447) puede intentar volver a aplicar esta misma migración y fallar con "la tabla ya existe". Alguien con acceso a prod debería revisar/reconciliar el estado de `_prisma_migrations` antes de confiar en que el próximo deploy corra limpio — no se intentó arreglar esto a mano en esta sesión por no poder verificar el resultado (no hay `psql` en esta máquina para inspeccionar esa tabla).
- **3.41 BAJO** — `/admin/html-in-canvas` promete en su copy de navegación "test custom HTML" pero son 4 demos fijos del spec WICG, sin ningún input para HTML propio del admin. No es un stub fingiendo funcionar — cada demo hace justo lo que muestra — solo una promesa de copy desalineada. [x] Corregido — `admin-navigation.ts` ahora usa el label "HTML-in-Canvas Demos" y la descripción "4 demos fijos del spec WICG; aún no permite ingresar HTML propio."
- **3.42 MEDIO/ALTO** — El botón "Crear señales de prueba" de `/admin/mission-control` siembra filas ficticias reales en la tabla de producción (`OperationalSignal`) sin gate de entorno (`NODE_ENV`) y sin distinción visual — `metadataJson.test: true` se guarda pero el frontend ni siquiera lo lee, así que las señales de prueba quedan mezcladas e indistinguibles de señales reales. `mission-control/page.tsx:648-667`, backend `operational-intelligence.controller.ts:97-151`. [x] Corregido (Crew G, 2026-07-22) — `seedTestSignals` ahora lanza `ForbiddenException` si `NODE_ENV === "production"`, mismo patrón ya usado para `requireBootstrapToken`/`resetTokenPreview`. No se agregó la distinción visual del badge `test` en el frontend (el botón ya no es alcanzable en producción, que era el riesgo principal) — se deja como mejora menor.
- **3.43 ALTO** — El selector de período de `/admin/reports` (Último mes/trimestre/año/Personalizado) es decorativo — el estado nunca se pasa a ninguna de las llamadas de carga de datos (`useEffect` con deps `[]`), así que cambiar de período solo re-etiqueta el mismo dataset (título del reporte y hasta el nombre del CSV exportado mienten sobre el rango real). `reports/page.tsx:33,43-116,141`. [x] Corregido (Crew G, 2026-07-22) — se quitó el selector de períodos falsos (ninguna llamada real soporta rango de fechas hoy) y se reemplazó por una etiqueta honesta ("Histórico completo"). Agregar filtrado real por rango requiere soporte de query params en el backend — no implementado aquí, se deja como trabajo futuro real en vez de una UI que finge filtrar. Pendiente verificación en vivo.
- **3.44 MEDIO** — La métrica "Tiempo promedio cierre" de `/admin/reports` está permanentemente hardcodeada (`"6.2 días" / "-1.1d"`, nunca calculada), presentada con el mismo estilo que las otras 5 métricas que sí son reales. `reports/page.tsx:26,109`. [x] Corregido (Crew G, 2026-07-22) — reemplazada por "—" (mismo placeholder honesto que las métricas aún no implementadas), en vez de un número inventado. Pendiente verificación en vivo.

**Segunda pasada de verificación en vivo (2026-08-02, con OPS_ADMIN real, ~10 pantallas + 2 lecturas de código dirigidas):** re-confirmó la mayoría de lo de arriba (3.1, 3.2, 3.6 visibles en pantalla; G-ADM-07/08 confirmados por código, ver abajo) y encontró 2 hallazgos nuevos no cubiertos por las rondas anteriores:

- **3.45 ALTO — recurrencia de RC1, en `/admin/disputes`** — los 4 KPIs de cabecera (Abiertas/Escaladas/Approval pendiente/Resueltas) muestran `0` en las cuatro categorías mientras la lista debajo muestra una disputa con estado `OPEN` real. Mismo patrón de comparación de status roto que 0.0/3.0, pero en una pantalla que ninguna ronda anterior había marcado. **Nota de confiabilidad:** la única disputa visible en el tenant de prueba tiene datos anómalos (ID `#dispute_` sin sufijo, "no tiene un trabajo vinculado") — podría ser un registro de seed roto en vez de representativo; confirmar con un dato de prueba limpio antes de asumir el mecanismo exacto. [x] Corregido (PR #518, 2026-08-02) — las 5 comparaciones de status del archivo (KPIs, filtro de tabs, `STATUS_CONFIG`, derivación del panel de workspace) se alinearon al enum real `DisputeStatus` (`OPEN/ASSIGNED/UNDER_REVIEW/RESOLVED/REJECTED`); `ASSIGNED`/`UNDER_REVIEW` se mapearon al balde "escalada". Pendiente verificación en vivo post-deploy (la disputa de prueba con datos anómalos sigue sin ser representativa).
- **3.46 MEDIO — recurrencia de 3.5, en `/admin/users`** — al menos 1 de 7 usuarios listados se muestra con un ID crudo (`cmr9ag8ww0002ph01937u...`) en vez de nombre, mientras otros usuarios en la misma lista sí resuelven a nombre real ("Sofi"). El fix de 3.5 (mapear ID → parte local del email) no cubre el caso donde ese fallback también falla. [x] Corregido (PR #518, 2026-08-02) — `deriveDisplayName()` ahora rechaza un nombre de org que calce con el patrón de `cuid()` crudo antes de confiarlo, cayendo al nombre derivado del email. No se tocó el dato de seed subyacente (el org con nombre corrupto sigue así en la BD), solo la defensa en el frontend.

Confirmado también hoy (sin cambios de código, solo verificación): G-ADM-07 (RBAC de `/v1/anatomy` y `/v1/knowledge`) — `anatomy.controller.ts`, `repo-knowledge.controller.ts` y `runtime-knowledge.controller.ts` completos, y los endpoints `domains`/`overview` de `knowledge.controller.ts`, todos gateados por `internal:architecture:read` (solo OPS_ADMIN); el resto de `knowledge.controller.ts` sigue en `knowledge:read` compartido a propósito (RAG legítimo para CLIENT/PRO/WORKER). G-ADM-08 (upload) — `uploads.controller.ts` línea 174 usa `resolveRequestContext(req)`, no el header `x-tenant-id`, con comentario explicando el fix; la descarga pública sigue sin firma/expiración pero ahora es diseño documentado (claves UUID no enumerables), no un descuido.

**Pendiente antes de poder cerrar este módulo:**
- [x] Conseguir credencial de OPS_ADMIN para repetir la navegación en vivo completa — verificación realizada el 2026-07-27 con la cuenta demo documentada, sin ejecutar acciones mutantes.
- [x] Lanzar la misma ronda de agentes enfocada en `apps/web/app/(app)/admin/**` — Crew G ya cubrió los hallazgos de código; esta sesión añadió una ronda E2E de navegación sobre las 59 páginas actuales (incluidas rutas dinámicas con IDs de prueba): 59/59 PASS, sin errores de render ni redirecciones.
- [x] Verificar en vivo el bug de "cero en 5 pantallas" (1.4) desde el lado admin — el 2026-07-27 Admin mostró 3 trabajos activos de 12 en `/admin/dashboard` y `/admin/jobs`; `/admin/workops` mostró 1 activo y 8 pendientes. El cero de `/admin/labor-engine` correspondió a 0 timers activos reales, no al conteo de trabajos.

**Re-verificación de código (2026-07-29):** tras los merges grandes de fines de julio (PR #470 agro offline, #471 sidebar Labor Engine, #472 Project Lifecycle Projection), se re-leyó el código actual de 3.0, 3.1, 3.2, 3.3, 3.5, 3.17, 3.18, 3.23, 3.26, 3.36, 3.37, 3.38, 3.41, 3.44 — los 14 siguen con el comportamiento descrito, sin regresión de esos merges (`git show d065a2f2 --stat` confirma que el PR #472 no toca ninguno de los archivos de estos hallazgos). Ítem **3.1 doblemente confirmado**: tanto el fix viejo de Crew G en `admin-navigation.ts` como el nuevo de hoy en el sidebar persistente (`layout.tsx`, PR #471) siguen presentes. **Ojo:** esto NO reemplaza la verificación en vivo con credencial OPS_ADMIN que el spec `admin-flows-remediation.spec.md` exige antes de poder considerar estos ítems realmente cerrados — es una confirmación de que el código no se rompió, no una confirmación visual nueva. No se pudo usar navegador esta sesión.

---

## Cosas ya confirmadas que funcionan bien (no perder tiempo re-revisando)

- RBAC guard deny-by-default — sólido, todos los endpoints mutantes revisados tienen guardia.
- Rate limiting en auth — bien aplicado (throttler global + límites específicos por endpoint).
- Enumeración de credenciales — mensajes de error genéricos, confirmado en vivo.
- Password reset — no filtra si un email existe.
- Contratos (`sign()`) — valida identidad correcta por slot de firma, idempotente.
- Reservas `create()` — race-safe vía índice único parcial en Postgres.
- Outbox v2 (ruta de evidencia) — transaccional, `FOR UPDATE SKIP LOCKED`, idempotente.
- Notificaciones — bien desacopladas, un fallo de envío no corrompe la transacción de negocio.
- SQL crudo y secretos hardcodeados — sweep completo del repo, limpio.
- XSS / `dangerouslySetInnerHTML` con contenido de usuario — no encontrado.
- Token de sesión — nunca toca `localStorage`, cookie `HttpOnly` firmada.
- Redirect `?from=` tras login — validado en cliente y servidor.
- Vinculación de cuentas Stripe Connect — no hay path de secuestro vía callback.
- Path traversal en storage — bloqueado correctamente.
- Vision-service — timeout/retry del lado cliente bien manejado (aunque el servicio en sí no tiene auth, ver 0.10).
- Fórmulas de materiales (pintura, drywall, piso, mulch) — estándar de construcción real, redondeo correcto (excepto madera, ver 0.30).
- Worker: tenant-scoping en los handlers de jobs — limpio.
- SSE: los 4 canales que no son `planStream`/`delegationsStream` sí derivan el tenant correctamente.
- `/admin/product-intelligence` — correctamente gateado (OPS_ADMIN + kill switch).
- autonomy-server — herramienta interna pura, sin superficie de ataque real.

---

## Notas de contexto (por si se pierde el hilo entre sesiones)

- La sesión de auditoría se hizo con una cuenta de prueba en el rol **Cliente**, contra `https://semse-web-production.up.railway.app` (producción real). No se disparó ningún botón que mueva dinero real ni resuelva disputas reales — esos hallazgos están confirmados solo por lectura de código.
- El proyecto Railway es **SEMSEproject** (id `95ad1b14-d1d9-467d-82d8-5354619ba873`), ambiente `production`, servicio API es **semse-API** (id `2bd1bd3f-9aa1-4fc3-a714-4cf90c52c770`).
- Paralelamente a esta auditoría, en la misma sesión se completó y mergeó a `main` el trabajo de **F2 — Prometeo Tool Registry gobernado** (gate híbrido de pagos), que no tiene relación con estos hallazgos.
 - La cuenta profesional real `[REDACTED_ACCOUNT_EMAIL]` (rol `PRO`) quedó bloqueada por el bug **0.32** (reset de contraseña no envía correo) y se desbloqueó a mano el 2026-07-20 escribiendo un hash de contraseña temporal directo en la base de datos de producción, como parche de emergencia — no como fix del bug real. **Se repitió el mismo bloqueo el 2026-07-21** (el usuario volvió a intentar "olvidé mi contraseña" varias veces, sin éxito por la misma causa) y se desbloqueó otra vez de la misma forma — evidencia de que 0.32 sigue activo y no es un evento aislado. Cada contraseña temporal se le dio al dueño de la cuenta una sola vez en el chat y no quedó guardada en ningún archivo (los scripts usados para generarla/escribirla se borraron después de cada uso). Debe revocarse/reemplazarse por un cambio real en cuanto el flujo de recuperación funcione.
