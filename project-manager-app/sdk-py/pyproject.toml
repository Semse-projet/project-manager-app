[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "semse-sdk"
version = "0.1.0"
description = "SEMSE satellite SDK — Python client for the /v1 API (SAT-001)"
requires-python = ">=3.9"
dependencies = []

[tool.setuptools.packages.find]
include = ["semse_sdk*"]
