coverage:
  status:
    project:
      default:
        target: 90%
        threshold: 1%
    patch:
      default:
        target: 85%
        threshold: 1%

comment:
  layout: "reach,diff,flags,files"
  require_changes: false
  after_n_builds: 1

ignore:
  - "tests/**"
  - "playwright.config.js"
